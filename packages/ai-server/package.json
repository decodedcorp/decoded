{
  "name": "@decoded/ai-server",
  "private": true,
  "scripts": {
    "dev": "uv run python -m src.main",
    "test": "uv run pytest",
    "lint": "uv run flake8 src",
    "format": "uv run black ."
  }
}
