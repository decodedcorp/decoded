---
phase: quick
plan: 260402-jg7
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/supabase/queries/warehouse-entities.server.ts
  - packages/web/lib/supabase/queries/main-page.server.ts
  - packages/web/app/page.tsx
autonomous: true
requirements: [QUICK-warehouse-enrichment]

must_haves:
  truths:
    - "Trending keywords section shows artist/group names from warehouse.artists and warehouse.groups with profile_image_url"
    - "Hero and grid sections display artist profile images from warehouse when available"
    - "Existing public.posts queries continue working as fallback when warehouse data is empty"
  artifacts:
    - path: "packages/web/lib/supabase/queries/warehouse-entities.server.ts"
      provides: "Warehouse entity query functions for artists, groups"
      exports: ["fetchWarehouseArtists", "fetchWarehouseGroups", "buildArtistProfileMap"]
    - path: "packages/web/lib/supabase/queries/main-page.server.ts"
      provides: "Updated fetchTrendingKeywordsServer using warehouse entities"
    - path: "packages/web/app/page.tsx"
      provides: "Main page enriched with warehouse artist profile images"
  key_links:
    - from: "packages/web/lib/supabase/queries/warehouse-entities.server.ts"
      to: "warehouse.artists, warehouse.groups"
      via: "createWarehouseServerClient"
      pattern: "wh\\.from\\(.(artists|groups).\\)"
    - from: "packages/web/app/page.tsx"
      to: "warehouse-entities.server.ts"
      via: "import { buildArtistProfileMap }"
      pattern: "buildArtistProfileMap"
---

<objective>
Enrich main page sections with warehouse entity data (artists, groups) instead of deriving entity info from public.posts text columns.

Purpose: The warehouse has proper artist/group entities with profile_image_url and i18n names (67 artists, 39 groups). Currently the main page derives artist names by counting occurrences in public.posts.artist_name text — unreliable and missing profile images.

Output: Trending keywords powered by warehouse entities with profile images; artist profile image enrichment available to hero/grid sections.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/web/lib/supabase/warehouse.ts
@packages/web/lib/supabase/warehouse-types.ts
@packages/web/lib/supabase/queries/main-page.server.ts
@packages/web/app/page.tsx
@docs/agent/warehouse-schema.md

<interfaces>
<!-- Warehouse client -->
From packages/web/lib/supabase/warehouse.ts:
```typescript
export async function createWarehouseServerClient(): Promise<SupabaseClient<WarehouseDatabase, "warehouse">>;
```

From packages/web/lib/supabase/warehouse-types.ts:
```typescript
export type ArtistRow = { id: string; name_ko: string | null; name_en: string | null; profile_image_url: string | null; primary_instagram_account_id: string | null; metadata: Json | null; created_at: string; updated_at: string; };
export type GroupRow = { id: string; name_ko: string | null; name_en: string | null; profile_image_url: string | null; primary_instagram_account_id: string | null; metadata: Json | null; created_at: string; updated_at: string; };
```

From packages/web/lib/supabase/queries/main-page.server.ts:
```typescript
export interface TrendingKeyword { id: string; label: string; href: string; image?: string; }
export async function fetchTrendingKeywordsServer(limit?: number): Promise<TrendingKeyword[]>;
```

From packages/web/lib/components/main/TrendingListSection.tsx:
```typescript
export interface TrendingKeywordItem { id: string; label: string; href: string; image?: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create warehouse entity query module + rewrite fetchTrendingKeywordsServer</name>
  <files>packages/web/lib/supabase/queries/warehouse-entities.server.ts, packages/web/lib/supabase/queries/main-page.server.ts</files>
  <action>
1. Create `warehouse-entities.server.ts` with these functions:

   a) `fetchWarehouseArtists(limit = 50)` — Query `warehouse.artists` for all artists with profile_image_url. Select `id, name_ko, name_en, profile_image_url`. Return `ArtistRow[]`. Sort by `created_at` desc.

   b) `fetchWarehouseGroups(limit = 50)` — Same pattern for `warehouse.groups`. Return `GroupRow[]`.

   c) `buildArtistProfileMap()` — Fetches both artists and groups, returns a `Map<string, { name: string; profileImageUrl: string | null }>` keyed by LOWERCASED name (both name_ko and name_en as separate keys pointing to same entry). This map is used to look up profile images when we only have a text name from public.posts. Example: artist with name_ko="카리나", name_en="Karina" creates two map entries both pointing to the same profile_image_url.

   All functions use `createWarehouseServerClient` from `@/lib/supabase/warehouse`. Wrap in try/catch, return empty array/map on error with console.error.

2. Rewrite `fetchTrendingKeywordsServer` in `main-page.server.ts`:

   - Import `buildArtistProfileMap` from `./warehouse-entities.server`
   - First, call `buildArtistProfileMap()` to get the name-to-profile map
   - Then run the existing public.posts query to get artist_name frequency counts (keep this logic — it determines trending order)
   - When building TrendingKeyword objects, look up each artist name in the profile map. If found, use `profileImageUrl` as the `image` field instead of `post.thumbnail_url`
   - Fallback: if warehouse has no match for an artist, keep using `thumbnail_url` from posts (existing behavior)
   - The `label` should prefer the warehouse name (proper casing) over the raw posts text if a match is found

   This is a HYBRID approach: trending order from public.posts frequency, entity data from warehouse.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo && bunx tsc --noEmit --project packages/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - warehouse-entities.server.ts exports fetchWarehouseArtists, fetchWarehouseGroups, buildArtistProfileMap
    - fetchTrendingKeywordsServer uses warehouse profile images when available, falls back to post thumbnails
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Enrich main page hero/grid with warehouse artist profile images</name>
  <files>packages/web/app/page.tsx</files>
  <action>
1. In `page.tsx`, import `buildArtistProfileMap` from `@/lib/supabase/queries/warehouse-entities.server`.

2. Add `buildArtistProfileMap()` to the existing `Promise.all` block (line ~82) as the 11th parallel fetch. Destructure as `artistProfileMap`.

3. Create a helper function `enrichArtistName(name: string | null, map: Map)` that:
   - Takes a name string (from public.posts.artist_name or group_name)
   - Looks up in the profile map (lowercase match)
   - Returns `{ displayName: string; profileImageUrl: string | null }` with the warehouse name if found, original name if not

4. Update `heroData` construction (line ~106): Add a new field `artistProfileImageUrl` from the profile map lookup on `featuredPost.artistName`. This requires checking if MainHeroData interface accepts it — if not, skip this sub-step and just use the enrichment for trending keywords only.

5. Update `gridItems` construction (line ~131): For each grid item's `title` field, use `enrichArtistName` to get the proper display name from warehouse.

6. Update `spotlightStyles` construction (line ~145): Use `enrichArtistName` for the `title` and `artistName` fields.

7. Update `whatsNewStyles` construction (line ~154): Same enrichment for `title` and `artistName` fields.

8. Do NOT change the data fetching for posts/spots/solutions — only enrich the display names and add profile image availability.

Keep all existing fallback behavior intact. If `artistProfileMap` is empty (warehouse down or no data), everything works exactly as before.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo && bunx tsc --noEmit --project packages/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - Main page parallel-fetches artist profile map from warehouse
    - Grid items, spotlight styles, and whatsNew styles use enriched artist names from warehouse
    - Fallback to original public.posts names when warehouse has no match
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd packages/web && bunx tsc --noEmit`
2. Dev server starts without errors: `cd packages/web && bun dev` (manual check)
3. Main page loads and trending keywords show profile images (when warehouse has data)
4. Main page works identically when warehouse returns empty (graceful fallback)
</verification>

<success_criteria>
- Trending keywords section uses warehouse artist/group profile_image_url instead of post thumbnail_url
- Artist names in grid/spotlight/whatsNew are enriched with proper warehouse entity names
- Zero regression — all existing public.posts queries still work as primary data source
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260402-jg7-warehouse/260402-jg7-SUMMARY.md`
</output>
