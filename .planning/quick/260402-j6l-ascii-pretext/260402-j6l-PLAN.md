---
phase: quick
plan: 260402-j6l
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/DecodedLogo.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "ASCII logo renders 'decoded' text with consistent character spacing"
    - "Text canvas uses pretext prepare() for font metric measurement"
    - "Visual output is at least as clear as before the change"
  artifacts:
    - path: "packages/web/lib/components/DecodedLogo.tsx"
      provides: "ASCII logo with pretext-based text measurement"
      contains: "prepare"
  key_links:
    - from: "packages/web/lib/components/DecodedLogo.tsx"
      to: "@chenglou/pretext"
      via: "import { prepare, layout }"
      pattern: "import.*prepare.*from.*@chenglou/pretext"
---

<objective>
Replace basic Canvas `measureText()` in DecodedLogo.tsx with `@chenglou/pretext`'s `prepare()` + `layout()` for more accurate font metrics in the CanvasTxt text renderer.

Purpose: pretext uses Canvas-based arithmetic that is more consistent across browsers and provides accurate font metrics without relying on the browser's often-imprecise `measureText()` baseline calculations. This improves vertical text positioning on the source canvas, which feeds into the ASCII shader for clearer character output.

Output: Updated DecodedLogo.tsx using pretext for text measurement.
</objective>

<execution_context>
@$HOME/.claude-pers/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/web/lib/components/DecodedLogo.tsx
@packages/web/lib/hooks/usePretext.ts
@packages/web/lib/components/Header.tsx
</context>

<interfaces>
<!-- From @chenglou/pretext (already installed, used in usePretext.ts) -->
```typescript
// prepare(text, font) returns an opaque handle for layout()
// layout(handle, containerWidth, lineHeight) returns { height, lineCount }
import { prepare, layout } from "@chenglou/pretext";
```

<!-- Current CanvasTxt class measurement (to be replaced) -->
```typescript
// Lines 274-287: CanvasTxt.resize() uses context.measureText()
// Lines 290-303: CanvasTxt.render() uses context.measureText() for yPos

// Lines 141-167: AsciiFilter.reset() uses context.measureText("A").width for column calculation
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Integrate pretext into CanvasTxt and AsciiFilter measurement</name>
  <files>packages/web/lib/components/DecodedLogo.tsx</files>
  <action>
Import `prepare` and `layout` from `@chenglou/pretext` at the top of DecodedLogo.tsx.

**CanvasTxt class changes (lines 247-317):**

1. In `resize()` method: After setting `this.context.font`, use `prepare(this.txt, this.font)` to get a handle, then `layout(handle, Infinity, this.fontSize)` to get text metrics. The `Infinity` width ensures single-line (no wrap). Use the returned `height` for canvas height sizing. Keep the existing `measureText().width` for canvas width since pretext doesn't expose individual character widths -- but add a 2px buffer to width for sub-pixel rounding safety.

2. In `render()` method: Use `prepare()` to get consistent ascent positioning. Compute `yPos` using the pretext-measured height minus a small padding offset instead of relying on `actualBoundingBoxAscent` which varies across browsers. Specifically:
   - Get handle via `prepare(this.txt, this.font)`
   - Get `{ height }` via `layout(handle, Infinity, this.fontSize)`
   - Set `yPos = height - 10` (padding offset) as baseline position
   - This replaces `10 + metrics.actualBoundingBoxAscent`

3. Remove the unused `rightMargin` variable in `render()` (line 300) -- it's computed but never used.

**AsciiFilter class changes (lines 141-167):**

Keep `measureText("A").width` in AsciiFilter since pretext's API returns height/lineCount, not character widths. The pretext integration is only valuable in CanvasTxt where vertical positioning accuracy matters for the source text that gets ASCII-converted.

**Summary of actual changes:**
- Add `import { prepare, layout } from "@chenglou/pretext"` at top
- CanvasTxt.resize(): Use pretext layout height for canvas height calculation
- CanvasTxt.render(): Use pretext-derived height for more consistent yPos baseline
- Clean up unused `rightMargin` variable
- AsciiFilter: Leave measureText as-is (pretext doesn't expose char widths)
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo && bunx tsc --noEmit --project packages/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>DecodedLogo.tsx imports pretext, CanvasTxt uses prepare()+layout() for text height measurement and vertical positioning, TypeScript compiles cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of ASCII logo</name>
  <files>packages/web/lib/components/DecodedLogo.tsx</files>
  <action>
Verify the ASCII logo renders correctly after pretext integration.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo && bunx tsc --noEmit --project packages/web/tsconfig.json 2>&1 | head -5</automated>
  </verify>
  <done>ASCII logo visually confirmed working with no regressions</done>
  <what-built>Replaced CanvasTxt text measurement with @chenglou/pretext for more accurate font metrics in the ASCII logo rendering</what-built>
  <how-to-verify>
    1. Run `cd /Users/kiyeol/development/decoded/decoded-monorepo && bun --filter web dev`
    2. Open browser to the app's mobile view (< 768px width) or homepage
    3. Verify the ASCII "decoded" logo renders in the header
    4. Check that text is properly centered and characters are clearly defined
    5. Compare with the previous rendering -- text positioning should be equal or better
    6. Test on both Chrome and Safari if possible for cross-browser consistency
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual regressions</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- ASCII logo visually renders correctly in mobile header
- No console errors related to pretext or text measurement
</verification>

<success_criteria>
- DecodedLogo.tsx uses @chenglou/pretext for CanvasTxt text measurement
- ASCII logo renders "decoded" text clearly with proper centering
- No visual regression from current rendering
</success_criteria>

<output>
After completion, create `.planning/quick/260402-j6l-ascii-pretext/260402-j6l-SUMMARY.md`
</output>
