---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/detail/ImageDetailModal.tsx
  - packages/web/lib/hooks/useImageModalAnimation.ts
autonomous: true
requirements: [QUICK-MOBILE-BOTTOMSHEET]

must_haves:
  truths:
    - "Mobile: modal slides up as bottom sheet covering ~90% of viewport, not full screen"
    - "Mobile: drag handle visible at top of sheet for affordance"
    - "Mobile: drag down gesture closes the modal (existing 100px threshold preserved)"
    - "Desktop: right-side drawer behavior unchanged"
    - "Mobile: rounded top corners on the sheet"
  artifacts:
    - path: "packages/web/lib/components/detail/ImageDetailModal.tsx"
      provides: "Bottom sheet layout for mobile, drawer for desktop"
      contains: "rounded-t"
    - path: "packages/web/lib/hooks/useImageModalAnimation.ts"
      provides: "GSAP animation targeting ~90vh instead of full screen on mobile"
      contains: "90"
  key_links:
    - from: "ImageDetailModal.tsx"
      to: "useImageModalAnimation.ts"
      via: "hook return values (handleTouchStart/Move/End, handleClose)"
      pattern: "useImageModalAnimation"
---

<objective>
Convert the mobile ImageDetailModal from full-screen takeover to a bottom-sheet style presentation.

Purpose: On mobile, the current modal covers 100% of the screen like a page navigation, losing the "modal" feeling. A bottom sheet (slide-up, rounded top corners, drag handle, ~90vh height) preserves context and feels native.

Output: Modified ImageDetailModal and useImageModalAnimation that render as a bottom sheet on mobile while keeping desktop drawer behavior identical.
</objective>

<execution_context>
@$HOME/.claude-pers/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/web/lib/components/detail/ImageDetailModal.tsx
@packages/web/lib/hooks/useImageModalAnimation.ts
@packages/web/lib/design-system/bottom-sheet.tsx (reference for styling conventions)
@packages/web/app/@modal/(.)posts/[id]/page.tsx
</context>

<interfaces>
<!-- Key types the executor needs -->

From useImageModalAnimation.ts:
```typescript
export interface UseImageModalAnimationReturn {
  handleClose: () => void;
  handleTouchStart: (e: React.TouchEvent) => void;
  handleTouchMove: (e: React.TouchEvent) => void;
  handleTouchEnd: () => void;
  handleMaximize: () => void;
  isClosing: boolean;
  isMaximizing: boolean;
}
```

From bottom-sheet.tsx (styling reference only, NOT composing):
- Background: #242424, rounded-t-[20px]
- Drag handle: 40x4px #3D3D3D rounded
- Safe area bottom padding: `pb-[env(safe-area-inset-bottom,0px)]`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Convert mobile modal to bottom-sheet style</name>
  <files>
    packages/web/lib/components/detail/ImageDetailModal.tsx
    packages/web/lib/hooks/useImageModalAnimation.ts
  </files>
  <action>
**ImageDetailModal.tsx changes (mobile only, preserve all desktop behavior):**

1. Change the drawer `<aside>` mobile classes:
   - Remove `h-full` on mobile. Instead use `h-[90vh]` (or `max-h-[90vh]`) so the sheet does NOT cover the full viewport.
   - Add `rounded-t-[20px]` on mobile (no rounding on desktop). Use responsive: `rounded-t-[20px] md:rounded-none`.
   - Keep `w-full` on mobile.
   - The parent container already has `items-end` which aligns the sheet to the bottom -- this is correct for bottom-sheet behavior.

2. Add a drag handle indicator (mobile only) inside the `<aside>`, BEFORE the scroll container:
   ```tsx
   {/* Drag handle - mobile only */}
   <div className="md:hidden flex items-center justify-center py-3 shrink-0">
     <div className="w-10 h-1 bg-[#3D3D3D] rounded-sm" />
   </div>
   ```
   This matches the existing BottomSheet design system handle (40x4px, #3D3D3D).

3. Add `overflow-hidden` and `rounded-t-[20px] md:rounded-none` to the aside to clip content within rounded corners.

4. Ensure the action buttons (close, maximize, report) remain properly positioned within the new shorter sheet height on mobile.

**useImageModalAnimation.ts changes:**

5. In the mount animation effect (line ~76-78), change the mobile initial position from `y: "100%"` to animate to a final position that leaves a gap at the top. Since the drawer element itself is now `h-[90vh]` and the container uses `items-end`, the drawer will naturally sit at the bottom with 10vh visible above. The GSAP animation just needs to slide from `y: "100%"` to `y: "0%"` as before -- the height constraint on the element handles the rest.

6. No changes needed to touch gesture handlers -- they already work on Y-axis for mobile and the 100px close threshold is appropriate.

**What NOT to change:**
- Desktop drawer behavior (right side, full height, 50vw/600-700px width)
- Floating image on desktop
- Touch gesture logic (already works correctly for bottom sheet)
- Close/maximize/escape handlers
- Content rendering (renderContent function)
- Do NOT use the BottomSheet component -- it has snap-point behavior unsuitable for this full-content modal. Only borrow its visual styling conventions.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo && npx tsc --noEmit --project packages/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - Mobile (< 768px): modal appears as bottom sheet with ~90vh height, rounded top corners, visible drag handle, backdrop visible above
    - Mobile: drag-to-close gesture still works (swipe down > 100px closes)
    - Desktop (>= 768px): right-side drawer behavior completely unchanged
    - No TypeScript errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Bottom sheet style modal on mobile -- slides up from bottom, rounded corners, drag handle, 90vh height with backdrop visible above</what-built>
  <how-to-verify>
    1. Run dev server: `cd packages/web && bun dev`
    2. Open browser, resize to mobile width (< 768px) or use Chrome DevTools mobile emulation
    3. Navigate to Explore page
    4. Click any post cell
    5. Verify: modal slides up from bottom, does NOT cover full screen (backdrop/blur visible at top)
    6. Verify: drag handle bar visible at top of sheet
    7. Verify: swipe down on the sheet closes it
    8. Verify: close button and maximize button still work
    9. Resize to desktop (>= 768px), click a cell
    10. Verify: right-side drawer behavior unchanged (full height, slides from right)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Mobile: bottom sheet with gap at top, rounded corners, drag handle
- Desktop: unchanged right-side drawer
- All existing functionality preserved (close, maximize, escape, drag gesture)
</verification>

<success_criteria>
- Mobile modal presents as bottom sheet (not full-screen page)
- Visual gap at top reveals backdrop behind
- Drag handle provides clear affordance for swipe-to-close
- Desktop experience identical to before
</success_criteria>

<output>
After completion, create `.planning/quick/260402-p1w-explore-mobile-bottom-sheet-modal/260402-p1w-SUMMARY.md`
</output>
