---
phase: 54-card-enrichment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/explore/ExploreCardCell.tsx
  - packages/web/app/explore/ExploreClient.tsx
autonomous: true
requirements:
  - CARD-02

must_haves:
  truths:
    - "Editorial 탭(hasMagazine=true)의 카드에 post_magazine_title이 하단 오버레이로 표시된다"
    - "post_magazine_title이 null인 경우 오버레이가 표시되지 않는다 (방어적 fallback 유지)"
    - "일반 Explore 탭(hasMagazine=false) 카드에는 editorialTitle 오버레이가 표시되지 않는다"
  artifacts:
    - path: "packages/web/lib/components/explore/ExploreCardCell.tsx"
      provides: "editorialTitle 오버레이 (lines 101-121) 이미 구현됨 — 회귀 없음 확인"
      contains: "item?.editorialTitle"
    - path: "packages/web/app/explore/ExploreClient.tsx"
      provides: "hasMagazine gate가 editorialTitle 전달을 올바르게 제어함"
      contains: "hasMagazine && item.title"
  key_links:
    - from: "packages/web/lib/hooks/useImages.ts"
      to: "PostGridItem.title"
      via: "post.post_magazine_title ?? post.title ?? null at line 236-238"
      pattern: "post_magazine_title"
    - from: "packages/web/app/explore/ExploreClient.tsx"
      to: "GridItem.editorialTitle"
      via: "hasMagazine && item.title conditional spread"
      pattern: "editorialTitle"
    - from: "packages/web/lib/components/explore/ExploreCardCell.tsx"
      to: "overlay div"
      via: "item?.editorialTitle && render"
      pattern: "editorialTitle"
---

<objective>
Editorial 카드의 `post_magazine_title` 오버레이 데이터 플로우를 검증하고, null 방어 로직을 확인한다. 대부분이 검증 작업이며 실제 코드 변경은 최소화된다.

Purpose: `hasMagazine=true` 필터로 가져온 post의 매거진 타이틀이 카드 하단에 오버레이로 표시됨을 보장한다.
Output: 검증 로그/주석 추가 (코드 변경 최소), 회귀 테스트 확인
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/54-card-enrichment/54-RESEARCH.md

<interfaces>
<!-- 이미 구현된 editorial overlay 계약 — 검증 참조 -->

From packages/web/lib/components/explore/ExploreCardCell.tsx (lines 101-121):
```tsx
{/* Editorial 타이틀 오버레이 - 검은 스킴 + 텍스트 아웃라인으로 어떤 배경에서도 선명하게 */}
{item?.editorialTitle && (
  <div className="absolute inset-x-0 bottom-0">
    <div
      className="absolute inset-x-0 bottom-0 h-20 bg-gradient-to-t from-black/95 via-black/80 to-transparent"
      aria-hidden
    />
    <div className="absolute inset-x-0 bottom-0 px-3.5 pb-3 pt-6">
      <p
        className="line-clamp-2 text-[13px] font-semibold leading-[1.35] tracking-tight text-white antialiased"
        style={{
          textShadow: "0 0 2px rgba(0,0,0,1), ...",
        }}
      >
        {item.editorialTitle}
      </p>
    </div>
  </div>
)}
```

From packages/web/app/explore/ExploreClient.tsx (lines 78-89):
```typescript
const gridItems: GridItem[] = useMemo(() => {
  return items
    .filter((item) => item.imageUrl != null)
    .map((item) => ({
      // ...
      ...(hasMagazine && item.title && { editorialTitle: item.title }),
    }));
}, [items, hasMagazine]);
```

From packages/web/lib/hooks/useImages.ts (lines 235-238):
```typescript
// editorial 오버레이: 매거진 타이틀 우선, 없으면 post.title
title:
  post.post_magazine_title ?? post.title ?? null,
```

데이터 플로우 결론:
- `hasMagazine=true` 필터 → Supabase `.not("post_magazine_id", "is", null)` → `post_magazine_title`이 항상 있는 행만 반환
- `useInfinitePosts` maps → `title = post.post_magazine_title ?? post.title ?? null`
- `ExploreClient` passes → `editorialTitle = item.title` (when `hasMagazine && item.title`)
- `ExploreCardCell` renders → overlay when `item.editorialTitle` is truthy
- null 방어: `item.title`이 null이면 spread 생략 → `editorialTitle` 전달 안 됨 → overlay 미노출
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 데이터 플로우 코드 검증 + null 방어 주석 보강</name>
  <files>
    packages/web/lib/hooks/useImages.ts,
    packages/web/app/explore/ExploreClient.tsx
  </files>
  <action>
**검증 1: useImages.ts — post_magazine_title 매핑 확인**

`packages/web/lib/hooks/useImages.ts` line 233-238을 읽어 현재 상태 확인:
- `spotCount: post.spot_count ?? 0` (54-01에서 수정됨 — 순서상 54-01이 먼저 실행되어야 하나 wave가 동일하므로 독립적으로 실행 가능)
- `title: post.post_magazine_title ?? post.title ?? null` 존재 여부 확인

만약 `title` 매핑이 예상과 다르다면 수정. 예상대로라면 주석 강화:
```typescript
// editorial 오버레이: hasMagazine=true 시 post_magazine_title이 항상 non-null임
// (Supabase 필터: .not("post_magazine_id", "is", null) 보장)
// 방어적 fallback: post_magazine_title 없는 경우 post.title 사용, 둘 다 없으면 null
title: post.post_magazine_title ?? post.title ?? null,
```

**검증 2: ExploreClient.tsx — hasMagazine gate 확인**

`packages/web/app/explore/ExploreClient.tsx` line 78-90 읽기:
- `...(hasMagazine && item.title && { editorialTitle: item.title })` 존재 여부 확인
- 조건 로직 검토: `hasMagazine=false`면 editorialTitle 전달 안 됨 (올바름)
- `item.title`이 null이면 spread 생략 (올바름)

변경 불필요하면 코드 그대로 유지. gate 로직이 다르다면 수정:
```typescript
...(hasMagazine && item.title != null && { editorialTitle: item.title }),
```
(`item.title && ...`은 빈 문자열도 걸러내므로 `item.title != null`로 더 명시적으로 변경 권장)

**검증 3: ExploreCardCell.tsx — overlay 구현 확인**

line 101-121 읽기. `{item?.editorialTitle && (...)}` 조건 존재 확인.
변경 없음 — 기존 구현 올바름.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo/packages/web && grep -n "post_magazine_title\|editorialTitle\|hasMagazine" lib/hooks/useImages.ts app/explore/ExploreClient.tsx lib/components/explore/ExploreCardCell.tsx 2>&1</automated>
  </verify>
  <done>
    - `useImages.ts`에 `post.post_magazine_title ?? post.title ?? null` 매핑 존재 확인
    - `ExploreClient.tsx`에 `hasMagazine && item.title` gate 존재 확인
    - `ExploreCardCell.tsx`에 `item?.editorialTitle` overlay 조건 존재 확인
  </done>
</task>

<task type="auto">
  <name>Task 2: Editorial 오버레이 회귀 테스트 추가 + 최종 타입체크</name>
  <files>
    packages/web/lib/components/explore/__tests__/ExploreCardCell.test.tsx
  </files>
  <action>
54-01의 테스트 파일(`__tests__/ExploreCardCell.test.tsx`)이 이미 존재한다면 CARD-02 관련 테스트 케이스를 추가한다. 파일이 없다면 신규 생성한다.

추가할 테스트 케이스:

```typescript
describe("Editorial title overlay (CARD-02)", () => {
  it("editorialTitle이 있으면 오버레이 텍스트가 렌더링된다", () => {
    const item = makeItem({ editorialTitle: "Vogue Korea May 2026" });
    render(<ExploreCardCell {...makeProps(item)} />);
    expect(screen.getByText("Vogue Korea May 2026")).toBeInTheDocument();
  });

  it("editorialTitle이 null이면 오버레이가 없다", () => {
    const item = makeItem({ editorialTitle: null });
    render(<ExploreCardCell {...makeProps(item)} />);
    expect(screen.queryByText(/Vogue/)).not.toBeInTheDocument();
  });

  it("editorialTitle이 undefined이면 오버레이가 없다", () => {
    const item = makeItem({ editorialTitle: undefined });
    render(<ExploreCardCell {...makeProps(item)} />);
    // overlay div가 없음 — gradient div가 없어야 함
    expect(document.querySelector(".from-black\\/95")).toBeNull();
  });
});
```

테스트 실행 후 PASS 확인. 이후 TypeScript 타입체크:
```bash
cd /Users/kiyeol/development/decoded/decoded-monorepo/packages/web
bun run typecheck
```

에러 있으면 수정. 없으면 완료.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo/packages/web && bun run test --run -- ExploreCardCell 2>&1 | tail -30</automated>
  </verify>
  <done>
    editorial overlay 관련 테스트 3개 GREEN.
    TypeScript 타입 에러 0건.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "post_magazine_title" packages/web/lib/hooks/useImages.ts` — 매핑 존재
2. `grep -n "editorialTitle" packages/web/app/explore/ExploreClient.tsx` — hasMagazine gate 존재
3. `grep -n "editorialTitle" packages/web/lib/components/explore/ExploreCardCell.tsx` — overlay 조건 존재
4. `bun run test --run -- ExploreCardCell` — editorial overlay 테스트 PASS
5. `bun run typecheck` — 에러 0건

**데이터 플로우 완전성 체크:**
```
Supabase posts.post_magazine_title
  → useImages.ts: title = post.post_magazine_title ?? post.title ?? null
  → ExploreClient.tsx: editorialTitle = item.title (when hasMagazine && item.title != null)
  → ExploreCardCell.tsx: {item?.editorialTitle && <overlay>}
```
각 단계에서 null이면 다음 단계로 전달 안 됨 — 올바른 방어적 설계.
</verification>

<success_criteria>
- Editorial 데이터 플로우 전 단계(Supabase → hook → client → cell)가 코드로 확인됨
- `post_magazine_title` null 시 오버레이 미표시 동작이 테스트로 검증됨
- `hasMagazine=false`(일반 Explore)에서 editorialTitle이 전달되지 않음이 로직으로 확인됨
- TypeScript 타입 에러 0건
- 기존 editorial overlay 코드(ExploreCardCell lines 101-121) 변경 없음 확인
</success_criteria>

<output>
After completion, create `.planning/phases/54-card-enrichment/54-02-SUMMARY.md`
</output>
