---
phase: 54-card-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/hooks/useImages.ts
  - packages/web/lib/components/ThiingsGrid.tsx
  - packages/web/app/explore/ExploreClient.tsx
  - packages/web/lib/components/explore/ExploreCardCell.tsx
  - packages/web/lib/components/explore/__tests__/ExploreCardCell.test.tsx
autonomous: true
requirements:
  - CARD-01

must_haves:
  truths:
    - "spotCount > 0인 Explore 카드에 숫자 배지 pill이 top-right에 표시된다"
    - "spotCount === 0 또는 undefined인 카드에는 배지가 표시되지 않는다"
    - "Explore 카드 레이아웃이 배지 추가 후에도 시각적으로 충돌하지 않는다"
  artifacts:
    - path: "packages/web/lib/hooks/useImages.ts"
      provides: "spotCount를 post.spot_count ?? 0으로 매핑"
      contains: "post.spot_count ?? 0"
    - path: "packages/web/lib/components/ThiingsGrid.tsx"
      provides: "GridItem 타입에 spotCount?: number 필드 추가"
      exports: ["GridItem", "ItemConfig"]
    - path: "packages/web/app/explore/ExploreClient.tsx"
      provides: "gridItems 매핑 시 spotCount > 0인 경우 전달"
    - path: "packages/web/lib/components/explore/ExploreCardCell.tsx"
      provides: "item.spotCount > 0일 때 pill 배지 렌더링"
    - path: "packages/web/lib/components/explore/__tests__/ExploreCardCell.test.tsx"
      provides: "spotCount 배지 렌더링 unit test"
  key_links:
    - from: "packages/web/lib/hooks/useImages.ts"
      to: "PostGridItem.spotCount"
      via: "post.spot_count ?? 0 mapping at line 233"
      pattern: "spot_count"
    - from: "packages/web/app/explore/ExploreClient.tsx"
      to: "GridItem.spotCount"
      via: "conditional spread in gridItems useMemo"
      pattern: "spotCount"
    - from: "packages/web/lib/components/explore/ExploreCardCell.tsx"
      to: "badge pill span"
      via: "item?.spotCount != null && item.spotCount > 0 guard"
      pattern: "spotCount > 0"
---

<objective>
`useInfinitePosts`에서 `spotCount: 0` 하드코딩을 실제 `post.spot_count` 값으로 교체하고, `GridItem` 타입에 `spotCount` 필드를 추가한 뒤 `ExploreCardCell`에 숫자 pill 배지를 렌더링한다.

Purpose: Explore 카드가 실제 spot 수를 반영하여 콘텐츠 밀도를 시각적으로 전달한다.
Output: 4개 파일 수정 + 테스트 파일 신규 생성
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-card-enrichment/54-RESEARCH.md

<interfaces>
<!-- 수정 대상 타입 계약 — 실행 전 참조 -->

From packages/web/lib/components/ThiingsGrid.tsx (line 21-31):
```typescript
export type GridItem = {
  id: string;
  imageUrl?: string | null;
  status?: "pending" | "extracted" | "skipped" | string;
  hasItems?: boolean;
  postId: string;
  postSource: PostSource;
  postAccount: string;
  postCreatedAt: string;
  editorialTitle?: string | null;
  // spotCount?: number  ← 이 필드가 없음. 추가 대상.
};

export type ItemConfig = {
  isMoving: boolean;
  position: Position;
  gridIndex: number;
  item?: GridItem;
};
```

From packages/web/lib/hooks/useImages.ts (line 226-238):
```typescript
const items: PostGridItem[] = (data ?? []).map((post: any) => ({
  id: post.id,
  imageUrl: post.image_url,
  postId: post.id,
  postSource: "post" as const,
  postAccount: post.artist_name ?? post.group_name ?? "",
  postCreatedAt: post.created_at,
  spotCount: 0,   // ← BUG: line 233, fix to post.spot_count ?? 0
  viewCount: post.view_count,
  title: post.post_magazine_title ?? post.title ?? null,
}));
```

From packages/web/app/explore/ExploreClient.tsx (line 78-89):
```typescript
const gridItems: GridItem[] = useMemo(() => {
  return items
    .filter((item) => item.imageUrl != null)
    .map((item) => ({
      id: item.id,
      imageUrl: item.imageUrl,
      postId: item.postId,
      postSource: item.postSource,
      postAccount: item.postAccount,
      postCreatedAt: item.postCreatedAt,
      ...(hasMagazine && item.title && { editorialTitle: item.title }),
      // spotCount 전달 없음 ← 추가 대상
    }));
}, [items, hasMagazine]);
```

From packages/web/lib/components/explore/ExploreCardCell.tsx (line 85-125):
```typescript
// <article> 내부 — editorial overlay는 line 101-121에 이미 존재
// spotCount badge는 없음 ← 추가 대상 (top-right absolute pill)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — ExploreCardCell 테스트 스캐폴드 작성</name>
  <files>packages/web/lib/components/explore/__tests__/ExploreCardCell.test.tsx</files>
  <behavior>
    - Test 1: spotCount=5 → badge pill 렌더링됨 (aria-label 또는 text "5" 존재)
    - Test 2: spotCount=0 → badge pill 없음
    - Test 3: spotCount=undefined → badge pill 없음
    - Test 4: editorialTitle="Vogue Korea" → 오버레이 텍스트 존재 (editorial overlay 기존 동작 회귀 방지)
  </behavior>
  <action>
`packages/web/lib/components/explore/__tests__/ExploreCardCell.test.tsx` 신규 생성.

테스트 환경 설정:
- `vitest` + `@testing-library/react`
- `ThiingsGrid.tsx`의 `ItemConfig` 타입으로 props 구성
- 이미지는 jsdom에서 자연스럽게 처리되지 않으므로 `next/image`를 vi.mock으로 모킹 (`<img src={src} alt={alt} />` 반환)
- GSAP Flip 플러그인 mock 필요: `vi.mock("gsap/Flip", () => ({ Flip: { getState: vi.fn(), from: vi.fn() } }))`
- `useTransitionStore`, `useTrackEvent` hook mock 필요

테스트 케이스 4개를 RED 상태로 작성. 실행 결과 FAIL이어야 Task 2 진행.

vitest config: `packages/web/vitest.config.ts` 참조.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo/packages/web && bun run test --run -- ExploreCardCell 2>&1 | tail -20</automated>
  </verify>
  <done>테스트 파일 존재, 4개 테스트 모두 RED(FAIL) 상태 확인</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: spotCount 데이터 매핑 + 타입 확장 + 배지 UI 구현</name>
  <files>
    packages/web/lib/hooks/useImages.ts,
    packages/web/lib/components/ThiingsGrid.tsx,
    packages/web/app/explore/ExploreClient.tsx,
    packages/web/lib/components/explore/ExploreCardCell.tsx
  </files>
  <behavior>
    - Test 1: spotCount=5 → badge pill 렌더링됨 GREEN
    - Test 2: spotCount=0 → badge pill 없음 GREEN
    - Test 3: spotCount=undefined → badge pill 없음 GREEN
    - Test 4: editorialTitle 오버레이 회귀 없음 GREEN
  </behavior>
  <action>
순서대로 수정 (타입 먼저, 그 다음 구현):

**Step 1 — ThiingsGrid.tsx: GridItem 타입에 spotCount 추가 (line 21-31)**
```typescript
export type GridItem = {
  id: string;
  imageUrl?: string | null;
  status?: "pending" | "extracted" | "skipped" | string;
  hasItems?: boolean;
  postId: string;
  postSource: PostSource;
  postAccount: string;
  postCreatedAt: string;
  editorialTitle?: string | null;
  spotCount?: number;   // 추가
};
```
`ItemConfig`는 `item?: GridItem`을 참조하므로 별도 수정 불필요.

**Step 2 — useImages.ts: line 233 수정**
```typescript
// BEFORE
spotCount: 0,
// AFTER
spotCount: post.spot_count ?? 0,
```

**Step 3 — ExploreClient.tsx: gridItems useMemo 내 spotCount 전달 (line 81-89)**
기존 map 블록에 한 줄 추가:
```typescript
...(item.spotCount != null && item.spotCount > 0 && { spotCount: item.spotCount }),
```
`hasMagazine && item.title` spread 다음 줄에 삽입. Explore 탭(`hasMagazine=false`)에서도 spotCount는 항상 전달 — hasMagazine 조건 없이 spreadするの이 올바르다.

**Step 4 — ExploreCardCell.tsx: badge pill 추가 (article 태그 내부, editorial overlay 위)**
`</article>` 닫기 전, editorial overlay div 다음에 삽입:
```tsx
{item?.spotCount != null && item.spotCount > 0 && (
  <div className="absolute top-2 right-2 z-10">
    <span
      className="inline-flex items-center rounded-full bg-black/70 px-2 py-0.5 text-[11px] font-semibold text-white tabular-nums backdrop-blur-sm"
      aria-label={`${item.spotCount} spots`}
    >
      {item.spotCount}
    </span>
  </div>
)}
```
위치: `top-2 right-2` — editorial title 오버레이(`bottom-0`)와 충돌 없음.
z-index: `z-10` — image 위, 다른 오버레이와 동일 레이어.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo/packages/web && bun run test --run -- ExploreCardCell 2>&1 | tail -20</automated>
  </verify>
  <done>4개 테스트 모두 GREEN. TypeScript 타입 에러 없음.</done>
</task>

<task type="auto">
  <name>Task 3: TypeScript 타입체크 + 빌드 검증</name>
  <files></files>
  <action>
수정된 4개 파일의 타입 안정성을 확인한다.

```bash
cd /Users/kiyeol/development/decoded/decoded-monorepo/packages/web
bun run typecheck
```

타입 에러 발생 시:
- `GridItem.spotCount` 관련 에러 → ThiingsGrid.tsx 타입 선언 재확인
- `PostGridItem`에 `spotCount` 없는 경우 → `packages/web/lib/hooks/useImages.ts` 상단의 `PostGridItem` 타입 정의 확인 (shared 패키지 타입일 수 있음)

빌드 확인:
```bash
cd /Users/kiyeol/development/decoded/decoded-monorepo
bun run build --filter=web 2>&1 | tail -30
```
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo/packages/web && bun run typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript 타입 에러 0건. 빌드 성공.</done>
</task>

</tasks>

<verification>
1. `bun run test --run -- ExploreCardCell` — 4개 테스트 PASS
2. `bun run typecheck` — 에러 0건
3. `grep "spot_count" packages/web/lib/hooks/useImages.ts` — line 233에 `post.spot_count ?? 0` 존재
4. `grep "spotCount" packages/web/lib/components/ThiingsGrid.tsx` — GridItem 타입에 `spotCount?: number` 존재
5. `grep "spotCount" packages/web/lib/components/explore/ExploreCardCell.tsx` — badge pill JSX 존재
</verification>

<success_criteria>
- `useInfinitePosts`가 `spotCount: 0` 하드코딩 대신 `post.spot_count ?? 0`을 사용한다
- `GridItem` 타입에 `spotCount?: number`가 추가되었다
- `ExploreClient`의 gridItems 매핑에서 `spotCount > 0`인 경우 전달된다
- `ExploreCardCell`에서 `spotCount > 0`일 때 top-right에 pill 배지가 렌더링된다
- `spotCount === 0` 또는 undefined면 배지 DOM 요소가 없다
- TypeScript 타입 에러 0건, 테스트 4개 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/54-card-enrichment/54-01-SUMMARY.md`
</output>
