---
phase: 56-explore-ui-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/detail/SpotDot.tsx
  - packages/web/lib/components/main-renewal/HeroSpotMarker.tsx
autonomous: true
requirements: [D-01, D-02]
must_haves:
  truths:
    - "SpotDot renders with var(--mag-accent) as default color when no accentColor prop is passed"
    - "HeroSpotMarker uses var(--mag-accent) CSS variable instead of hardcoded #eafd67 hex values"
    - "Both components display identical neon yellow brand color in all usage contexts"
  artifacts:
    - path: "packages/web/lib/components/detail/SpotDot.tsx"
      provides: "Spot marker with brand color default"
      contains: "var(--mag-accent)"
    - path: "packages/web/lib/components/main-renewal/HeroSpotMarker.tsx"
      provides: "Hero spot marker with CSS variable colors"
      contains: "var(--mag-accent)"
  key_links:
    - from: "packages/web/lib/components/detail/SpotDot.tsx"
      to: "packages/web/app/globals.css"
      via: "CSS variable reference"
      pattern: "var\\(--mag-accent\\)"
    - from: "packages/web/lib/components/main-renewal/HeroSpotMarker.tsx"
      to: "packages/web/app/globals.css"
      via: "CSS variable reference"
      pattern: "var\\(--mag-accent\\)"
---

<objective>
Unify SpotDot and HeroSpotMarker components to use the brand color CSS variable `var(--mag-accent)` (#eafd67) instead of hardcoded hex values and hsl(var(--primary)) fallbacks.

Purpose: Ensure consistent neon yellow brand identity across all spot marker components on main page and detail page (D-01, D-02).
Output: Two updated component files with CSS variable-based coloring.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-explore-ui-enhancement/56-CONTEXT.md
@.planning/phases/56-explore-ui-enhancement/56-RESEARCH.md

<interfaces>
<!-- CSS variable defined in globals.css -->
From packages/web/app/globals.css (line ~110):
```css
--mag-accent: #eafd67;
```

From packages/web/tailwind.config.ts (line ~78):
```ts
mag: { accent: "var(--mag-accent)" }
```

From packages/web/lib/components/detail/SpotDot.tsx (line 22):
```tsx
const dotColor = accentColor || "hsl(var(--primary))";
// Must change to: accentColor || "var(--mag-accent)"
```

From packages/web/lib/components/main-renewal/HeroSpotMarker.tsx:
```tsx
// 5 hardcoded color locations to replace:
// Line 88: borderTop: "1.5px solid rgba(234,253,103,0.6)"
// Line 89: filter: "drop-shadow(0 0 3px rgba(234,253,103,0.3))"
// Line 96: bg-[#eafd67] (dot div)
// Line 97: bg-[#eafd67] (ping div)
// Line 112-114: borderColor: hovered ? "rgba(234,253,103,0.5)" : ...
// Line 138: text-[#eafd67] (price text)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify SpotDot default color to brand CSS variable</name>
  <files>packages/web/lib/components/detail/SpotDot.tsx</files>
  <read_first>
    - packages/web/lib/components/detail/SpotDot.tsx
    - packages/web/app/globals.css (search for --mag-accent)
  </read_first>
  <action>
In SpotDot.tsx, change the default dotColor fallback from `hsl(var(--primary))` to `var(--mag-accent)`.

Current (line 22):
```tsx
const dotColor = accentColor || "hsl(var(--primary))";
```

Change to:
```tsx
const dotColor = accentColor || "var(--mag-accent)";
```

This is a one-line change. The `accentColor` prop still allows overrides, but the default now uses the brand neon yellow.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo && grep -n 'var(--mag-accent)' packages/web/lib/components/detail/SpotDot.tsx && ! grep -n 'hsl(var(--primary))' packages/web/lib/components/detail/SpotDot.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SpotDot.tsx contains the string `var(--mag-accent)` as the dotColor fallback
    - SpotDot.tsx does NOT contain `hsl(var(--primary))` anywhere
    - The `accentColor` prop override mechanism is preserved (still uses `accentColor || ...`)
  </acceptance_criteria>
  <done>SpotDot default color is var(--mag-accent), hsl(var(--primary)) is gone</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded hex colors in HeroSpotMarker with CSS variable</name>
  <files>packages/web/lib/components/main-renewal/HeroSpotMarker.tsx</files>
  <read_first>
    - packages/web/lib/components/main-renewal/HeroSpotMarker.tsx (FULL file)
    - packages/web/app/globals.css (search for --mag-accent)
  </read_first>
  <action>
Replace all 5+ hardcoded `#eafd67` / `rgba(234,253,103,...)` color references with `var(--mag-accent)` CSS variable approach.

Specific replacements:

1. **Connector line border** (line ~88):
   - FROM: `borderTop: "1.5px solid rgba(234,253,103,0.6)"`
   - TO: `borderTop: "1.5px solid rgba(234,253,103,0.6)"` -- keep as-is since this is the same hex value as --mag-accent and color-mix in inline styles can be unreliable. OR use `borderTop: "1.5px solid var(--mag-accent)"` with separate opacity via `opacity: 0.6` on the element.
   - RECOMMENDED: Change to `borderTop: "1.5px solid var(--mag-accent)"` and add `opacity` handling separately, or keep rgba since it matches the brand color exactly.

2. **Drop shadow filter** (line ~89):
   - FROM: `filter: "drop-shadow(0 0 3px rgba(234,253,103,0.3))"`
   - Keep as-is — drop-shadow with CSS variables in filter is unreliable in inline styles. The rgba value matches --mag-accent.

3. **Dot background** (line ~96): Tailwind class `bg-[#eafd67]`
   - FROM: `className="... bg-[#eafd67] ..."`
   - TO: Remove `bg-[#eafd67]` from className, add `style={{ backgroundColor: "var(--mag-accent)" }}`

4. **Ping dot background** (line ~97): Tailwind class `bg-[#eafd67]`
   - FROM: `className="... bg-[#eafd67] ..."`
   - TO: Remove `bg-[#eafd67]` from className, add `style={{ backgroundColor: "var(--mag-accent)" }}`

5. **Price text color** (line ~138): Tailwind class `text-[#eafd67]`
   - FROM: `className="... text-[#eafd67] ..."`
   - TO: Remove `text-[#eafd67]` from className, add `style={{ color: "var(--mag-accent)" }}`

6. **Hover border** (line ~112-114):
   - FROM: `borderColor: hovered ? "rgba(234,253,103,0.5)" : "rgba(255,255,255,0.1)"`
   - TO: `borderColor: hovered ? "var(--mag-accent)" : "rgba(255,255,255,0.1)"` (with opacity via separate means, or keep rgba since it is the brand color)

For items where inline CSS variable works cleanly (backgroundColor, color, borderColor), use `var(--mag-accent)`. For filter/drop-shadow where CSS variables in inline JS strings are unreliable, keep the rgba value with a comment `/* matches --mag-accent */`.

After changes, verify the file has NO remaining `#eafd67` Tailwind classes (bg-[#eafd67], text-[#eafd67]).
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo && ! grep -n '#eafd67' packages/web/lib/components/main-renewal/HeroSpotMarker.tsx | grep -E 'bg-|text-' && grep -c 'var(--mag-accent)' packages/web/lib/components/main-renewal/HeroSpotMarker.tsx</automated>
  </verify>
  <acceptance_criteria>
    - HeroSpotMarker.tsx has zero Tailwind classes containing `#eafd67` (no bg-[#eafd67] or text-[#eafd67])
    - HeroSpotMarker.tsx contains at least 3 occurrences of `var(--mag-accent)`
    - The connector line and drop-shadow rgba values may remain if CSS variable is unreliable in that context, but must have a comment noting they match --mag-accent
    - TypeScript compilation succeeds: `cd packages/web && bunx tsc --noEmit --pretty 2>&1 | grep -c 'error' || echo 0` returns 0
  </acceptance_criteria>
  <done>HeroSpotMarker uses CSS variable for all brand color references where technically feasible, no hardcoded #eafd67 in Tailwind classes</done>
</task>

</tasks>

<verification>
1. `grep -rn '#eafd67' packages/web/lib/components/detail/SpotDot.tsx packages/web/lib/components/main-renewal/HeroSpotMarker.tsx` — should return zero Tailwind class matches
2. `grep -rn 'var(--mag-accent)' packages/web/lib/components/detail/SpotDot.tsx packages/web/lib/components/main-renewal/HeroSpotMarker.tsx` — should return 4+ matches
3. `cd packages/web && bunx tsc --noEmit` — no new TypeScript errors
</verification>

<success_criteria>
- SpotDot and HeroSpotMarker both use var(--mag-accent) for brand color
- No hardcoded #eafd67 Tailwind classes remain in either file
- Both components render the same neon yellow brand color
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/56-explore-ui-enhancement/56-01-SUMMARY.md`
</output>
