---
phase: 50-saved-tab-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/profile/SavedGrid.tsx
  - packages/web/lib/stores/collectionStore.ts
  - packages/web/lib/components/collection/index.ts
  - packages/web/lib/components/collection/PinGrid.tsx
  - packages/web/lib/components/collection/PinCard.tsx
  - packages/web/lib/components/collection/BoardGrid.tsx
  - packages/web/lib/components/collection/BoardCard.tsx
  - packages/web/lib/components/collection/CollageView.tsx
  - packages/web/lib/components/collection/CollectionHeader.tsx
autonomous: true
requirements: [SAVED-03, SAVED-04, SAVED-05]

must_haves:
  truths:
    - "Saved 탭이 실제 API 데이터(saved_posts 테이블)를 표시한다"
    - "스크롤 끝에 도달하면 다음 페이지가 자동 로드된다"
    - "저장한 포스트가 없으면 Bookmark 아이콘 + 안내 문구가 표시된다"
    - "MOCK_PINS/MOCK_BOARDS가 코드베이스에서 완전 제거된다"
  artifacts:
    - path: "packages/web/lib/components/profile/SavedGrid.tsx"
      provides: "Saved posts grid with infinite scroll"
      contains: "useInfiniteQuery"
    - path: "packages/web/lib/stores/collectionStore.ts"
      provides: "Collection store with mock data removed"
      not_contains: "MOCK_PINS"
  key_links:
    - from: "packages/web/lib/components/profile/SavedGrid.tsx"
      to: "packages/web/lib/api/generated/users/users.ts"
      via: "getMySaved raw function in useInfiniteQuery queryFn"
      pattern: "getMySaved.*page.*per_page"
    - from: "packages/web/lib/components/profile/SavedGrid.tsx"
      to: "packages/web/lib/api/generated/models"
      via: "SavedItem type import"
      pattern: "import.*SavedItem.*from.*generated/models"
---

<objective>
Rewrite SavedGrid.tsx to display real saved posts with infinite scroll, remove all mock data from collectionStore, and delete dead collection components (PinGrid, BoardGrid, CollageView, etc.).

Purpose: Complete the Saved tab with real API data, matching the TriesGrid pattern from Phase 49.
Output: Working SavedGrid with useInfiniteQuery, mock-free collectionStore, dead code removed.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-saved-tab-frontend/50-CONTEXT.md
@.planning/phases/50-saved-tab-frontend/50-RESEARCH.md

<interfaces>
<!-- Generated API types the executor needs -->

From packages/web/lib/api/generated/models/savedItem.ts:
```typescript
export interface SavedItem {
  id: string;
  post_id: string;
  post_title?: string | null;
  post_thumbnail_url?: string | null;
  saved_at: string;
}
```

From packages/web/lib/api/generated/models/getMySaved200.ts:
```typescript
export type GetMySaved200 = {
  data: SavedItem[];
  pagination: PaginationMeta;
};
```

From packages/web/lib/api/generated/models/paginationMeta.ts:
```typescript
export interface PaginationMeta {
  current_page: number;
  per_page: number;
  total_items: number;
  total_pages: number;
}
```

From packages/web/lib/api/generated/users/users.ts:
```typescript
export const getMySaved = (
  params?: GetMySavedParams,
  options?: RequestInit,
  signal?: AbortSignal
) => customInstance<GetMySaved200>(...);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SavedGrid with useInfiniteQuery + infinite scroll</name>
  <files>packages/web/lib/components/profile/SavedGrid.tsx</files>
  <read_first>
    - packages/web/lib/components/profile/TriesGrid.tsx (canonical pattern to copy)
    - packages/web/lib/components/profile/SavedGrid.tsx (current implementation to replace)
    - packages/web/lib/api/generated/models/savedItem.ts (SavedItem type)
  </read_first>
  <action>
    Rewrite SavedGrid.tsx following the TriesGrid.tsx pattern exactly:

    1. Replace all imports: remove collectionStore, PinGrid, BoardGrid, CollageView. Add useInfiniteQuery, useRef, useEffect, Bookmark (lucide-react), cn, Image (next/image), getMySaved (from @/lib/api/generated/users/users), SavedItem (from @/lib/api/generated/models).

    2. useInfiniteQuery setup:
       - queryKey: ["/api/v1/users/me/saved"]
       - queryFn: getMySaved({ page: pageParam as number, per_page: 20 })
       - getNextPageParam: lastPage.pagination.current_page < lastPage.pagination.total_pages ? current_page + 1 : undefined
       - initialPageParam: 1, staleTime: 60000

    3. Flatten pages: `const items: SavedItem[] = data?.pages.flatMap((p) => p.data) ?? []`

    4. Native IntersectionObserver sentinel (copy from TriesGrid verbatim): sentinelRef, useEffect with observer, threshold 0.1.

    5. Loading state: skeleton grid (grid-cols-2 md:grid-cols-3 gap-3, 4x aspect-[3/4] rounded-xl bg-muted animate-pulse).

    6. Error state: "Failed to load saved posts" + Retry button (same pattern as TriesGrid).

    7. Empty state: Bookmark icon (h-10 w-10) + "저장한 포스트가 없습니다" + "관심 있는 포스트를 저장해 보세요" sub-text.

    8. Card rendering: For each SavedItem:
       - Wrap in Link to `/images/${item.post_id}` (if post route exists) or a div
       - aspect-[3/4] container with rounded-xl, border, hover effects (same classes as TriesGrid button)
       - Image: use `item.post_thumbnail_url` with fallback — if null, render a `bg-muted` div with Bookmark icon centered
       - Bottom gradient overlay: show `item.post_title ?? "Untitled"` (left-aligned) + saved_at date formatted as ko-KR month/day (right-aligned)
       - Use next/image with fill, object-cover, sizes="(max-width: 768px) 50vw, 33vw"

    9. isFetchingNextPage: show 2 more skeleton placeholders.

    10. Remove ALL references to Pins/Boards/Collage sub-tabs. No sub-tab bar.

    IMPORTANT: post_thumbnail_url is `string | null`. NEVER pass null to next/image src. Guard with conditional rendering.
    IMPORTANT: post_title is `string | null`. Use `?? "Untitled"` fallback.
  </action>
  <verify>
    <automated>cd packages/web && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "useInfiniteQuery" packages/web/lib/components/profile/SavedGrid.tsx returns 1
    - grep -c "getMySaved" packages/web/lib/components/profile/SavedGrid.tsx returns at least 1
    - grep -c "IntersectionObserver" packages/web/lib/components/profile/SavedGrid.tsx returns 1
    - grep -c "collectionStore" packages/web/lib/components/profile/SavedGrid.tsx returns 0
    - grep -c "PinGrid\|BoardGrid\|CollageView" packages/web/lib/components/profile/SavedGrid.tsx returns 0
    - grep -c "저장한 포스트가 없습니다" packages/web/lib/components/profile/SavedGrid.tsx returns 1
    - bun run typecheck passes with 0 errors
  </acceptance_criteria>
  <done>SavedGrid renders real saved posts via useInfiniteQuery with getMySaved, infinite scroll works via IntersectionObserver, all three UI states (loading/error/empty) handled, no mock data references</done>
</task>

<task type="auto">
  <name>Task 2: Remove mock data from collectionStore + delete dead collection components</name>
  <files>
    packages/web/lib/stores/collectionStore.ts
    packages/web/lib/components/collection/PinGrid.tsx
    packages/web/lib/components/collection/PinCard.tsx
    packages/web/lib/components/collection/BoardGrid.tsx
    packages/web/lib/components/collection/BoardCard.tsx
    packages/web/lib/components/collection/CollageView.tsx
    packages/web/lib/components/collection/CollectionHeader.tsx
    packages/web/lib/components/collection/index.ts
  </files>
  <read_first>
    - packages/web/lib/stores/collectionStore.ts (identify MOCK_PINS/MOCK_BOARDS line ranges)
    - packages/web/lib/components/collection/index.ts (current exports to clean up)
  </read_first>
  <action>
    1. collectionStore.ts cleanup:
       - Delete the entire MOCK_PINS constant (lines 38-119, the Pin[] array)
       - Delete the entire MOCK_BOARDS constant (lines 121-154, the Board[] array)
       - Update loadCollection to be a no-op: `loadCollection: async () => { /* mock data removed */ }`
       - Keep the store structure (CollectionState, Pin, Board types, useCollectionStore export) — these types may be referenced elsewhere even though the store itself is dead

    2. Delete dead collection components that were ONLY imported by old SavedGrid:
       - DELETE: PinGrid.tsx (only imported by old SavedGrid)
       - DELETE: PinCard.tsx (only imported by PinGrid)
       - DELETE: BoardGrid.tsx (only imported by old SavedGrid)
       - DELETE: BoardCard.tsx (only imported by BoardGrid)
       - DELETE: CollageView.tsx (only imported by old SavedGrid)
       - DELETE: CollectionHeader.tsx (only imported by old SavedGrid sub-components)

    3. Update collection/index.ts:
       - Remove exports for deleted components (PinGrid, PinCard, BoardGrid, BoardCard, CollageView)
       - Keep CollectionShareSheet export (not related to pins/boards)
       - If index.ts becomes empty except CollectionShareSheet, keep it

    4. Do NOT delete: BookshelfView, IssueDetailPanel, IssuePreviewCard, IssueSpine, ShelfRow, StudioHUD, StudioLoader, EmptyBookshelf, EmptyStudio, MagazinePreviewModal, studio/, types.ts — these are unrelated magazine/studio components.
  </action>
  <verify>
    <automated>cd packages/web && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "MOCK_PINS" packages/web/lib/stores/collectionStore.ts returns 0
    - grep -c "MOCK_BOARDS" packages/web/lib/stores/collectionStore.ts returns 0
    - test ! -f packages/web/lib/components/collection/PinGrid.tsx (file deleted)
    - test ! -f packages/web/lib/components/collection/BoardGrid.tsx (file deleted)
    - test ! -f packages/web/lib/components/collection/CollageView.tsx (file deleted)
    - test ! -f packages/web/lib/components/collection/PinCard.tsx (file deleted)
    - test ! -f packages/web/lib/components/collection/BoardCard.tsx (file deleted)
    - test ! -f packages/web/lib/components/collection/CollectionHeader.tsx (file deleted)
    - bun run typecheck passes with 0 errors
  </acceptance_criteria>
  <done>MOCK_PINS and MOCK_BOARDS completely removed from collectionStore, 6 dead collection components deleted, typecheck clean</done>
</task>

</tasks>

<verification>
1. `cd packages/web && bun run typecheck` — zero errors
2. `cd packages/web && bun run lint` — no new lint errors
3. `grep -r "MOCK_PINS\|MOCK_BOARDS" packages/web/lib/` — zero matches
4. `grep -r "collectionStore" packages/web/lib/components/profile/SavedGrid.tsx` — zero matches
5. `grep "useInfiniteQuery" packages/web/lib/components/profile/SavedGrid.tsx` — one match
6. `grep "getMySaved" packages/web/lib/components/profile/SavedGrid.tsx` — at least one match
</verification>

<success_criteria>
- SavedGrid.tsx uses useInfiniteQuery with getMySaved raw function (not the hook)
- Infinite scroll via native IntersectionObserver sentinel
- Empty state shows Bookmark icon + Korean message
- Loading state shows skeleton grid
- Error state shows retry button
- MOCK_PINS and MOCK_BOARDS completely removed from codebase
- Dead collection components (PinGrid, BoardGrid, CollageView, PinCard, BoardCard, CollectionHeader) deleted
- TypeScript typecheck passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/50-saved-tab-frontend/50-01-SUMMARY.md`
</output>
