---
phase: 58-artist-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/hooks/useTrendingArtists.ts
  - packages/web/lib/components/explore/ArtistProfileCard.tsx
  - packages/web/lib/components/explore/TrendingArtistsSection.tsx
  - packages/web/lib/components/explore/index.ts
  - packages/web/app/explore/ExploreClient.tsx
autonomous: true
requirements: [SC-01, SC-02]

must_haves:
  truths:
    - "Explore 페이지 상단(masonry 위)에 트렌딩 아티스트 수평 스크롤 섹션이 표시된다"
    - "아티스트 카드 클릭 시 해당 아티스트 포스트로 그리드가 필터링된다"
    - "트렌딩 아티스트가 3명 미만이면 섹션이 숨겨진다"
    - "Editorial 탭에서는 트렌딩 섹션이 보이지 않는다"
  artifacts:
    - path: "packages/web/lib/hooks/useTrendingArtists.ts"
      provides: "Supabase aggregation hook for trending artists"
      exports: ["useTrendingArtists", "TrendingArtist"]
    - path: "packages/web/lib/components/explore/ArtistProfileCard.tsx"
      provides: "Individual artist card with avatar, name, post count"
      exports: ["ArtistProfileCard"]
    - path: "packages/web/lib/components/explore/TrendingArtistsSection.tsx"
      provides: "Horizontal scroll section containing artist cards"
      exports: ["TrendingArtistsSection"]
  key_links:
    - from: "packages/web/lib/components/explore/TrendingArtistsSection.tsx"
      to: "packages/web/lib/hooks/useTrendingArtists.ts"
      via: "useTrendingArtists(7, 10)"
      pattern: "useTrendingArtists"
    - from: "packages/web/lib/components/explore/TrendingArtistsSection.tsx"
      to: "packages/shared/stores/hierarchicalFilterStore.ts"
      via: "setCast(name, name, name) on card click"
      pattern: "setCast"
    - from: "packages/web/app/explore/ExploreClient.tsx"
      to: "packages/web/lib/components/explore/TrendingArtistsSection.tsx"
      via: "conditional render {!hasMagazine && <TrendingArtistsSection />}"
      pattern: "TrendingArtistsSection"
---

<objective>
Add a Trending Artists section to the Explore page: a horizontally scrolling row of artist profile cards placed above the masonry grid, sourced from posts.artist_name aggregation. Clicking an artist card filters the grid via the existing castId filter.

Purpose: Enable artist-based content discovery directly from Explore.
Output: useTrendingArtists hook, ArtistProfileCard, TrendingArtistsSection, ExploreClient integration.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-artist-discovery/058-CONTEXT.md
@.planning/phases/58-artist-discovery/058-RESEARCH.md

@packages/web/app/explore/ExploreClient.tsx
@packages/web/lib/components/search/PersonResultCard.tsx
@packages/web/lib/components/explore/index.ts
@packages/web/lib/hooks/useImages.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/shared/stores/hierarchicalFilterStore.ts:
```typescript
setCast: (castId: string | null, label?: string, labelKo?: string) => void;
```

From packages/web/lib/supabase/client.ts:
```typescript
export const supabaseBrowserClient = createBrowserClient<Database>(...);
```

From ExploreClient.tsx (line 32):
```typescript
export function ExploreClient({ initialPosts: _initialPosts, hasMagazine }: Props) {
  // hasMagazine used to conditionally render trending section
```

From ExploreClient.tsx (lines 99-121) — insertion point:
```tsx
// Line 99: <div className="relative h-[calc(100dvh-120px)] ...">
// Line 101-103: Desktop filter bar
// Line 106-114: Mobile filter toggle button
// Line 117-120: Mobile filter sheet
// >>> INSERT TrendingArtistsSection HERE (line ~121) <<<
// Line 122: <div className="relative flex-1"> — masonry grid
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useTrendingArtists hook and ArtistProfileCard component</name>
  <files>
    packages/web/lib/hooks/useTrendingArtists.ts,
    packages/web/lib/components/explore/ArtistProfileCard.tsx
  </files>
  <read_first>
    packages/web/lib/hooks/useImages.ts,
    packages/web/lib/components/search/PersonResultCard.tsx,
    packages/web/lib/supabase/client.ts
  </read_first>
  <action>
    **1. Create `packages/web/lib/hooks/useTrendingArtists.ts`:**

    Export interface `TrendingArtist` with fields: `name: string`, `postCount: number`, `imageUrl: string`.

    Create `fetchTrendingArtists(dayWindow: number, limit: number)` function:
    - Calculate cutoff date: `new Date(Date.now() - dayWindow * 24 * 60 * 60 * 1000).toISOString()`
    - Query `supabaseBrowserClient.from("posts").select("artist_name, image_url, created_at")` with filters:
      - `.eq("status", "active")`
      - `.not("artist_name", "is", null)`
      - `.not("image_url", "is", null)`
      - `.gte("created_at", cutoff)`
      - `.order("created_at", { ascending: false })`
      - `.limit(200)`
    - Group results client-side using `Map<string, { postCount: number; imageUrl: string }>`:
      - For each post, increment postCount; use FIRST image_url (most recent) as representative
    - Sort by postCount descending, take top `limit` entries
    - If result has fewer than 3 artists, call `fetchAllTimeTopArtists(limit)` as fallback

    Create `fetchAllTimeTopArtists(limit: number)`:
    - Same query but without `.gte("created_at", cutoff)`, order by `view_count` descending
    - Same client-side grouping logic
    - Return empty array on error

    Export `useTrendingArtists(dayWindow = 7, limit = 10)`:
    - Uses `useQuery<TrendingArtist[]>` from `@tanstack/react-query`
    - queryKey: `["trending-artists", { dayWindow, limit }]`
    - staleTime: `1000 * 60 * 5` (5 min)
    - gcTime: `1000 * 60 * 10` (10 min)

    **2. Create `packages/web/lib/components/explore/ArtistProfileCard.tsx`:**

    "use client" directive. Import `memo` from react, `Image` from next/image, `User` from lucide-react, `TrendingArtist` type.

    Interface: `{ artist: TrendingArtist; onClick: (name: string) => void }`

    Render a `<button>` (type="button") with:
    - Flex column, items-center, gap-2, flex-none, w-[72px] md:w-[80px], group class
    - Circular avatar: w-14 h-14 md:w-16 md:h-16, rounded-full, overflow-hidden, bg-muted
    - Hover ring effect: `ring-2 ring-transparent group-hover:ring-[var(--mag-accent)] transition-all`
    - Use `next/image` with fill, sizes="64px", object-cover, object-top (prefer face area)
    - Fallback: if image errors, show `<User>` icon from lucide-react (use onError state)
    - Name: text-[11px] font-medium text-foreground truncate w-full text-center
    - Post count: text-[10px] text-muted-foreground, format as `{postCount} posts`
    - onClick calls `onClick(artist.name)`

    Wrap component in `memo()`.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo/packages/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/web/lib/hooks/useTrendingArtists.ts` exists and exports `useTrendingArtists` and `TrendingArtist`
    - File `packages/web/lib/components/explore/ArtistProfileCard.tsx` exists and exports `ArtistProfileCard`
    - `useTrendingArtists.ts` contains `supabaseBrowserClient.from("posts")` query
    - `useTrendingArtists.ts` contains `.not("artist_name", "is", null)` filter
    - `useTrendingArtists.ts` contains `staleTime: 1000 * 60 * 5`
    - `useTrendingArtists.ts` contains fallback logic (`fetchAllTimeTopArtists`)
    - `ArtistProfileCard.tsx` contains `ring-[var(--mag-accent)]` hover effect
    - `ArtistProfileCard.tsx` uses `next/image` with `fill` prop
    - `ArtistProfileCard.tsx` uses `memo` wrapper
    - TypeScript compilation passes without errors in these files
  </acceptance_criteria>
  <done>useTrendingArtists hook fetches and aggregates artist data from posts table with 7-day window + fallback. ArtistProfileCard renders circular avatar, name, post count with hover ring effect.</done>
</task>

<task type="auto">
  <name>Task 2: Create TrendingArtistsSection and integrate into ExploreClient</name>
  <files>
    packages/web/lib/components/explore/TrendingArtistsSection.tsx,
    packages/web/lib/components/explore/index.ts,
    packages/web/app/explore/ExploreClient.tsx
  </files>
  <read_first>
    packages/web/lib/hooks/useTrendingArtists.ts,
    packages/web/lib/components/explore/ArtistProfileCard.tsx,
    packages/web/lib/components/explore/index.ts,
    packages/web/app/explore/ExploreClient.tsx,
    packages/shared/stores/hierarchicalFilterStore.ts
  </read_first>
  <action>
    **1. Create `packages/web/lib/components/explore/TrendingArtistsSection.tsx`:**

    "use client" directive. Import:
    - `useHierarchicalFilterStore` from `@decoded/shared/stores/hierarchicalFilterStore`
    - `useTrendingArtists` from `@/lib/hooks/useTrendingArtists`
    - `ArtistProfileCard` from `./ArtistProfileCard`

    Component `TrendingArtistsSection`:
    - Call `useTrendingArtists(7, 10)` — destructure `{ data: artists, isLoading }`
    - Get `setCast` from `useHierarchicalFilterStore((s) => s.setCast)`
    - Early return null if: `isLoading || !artists || artists.length < 3`
    - `handleArtistClick = (name: string) => setCast(name, name, name)` — passes artist_name as castId, label, and labelKo (artist names are typically English in this dataset)
    - Render:
      - `<section className="px-4 py-3 border-b border-border flex-shrink-0">`
      - Label: `<p className="text-[10px] font-semibold uppercase tracking-widest text-muted-foreground mb-2">Trending Artists</p>`
      - Scroll container: `<div className="flex gap-4 overflow-x-auto snap-x snap-mandatory pb-1" style={{ scrollbarWidth: "none" }}>`
        - Also add `className` with `scrollbar-hide` if the utility exists (check globals.css) — otherwise rely on `scrollbarWidth: "none"` inline style and add `-webkit-scrollbar: none` via a `<style>` tag or inline
      - Map `artists` to `<div key={artist.name} className="snap-center"><ArtistProfileCard artist={artist} onClick={handleArtistClick} /></div>`

    **2. Update `packages/web/lib/components/explore/index.ts`:**

    Add export: `export { TrendingArtistsSection } from "./TrendingArtistsSection";`

    **3. Update `packages/web/app/explore/ExploreClient.tsx`:**

    - Add import: `TrendingArtistsSection` from `@/lib/components/explore` (add to existing import destructuring from that barrel)
    - Insert `{!hasMagazine && <TrendingArtistsSection />}` between the `<ExploreFilterSheet>` closing tag (line ~120) and the `<div className="relative flex-1">` (line ~122)
    - This ensures the trending section only appears on the Explore tab, NOT on the Editorial tab
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo/packages/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/web/lib/components/explore/TrendingArtistsSection.tsx` exists and exports `TrendingArtistsSection`
    - `TrendingArtistsSection.tsx` contains `useTrendingArtists(7, 10)`
    - `TrendingArtistsSection.tsx` contains `setCast(name, name, name)` for artist click handling
    - `TrendingArtistsSection.tsx` contains `artists.length < 3` guard to hide section when data is sparse
    - `TrendingArtistsSection.tsx` contains `overflow-x-auto snap-x` for horizontal scroll
    - `TrendingArtistsSection.tsx` contains `scrollbarWidth: "none"` for hidden scrollbar
    - `index.ts` contains `export { TrendingArtistsSection }`
    - `ExploreClient.tsx` contains `!hasMagazine && <TrendingArtistsSection` — only show on Explore tab
    - `ExploreClient.tsx` imports `TrendingArtistsSection`
    - TrendingArtistsSection is placed AFTER ExploreFilterSheet and BEFORE the flex-1 grid div
    - TypeScript compilation passes without errors
  </acceptance_criteria>
  <done>TrendingArtistsSection renders horizontal scrolling artist cards above masonry grid on Explore tab only. Clicking an artist sets castId filter to filter the grid. Section hides when fewer than 3 trending artists.</done>
</task>

</tasks>

<verification>
1. `cd packages/web && npx tsc --noEmit` — TypeScript compilation passes
2. `cd packages/web && bun run build` — Next.js build succeeds
3. Visual check: Explore page shows trending artists section above grid (only on Explore tab, not Editorial)
4. Click artist card: grid filters to show only that artist's posts
5. Sparse data: section hidden when fewer than 3 artists
</verification>

<success_criteria>
- Explore page displays horizontally scrolling trending artists section above masonry grid
- Artist cards show circular avatar, name, and post count
- Clicking artist card filters the grid via setCast (no navigation to non-existent /cast/[id])
- Section only appears on Explore tab (!hasMagazine), not on Editorial tab
- Section gracefully hides when trending data is sparse (< 3 artists)
- 5-minute stale time prevents excessive re-fetches
- TypeScript and Next.js build pass
</success_criteria>

<output>
After completion, create `.planning/phases/58-artist-discovery/58-01-SUMMARY.md`
</output>
