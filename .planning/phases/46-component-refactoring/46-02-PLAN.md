---
phase: 46-component-refactoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/vton/VtonModal.tsx
  - packages/web/lib/components/vton/VtonLoadingAnimation.tsx
  - packages/web/lib/components/vton/BeforeAfterSlider.tsx
  - packages/web/lib/components/vton/VtonBackgroundNotifier.tsx
  - packages/web/lib/hooks/useVtonScrollLock.ts
  - packages/web/lib/hooks/useVtonItemFetch.ts
  - packages/web/lib/hooks/useVtonTryOn.ts
autonomous: true
requirements: [REF-02]

must_haves:
  truths:
    - "VtonModal.tsx is under 300 lines"
    - "VtonModal renders and behaves identically — open/close, try-on, item search, result display all work"
    - "LazyVtonModal dynamic import still works (VtonModal is named export from same path)"
    - "VtonBackgroundNotifier remains independently exportable"
  artifacts:
    - path: "packages/web/lib/components/vton/VtonModal.tsx"
      provides: "Slim modal composition — layout + hook orchestration only"
      contains: "export function VtonModal"
    - path: "packages/web/lib/components/vton/VtonLoadingAnimation.tsx"
      provides: "Loading animation subcomponent"
      contains: "export function VtonLoadingAnimation"
    - path: "packages/web/lib/components/vton/BeforeAfterSlider.tsx"
      provides: "Before/after image comparison slider"
      contains: "export function BeforeAfterSlider"
    - path: "packages/web/lib/components/vton/VtonBackgroundNotifier.tsx"
      provides: "Background job notification toast"
      contains: "export function VtonBackgroundNotifier"
    - path: "packages/web/lib/hooks/useVtonScrollLock.ts"
      provides: "Body scroll lock effect"
      contains: "export function useVtonScrollLock"
    - path: "packages/web/lib/hooks/useVtonItemFetch.ts"
      provides: "Item fetch + debounced search logic"
      contains: "export function useVtonItemFetch"
    - path: "packages/web/lib/hooks/useVtonTryOn.ts"
      provides: "Try-on execution + save-to-profile logic"
      contains: "export function useVtonTryOn"
  key_links:
    - from: "packages/web/lib/components/vton/VtonModal.tsx"
      to: "packages/web/lib/hooks/useVtonTryOn.ts"
      via: "hook call in component body"
      pattern: "useVtonTryOn"
    - from: "packages/web/lib/components/vton/VtonModal.tsx"
      to: "packages/web/lib/hooks/useVtonItemFetch.ts"
      via: "hook call in component body"
      pattern: "useVtonItemFetch"
    - from: "packages/web/lib/components/vton/LazyVtonModal.tsx"
      to: "packages/web/lib/components/vton/VtonModal.tsx"
      via: "dynamic import"
      pattern: "import.*VtonModal"
    - from: "packages/web/lib/components/vton/VtonModal.tsx"
      to: "packages/web/lib/components/vton/VtonBackgroundNotifier.tsx"
      via: "re-export (LazyVtonModal imports mod.VtonBackgroundNotifier from VtonModal)"
      pattern: "VtonBackgroundNotifier"
---

<objective>
Extract VtonModal's logic into 3 custom hooks and promote 3 private subcomponents to standalone files, reducing the 907-line component to under 300 lines.

Purpose: REF-02 requires VtonModal(880+ lines) to be split into hooks and subcomponents with VtonModal.tsx under 300 lines.
Output: VtonModal.tsx (~150 lines), 3 hook files, 3 subcomponent files
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-component-refactoring/46-RESEARCH.md

@packages/web/lib/components/vton/VtonModal.tsx
@packages/web/lib/components/vton/LazyVtonModal.tsx
@packages/web/lib/stores/vtonStore.ts
@packages/web/lib/hooks/useDebounce.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract hooks and promote subcomponents from VtonModal</name>
  <files>
    packages/web/lib/hooks/useVtonScrollLock.ts,
    packages/web/lib/hooks/useVtonItemFetch.ts,
    packages/web/lib/hooks/useVtonTryOn.ts,
    packages/web/lib/components/vton/VtonLoadingAnimation.tsx,
    packages/web/lib/components/vton/BeforeAfterSlider.tsx,
    packages/web/lib/components/vton/VtonBackgroundNotifier.tsx
  </files>
  <read_first>
    packages/web/lib/components/vton/VtonModal.tsx,
    packages/web/lib/components/vton/LazyVtonModal.tsx,
    packages/web/lib/stores/vtonStore.ts,
    packages/web/lib/hooks/useDebounce.ts
  </read_first>
  <action>
Read VtonModal.tsx fully and create 6 extracted files:

**Hooks (in packages/web/lib/hooks/):**

1. **useVtonScrollLock.ts** (~30 lines):
   - Extract the `useEffect` that sets `document.body.style.overflow = "hidden"` when modal opens
   - Signature: `useVtonScrollLock(isOpen: boolean): void`
   - Cleanup restores all overflow/touchAction styles

2. **useVtonItemFetch.ts** (~80 lines):
   - Extract item fetching logic: the `useEffect` that fetches items based on category and search query
   - Use the EXISTING `useDebounce` hook from `@/lib/hooks/useDebounce` instead of VtonModal's inline debounce timer
   - Signature: `useVtonItemFetch(isOpen: boolean, activeCategory: Category, searchQuery: string, preloadedItems?: ItemData[]): { items: ItemData[], isLoadingItems: boolean }`
   - Include the AbortController pattern (Phase 44 established)
   - Move `ItemData` type and `Category` type and `CATEGORIES` constant into this file or a shared types file

3. **useVtonTryOn.ts** (~100 lines):
   - Extract `handleTryOn` callback and `handleSaveToProfile` callback
   - Signature: `useVtonTryOn(options: { personImage, selectedItems, abortControllerRef, onSuccess, onError }): { handleTryOn, handleSaveToProfile, isSaving }`
   - Move `dataUrlToBlob` and `copyImageToClipboard` utility functions into this file (they are only used by try-on logic)
   - Uses the shared `abortControllerRef` pattern from Phase 44

**Subcomponents (in packages/web/lib/components/vton/):**

4. **VtonLoadingAnimation.tsx** (~80 lines):
   - Move the private `VtonLoadingAnimation` function component (currently ~lines 52-109 in VtonModal.tsx)
   - Export as named export: `export function VtonLoadingAnimation(props: VtonLoadingAnimationProps)`
   - Define its props interface explicitly (currently receives: stage, progress, statusMessage)

5. **BeforeAfterSlider.tsx** (~80 lines):
   - Move the private `BeforeAfterSlider` function component (currently ~lines 111-189)
   - Export as named export: `export function BeforeAfterSlider(props: BeforeAfterSliderProps)`
   - Define its props interface (personPreview, resultImage, etc.)

6. **VtonBackgroundNotifier.tsx** (~40 lines):
   - Move the already-exported `VtonBackgroundNotifier` component (currently ~lines 191-231)
   - This is already self-contained — just move to its own file
   - Preserve its export signature

CRITICAL: All types used across boundaries (ItemData, Category, CATEGORIES) should be defined in the hook or subcomponent that primarily owns them, then re-exported from VtonModal.tsx if other files need them. Check if any external file imports these types from VtonModal.tsx.
  </action>
  <verify>
    <automated>for f in packages/web/lib/hooks/useVtonScrollLock.ts packages/web/lib/hooks/useVtonItemFetch.ts packages/web/lib/hooks/useVtonTryOn.ts packages/web/lib/components/vton/VtonLoadingAnimation.tsx packages/web/lib/components/vton/BeforeAfterSlider.tsx packages/web/lib/components/vton/VtonBackgroundNotifier.tsx; do test -f "$f" && echo "EXISTS: $f" || echo "MISSING: $f"; done && grep -q "useDebounce" packages/web/lib/hooks/useVtonItemFetch.ts && grep -q "dataUrlToBlob" packages/web/lib/hooks/useVtonTryOn.ts && echo "HOOKS PASS" || echo "HOOKS FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist
    - useVtonScrollLock.ts contains `export function useVtonScrollLock`
    - useVtonItemFetch.ts contains `export function useVtonItemFetch` and imports `useDebounce` from `@/lib/hooks/useDebounce`
    - useVtonTryOn.ts contains `export function useVtonTryOn`
    - VtonLoadingAnimation.tsx contains `export function VtonLoadingAnimation`
    - BeforeAfterSlider.tsx contains `export function BeforeAfterSlider`
    - VtonBackgroundNotifier.tsx contains `export function VtonBackgroundNotifier`
  </acceptance_criteria>
  <done>All 6 extracted files exist with proper exports and self-contained logic</done>
</task>

<task type="auto">
  <name>Task 2: Slim VtonModal.tsx to composition-only component</name>
  <files>packages/web/lib/components/vton/VtonModal.tsx</files>
  <read_first>
    packages/web/lib/components/vton/VtonModal.tsx,
    packages/web/lib/components/vton/LazyVtonModal.tsx,
    packages/web/lib/hooks/useVtonScrollLock.ts,
    packages/web/lib/hooks/useVtonItemFetch.ts,
    packages/web/lib/hooks/useVtonTryOn.ts,
    packages/web/lib/components/vton/VtonLoadingAnimation.tsx,
    packages/web/lib/components/vton/BeforeAfterSlider.tsx,
    packages/web/lib/components/vton/VtonBackgroundNotifier.tsx
  </read_first>
  <action>
Rewrite VtonModal.tsx to be a slim composition-only component:

1. Import the 3 hooks: `useVtonScrollLock`, `useVtonItemFetch`, `useVtonTryOn`
2. Import the 3 subcomponents: `VtonLoadingAnimation`, `BeforeAfterSlider` (used in JSX)
3. Import from vtonStore as before (useVtonStore)

4. VtonModal component body should only contain:
   - UI state that drives the form: `personImage`, `personPreview`, `selectedItems`, `selectedPostItemIds`, `error`, `searchQuery`, `activeCategory`
   - Hook calls: `useVtonScrollLock(isOpen)`, `useVtonItemFetch(...)`, `useVtonTryOn(...)`
   - Keyboard close effect (Escape key — simple enough to stay inline, ~8 lines)
   - `handleClose` callback (resets state + calls store close)
   - JSX layout composing the subcomponents

5. UNCONDITIONALLY re-export `VtonBackgroundNotifier` from this file — LazyVtonModal.tsx imports it via:
   `import("./VtonModal").then((mod) => ({ default: mod.VtonBackgroundNotifier }))`
   Add: `export { VtonBackgroundNotifier } from "./VtonBackgroundNotifier"`

6. The `export function VtonModal` named export MUST be preserved — LazyVtonModal.tsx does `import("./VtonModal").then((mod) => ({ default: mod.VtonModal }))`.

Target: VtonModal.tsx should be ~150 lines.
  </action>
  <verify>
    <automated>LINE_COUNT=$(wc -l < packages/web/lib/components/vton/VtonModal.tsx); echo "VtonModal: $LINE_COUNT lines"; test "$LINE_COUNT" -le 300 && grep -q "export function VtonModal" packages/web/lib/components/vton/VtonModal.tsx && grep -q "useVtonScrollLock" packages/web/lib/components/vton/VtonModal.tsx && grep -q "useVtonItemFetch" packages/web/lib/components/vton/VtonModal.tsx && grep -q "useVtonTryOn" packages/web/lib/components/vton/VtonModal.tsx && grep -q "VtonBackgroundNotifier" packages/web/lib/components/vton/VtonModal.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - VtonModal.tsx is 300 lines or fewer (target ~150)
    - `export function VtonModal` is present (LazyVtonModal compat)
    - All 3 hooks are imported and called
    - No `dataUrlToBlob`, `copyImageToClipboard` function definitions remain
    - No `VtonLoadingAnimation` or `BeforeAfterSlider` component definitions remain (only imports)
    - No `VtonBackgroundNotifier` component definition remains — only a re-export: `export { VtonBackgroundNotifier } from "./VtonBackgroundNotifier"`
    - `grep -q 'VtonBackgroundNotifier' packages/web/lib/components/vton/VtonModal.tsx` succeeds
  </acceptance_criteria>
  <done>VtonModal.tsx is under 300 lines, all logic extracted to hooks, subcomponents in their own files, LazyVtonModal import still works, VtonBackgroundNotifier unconditionally re-exported</done>
</task>

</tasks>

<verification>
1. `wc -l packages/web/lib/components/vton/VtonModal.tsx` reports <= 300
2. `grep "mod.VtonModal" packages/web/lib/components/vton/LazyVtonModal.tsx` — dynamic import path unchanged
3. `grep -q 'VtonBackgroundNotifier' packages/web/lib/components/vton/VtonModal.tsx` — re-export present
</verification>

<success_criteria>
- VtonModal.tsx <= 300 lines (REF-02 satisfied)
- LazyVtonModal dynamic import works (named export preserved)
- VtonBackgroundNotifier unconditionally re-exported from VtonModal.tsx (LazyVtonModal compat)
- All extracted hooks and subcomponents are self-contained files
</success_criteria>

<output>
After completion, create `.planning/phases/46-component-refactoring/46-02-SUMMARY.md`
</output>
