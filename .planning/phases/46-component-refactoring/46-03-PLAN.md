---
phase: 46-component-refactoring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/detail/ItemDetailCard.tsx
  - packages/web/lib/hooks/useAdoptDropdown.ts
  - packages/web/lib/hooks/useItemCardGSAP.ts
  - packages/web/lib/components/detail/TopSolutionCard.tsx
  - packages/web/lib/components/detail/OtherSolutionsList.tsx
  - packages/web/lib/components/detail/ImageDetailModal.tsx
  - packages/web/lib/hooks/useImageModalAnimation.ts
autonomous: true
requirements: [REF-03, REF-04]

must_haves:
  truths:
    - "ItemDetailCard.tsx is under 300 lines"
    - "ImageDetailModal.tsx is under 300 lines"
    - "ItemDetailCard renders identically — adopt dropdown, solution display, GSAP scroll all work"
    - "ImageDetailModal renders identically — GSAP entry/exit, touch gestures, close animation all work"
    - "Existing imports of ItemDetailCard and ImageDetailModal from other files still resolve"
  artifacts:
    - path: "packages/web/lib/hooks/useAdoptDropdown.ts"
      provides: "Adopt dropdown state, click-outside listener, adopt/unadopt mutations"
      contains: "export function useAdoptDropdown"
    - path: "packages/web/lib/hooks/useItemCardGSAP.ts"
      provides: "ScrollTrigger animation for item card"
      contains: "export function useItemCardGSAP"
    - path: "packages/web/lib/components/detail/TopSolutionCard.tsx"
      provides: "Top solution display card with adopt/unadopt actions"
      contains: "export function TopSolutionCard"
    - path: "packages/web/lib/components/detail/OtherSolutionsList.tsx"
      provides: "Other solutions expandable list"
      contains: "export function OtherSolutionsList"
    - path: "packages/web/lib/hooks/useImageModalAnimation.ts"
      provides: "GSAP context, mount/close animation, touch handlers for ImageDetailModal"
      contains: "export function useImageModalAnimation"
    - path: "packages/web/lib/components/detail/ItemDetailCard.tsx"
      provides: "Slim item detail card composing hooks and subcomponents"
      contains: "export function ItemDetailCard"
    - path: "packages/web/lib/components/detail/ImageDetailModal.tsx"
      provides: "Slim image detail modal composing animation hook"
      contains: "export function ImageDetailModal"
  key_links:
    - from: "packages/web/lib/components/detail/ItemDetailCard.tsx"
      to: "packages/web/lib/hooks/useAdoptDropdown.ts"
      via: "hook call"
      pattern: "useAdoptDropdown"
    - from: "packages/web/lib/components/detail/ItemDetailCard.tsx"
      to: "packages/web/lib/hooks/useItemCardGSAP.ts"
      via: "hook call with cardRef and contentRef"
      pattern: "useItemCardGSAP"
    - from: "packages/web/lib/components/detail/ImageDetailModal.tsx"
      to: "packages/web/lib/hooks/useImageModalAnimation.ts"
      via: "hook call owning GSAP ctxRef"
      pattern: "useImageModalAnimation"
---

<objective>
Refactor ItemDetailCard (769 lines) and ImageDetailModal (730 lines) by extracting hooks and subcomponents, reducing each to under 300 lines.

Purpose: REF-03 and REF-04 require both components under 300 lines. They share the detail/ directory but have no code dependencies on each other.
Output: ItemDetailCard.tsx (~200 lines), ImageDetailModal.tsx (~180 lines), 2 hooks + 2 subcomponents for ItemDetailCard, 1 hook for ImageDetailModal
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-component-refactoring/46-RESEARCH.md

@packages/web/lib/components/detail/ItemDetailCard.tsx
@packages/web/lib/components/detail/ImageDetailModal.tsx
@packages/web/lib/components/detail/ImageDetailContent.tsx
@packages/web/lib/hooks/useSolutions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ItemDetailCard hooks and subcomponents</name>
  <files>
    packages/web/lib/hooks/useAdoptDropdown.ts,
    packages/web/lib/hooks/useItemCardGSAP.ts,
    packages/web/lib/components/detail/TopSolutionCard.tsx,
    packages/web/lib/components/detail/OtherSolutionsList.tsx,
    packages/web/lib/components/detail/ItemDetailCard.tsx
  </files>
  <read_first>
    packages/web/lib/components/detail/ItemDetailCard.tsx,
    packages/web/lib/hooks/useSolutions.ts,
    packages/web/lib/components/detail/types.ts
  </read_first>
  <action>
Read ItemDetailCard.tsx fully, then create extracted files and slim the component:

**useAdoptDropdown.ts** (~70 lines):
- Extract `adoptTargetId` state, `adoptDropdownRef` ref, click-outside `useEffect`
- Import and use `useAdoptSolution` and `useUnadoptSolution` from `@/lib/hooks/useSolutions`
- Signature: `useAdoptDropdown(spotId: string | null | undefined): { adoptTargetId, setAdoptTargetId, adoptDropdownRef, handleAdopt, handleUnadopt, adoptMutation, unadoptMutation }`
- The click-outside listener should use `useEffect` with `mousedown` event on `document`

**useItemCardGSAP.ts** (~40 lines):
- Extract the `useGSAP(() => { ... }, { scope: cardRef })` block that does ScrollTrigger animation
- Signature: `useItemCardGSAP(cardRef: RefObject, contentRef: RefObject, isModal: boolean): void`
- Uses `useGSAP` from `@gsap/react` with `{ scope: cardRef }` (Phase 44 pattern)
- Registers `ScrollTrigger` plugin

**TopSolutionCard.tsx** (~120 lines):
- Extract the inline IIFE that builds `topSolutionCard` JSX
- Props: `{ topSolution, isPostOwner, spotId, adoptTargetId, setAdoptTargetId, handleAdopt, handleUnadopt, adoptDropdownRef, adoptMutation, unadoptMutation }`
- Contains the adopt dropdown UI and solution image/brand display

**OtherSolutionsList.tsx** (~80 lines):
- Extract the `othersSection` conditional block
- Props: `{ otherSolutions, spotId, onAddSolution }`
- Contains the expandable "Other Solutions" list

**Slim ItemDetailCard.tsx** (~200 lines):
- Import and call `useAdoptDropdown(spotId)` and `useItemCardGSAP(cardRef, contentRef, isModal)`
- Import `TopSolutionCard` and `OtherSolutionsList`
- Keep display derivations (displayBrand, displayName, etc.) inline — they're simple computed values
- Keep the `useSolutions(spotId)` call and derive `topSolution` / `otherSolutions`
- JSX composes the subcomponents instead of inline IIFE blocks
  </action>
  <verify>
    <automated>LINE_COUNT=$(wc -l < packages/web/lib/components/detail/ItemDetailCard.tsx); echo "ItemDetailCard: $LINE_COUNT lines"; test "$LINE_COUNT" -le 300 && grep -q "export function ItemDetailCard" packages/web/lib/components/detail/ItemDetailCard.tsx && grep -q "useAdoptDropdown" packages/web/lib/components/detail/ItemDetailCard.tsx && grep -q "useItemCardGSAP" packages/web/lib/components/detail/ItemDetailCard.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - ItemDetailCard.tsx is 300 lines or fewer (target ~200)
    - `export function ItemDetailCard` is present
    - useAdoptDropdown.ts and useItemCardGSAP.ts exist with proper exports
    - TopSolutionCard.tsx and OtherSolutionsList.tsx exist with proper exports
    - No inline IIFE for `topSolutionCard` remains in ItemDetailCard.tsx
    - `useGSAP` import is in useItemCardGSAP.ts, not in ItemDetailCard.tsx
  </acceptance_criteria>
  <done>ItemDetailCard.tsx is under 300 lines with adopt logic, GSAP animation, and solution UI in dedicated files</done>
</task>

<task type="auto">
  <name>Task 2: Extract ImageDetailModal animation hook and slim component</name>
  <files>
    packages/web/lib/hooks/useImageModalAnimation.ts,
    packages/web/lib/components/detail/ImageDetailModal.tsx
  </files>
  <read_first>
    packages/web/lib/components/detail/ImageDetailModal.tsx,
    packages/web/lib/components/detail/ImageDetailContent.tsx,
    packages/web/lib/components/detail/ImageDetailPreview.tsx,
    packages/web/lib/stores/transitionStore.ts
  </read_first>
  <action>
Read ImageDetailModal.tsx fully, then extract the animation hook and slim the component:

**useImageModalAnimation.ts** (~200 lines):
- This is the core extraction — the GSAP context management and all animation logic
- Extract:
  - `ctxRef` (gsap.Context ref) — MUST be owned by this hook, not the component
  - Mount animation effect (the large useEffect that creates entry animation for drawer + floating image)
  - `handleClose` animation sequence (GSAP timeline that animates out, then calls router.back)
  - Touch handlers: `handleTouchStart`, `handleTouchMove`, `handleTouchEnd` — these reference `ctxRef.current.add()` so they MUST be in the same hook
  - `handleMaximize` (navigates to full page)
  - `isClosing` state
- Signature: `useImageModalAnimation(options: { imageId, imageData, originRect, imgSrc, backdropRef, drawerRef, floatingImageRef, contentRef, closeButtonRef, router }): { handleClose, handleTouchStart, handleTouchMove, handleTouchEnd, handleMaximize, isClosing }`
- CRITICAL: The GSAP Flip plugin usage and the floating image entry animation span multiple refs. All animation that touches `ctxRef` goes in this hook. The hook calls `ctxRef.current.revert()` in its cleanup return.

**Slim ImageDetailModal.tsx** (~180 lines):
- Keep: data fetching hooks (`usePostDetailForImage`, `usePostMagazine`), ref declarations, track event effect
- Import and call `useImageModalAnimation(...)` to get animation handlers
- The `renderContent()` inner function: check if the existing `ImageDetailContent.tsx` (479 lines) handles the same rendering. If `renderContent()` differs from `ImageDetailContent`, keep it inline as a local function (it's only ~90 lines of JSX switching on loading/error/content state). Do NOT create a duplicate.
- JSX layout: backdrop div, drawer div with touch handlers, floating image, close button — wired to hook returns
  </action>
  <verify>
    <automated>LINE_COUNT=$(wc -l < packages/web/lib/components/detail/ImageDetailModal.tsx); echo "ImageDetailModal: $LINE_COUNT lines"; test "$LINE_COUNT" -le 300 && grep -q "export function ImageDetailModal" packages/web/lib/components/detail/ImageDetailModal.tsx && grep -q "useImageModalAnimation" packages/web/lib/components/detail/ImageDetailModal.tsx && test -f packages/web/lib/hooks/useImageModalAnimation.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - ImageDetailModal.tsx is 300 lines or fewer (target ~180)
    - `export function ImageDetailModal` is present
    - useImageModalAnimation.ts exists with `export function useImageModalAnimation`
    - No `gsap.context()` or `ctxRef` creation remains in ImageDetailModal.tsx (owned by hook)
    - Touch handler functions (`handleTouchStart/Move/End`) are not defined in ImageDetailModal.tsx (returned from hook)
    - `handleClose` is not defined in ImageDetailModal.tsx (returned from hook)
    - If `renderContent()` is kept inline: it is under 100 lines and counts toward the 300-line budget
  </acceptance_criteria>
  <done>ImageDetailModal.tsx is under 300 lines, all GSAP animation and touch handling extracted to useImageModalAnimation hook; if renderContent() stays inline it is under 100 lines</done>
</task>

</tasks>

<verification>
1. `wc -l packages/web/lib/components/detail/ItemDetailCard.tsx` reports <= 300
2. `wc -l packages/web/lib/components/detail/ImageDetailModal.tsx` reports <= 300
3. `grep "export function ItemDetailCard" packages/web/lib/components/detail/ItemDetailCard.tsx` succeeds
4. `grep "export function ImageDetailModal" packages/web/lib/components/detail/ImageDetailModal.tsx` succeeds
5. All new hook/subcomponent files exist and have proper exports
</verification>

<success_criteria>
- ItemDetailCard.tsx <= 300 lines (REF-03 satisfied)
- ImageDetailModal.tsx <= 300 lines (REF-04 satisfied)
- All extracted hooks and subcomponents are self-contained
- No behavioral changes to either component
- GSAP animation lifecycle preserved (ctxRef in hook, not split across files)
</success_criteria>

<output>
After completion, create `.planning/phases/46-component-refactoring/46-03-SUMMARY.md`
</output>
