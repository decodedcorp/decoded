---
phase: 46-component-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/ThiingsGrid.tsx
  - packages/web/lib/components/ThiingsGridPhysics.ts
  - packages/web/lib/components/ThiingsGridObservers.ts
autonomous: true
requirements: [REF-01]

must_haves:
  truths:
    - "ThiingsGrid.tsx is under 300 lines"
    - "ThiingsGrid renders and behaves identically to before (external API unchanged)"
    - "Physics loop (RAF), spiral math, and IntersectionObservers work correctly after extraction"
    - "All exported types (GridItem, ThiingsGridProps, ItemConfig, Position, PostSource) remain importable from ThiingsGrid.tsx"
  artifacts:
    - path: "packages/web/lib/components/ThiingsGridPhysics.ts"
      provides: "PhysicsEngine class with RAF loop, velocity, drag, spiral math"
      min_lines: 150
    - path: "packages/web/lib/components/ThiingsGridObservers.ts"
      provides: "IntersectionObserver setup for card visibility and image lazy-loading"
      min_lines: 60
    - path: "packages/web/lib/components/ThiingsGrid.tsx"
      provides: "Slim class component delegating to PhysicsEngine and Observers"
      contains: "class ThiingsGrid"
  key_links:
    - from: "packages/web/lib/components/ThiingsGrid.tsx"
      to: "packages/web/lib/components/ThiingsGridPhysics.ts"
      via: "this.physicsEngine = new ThiingsGridPhysics(...)"
      pattern: "new ThiingsGridPhysics"
    - from: "packages/web/lib/components/ThiingsGrid.tsx"
      to: "packages/web/lib/components/ThiingsGridObservers.ts"
      via: "import and delegation from componentDidMount"
      pattern: "ThiingsGridObservers"
---

<objective>
Extract ThiingsGrid's physics engine and IntersectionObserver logic into separate files to reduce the 950-line class component to under 300 lines.

Purpose: REF-01 requires ThiingsGrid(950 lines) to be under 300 lines. The class component pattern is preserved (no functional conversion), but physics engine, spiral math, and observer setup are extracted to dedicated files.
Output: ThiingsGrid.tsx (~200 lines), ThiingsGridPhysics.ts (~250 lines), ThiingsGridObservers.ts (~80 lines)
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-component-refactoring/46-RESEARCH.md

@packages/web/lib/components/ThiingsGrid.tsx
@packages/web/lib/components/ThiingsGrid.best-practices.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PhysicsEngine class and Observer helpers from ThiingsGrid</name>
  <files>
    packages/web/lib/components/ThiingsGridPhysics.ts,
    packages/web/lib/components/ThiingsGridObservers.ts
  </files>
  <read_first>
    packages/web/lib/components/ThiingsGrid.tsx,
    packages/web/lib/components/ThiingsGrid.best-practices.md,
    packages/web/app/explore/ExploreClient.tsx
  </read_first>
  <action>
Create two new files extracted from ThiingsGrid.tsx:

**ThiingsGridPhysics.ts** — a standalone class (NOT a React component):
- Move `debounce` and `throttle` utility functions (lines 16-81)
- Move `getDistance` helper (line 83-87)
- Move `PhysicsState` type (lines 118-125)
- Create `ThiingsGridPhysics` class that encapsulates:
  - All physics state (`offset`, `startPos`, `velocity`, `isDragging`, `lastMoveTime`, `velocityHistory`)
  - `spiralPositions` cache and `ensureSpiralPositions()` / `getSpiralPosition()` methods
  - `startPhysicsLoop()` and the RAF `loop()` method
  - `handleDown()`, `handleMove()`, `handleUp()`, `handleWheel()` methods
  - `checkInfiniteScroll()` method
  - A `destroy()` method that cancels animationFrame and resets state
- Constructor accepts: `{ gridSize, onStateUpdate, onReachEnd, hasMore, isLoadingMore }` where `onStateUpdate` is a callback ThiingsGrid passes to receive physics-driven state changes (gridItems, isMoving, isDragging, maxVisibleIndex)
- Export `ThiingsGridPhysics` class and re-export `PhysicsState` type

**ThiingsGridObservers.ts** — helper functions:
- Move `initializeIntersectionObserver` logic — accepts container element, stagger caches, and returns the IntersectionObserver instance
- Move `initializeImageObserver` logic — accepts container element, returns IntersectionObserver
- Move `observeCardElements` and `observeImages` helper logic
- Export functions that ThiingsGrid calls from `componentDidMount` and `componentDidUpdate`

CRITICAL constraints:
- `{ passive: false }` on wheel and touchmove event listeners MUST be preserved
- `isComponentMounted` flag coordination — the physics class must expose a way to check/set this
- `WeakMap` stagger caches stay on the physics/observer class, not on ThiingsGrid
- DO NOT change any exported types or the default export of ThiingsGrid
  </action>
  <verify>
    <automated>test -f packages/web/lib/components/ThiingsGridPhysics.ts && test -f packages/web/lib/components/ThiingsGridObservers.ts && grep -q "class ThiingsGridPhysics" packages/web/lib/components/ThiingsGridPhysics.ts && grep -q "export" packages/web/lib/components/ThiingsGridObservers.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - ThiingsGridPhysics.ts exists with `class ThiingsGridPhysics` exported
    - ThiingsGridObservers.ts exists with observer setup functions exported
    - PhysicsEngine has a `destroy()` method
    - `passive: false` appears in event listener registrations within the physics or observer files
  </acceptance_criteria>
  <done>PhysicsEngine class and Observer helpers are standalone files with clean APIs</done>
</task>

<task type="auto">
  <name>Task 2: Slim ThiingsGrid.tsx to delegate to extracted modules</name>
  <files>packages/web/lib/components/ThiingsGrid.tsx</files>
  <read_first>
    packages/web/lib/components/ThiingsGrid.tsx,
    packages/web/lib/components/ThiingsGridPhysics.ts,
    packages/web/lib/components/ThiingsGridObservers.ts,
    packages/web/app/explore/ExploreClient.tsx,
    packages/web/lib/components/PostBadge.tsx,
    packages/web/lib/components/explore/ExploreCardCell.tsx
  </read_first>
  <action>
Rewrite ThiingsGrid.tsx to delegate to the extracted modules:

1. Keep all exported types at the top: `Position`, `PostSource`, `GridItem`, `GridItemInternal`, `ItemConfig`, `ThiingsGridProps` — these MUST remain exported from this file because ExploreClient.tsx, PostBadge.tsx, and ExploreCardCell.tsx import them.

2. Import `ThiingsGridPhysics` from `./ThiingsGridPhysics` and observer helpers from `./ThiingsGridObservers`.

3. The class component retains:
   - `constructor()` — creates `this.physicsEngine = new ThiingsGridPhysics(...)` with callbacks
   - `componentDidMount()` — calls `this.physicsEngine.start()`, sets up observers via helpers, registers event listeners (keeping `passive: false` on wheel/touchmove)
   - `componentDidUpdate(prevProps)` — updates physics engine when props change (items, gridSize, hasMore)
   - `componentWillUnmount()` — calls `this.physicsEngine.destroy()`, disconnects observers
   - `render()` — same JSX output as before

4. Remove ALL inlined physics logic, spiral math, debounce/throttle, observer setup from ThiingsGrid.tsx.

5. The `State` type stays in ThiingsGrid.tsx (it's React render state).

6. Preserve the default export: `export default ThiingsGrid;`

Target: ThiingsGrid.tsx should be ~200 lines (types ~50, class ~150).
  </action>
  <verify>
    <automated>LINE_COUNT=$(wc -l < packages/web/lib/components/ThiingsGrid.tsx); echo "ThiingsGrid: $LINE_COUNT lines"; test "$LINE_COUNT" -le 300 && grep -q "export default ThiingsGrid" packages/web/lib/components/ThiingsGrid.tsx && grep -q "export type GridItem" packages/web/lib/components/ThiingsGrid.tsx && grep -q "ThiingsGridPhysics" packages/web/lib/components/ThiingsGrid.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - ThiingsGrid.tsx is 300 lines or fewer (target ~200)
    - `export default ThiingsGrid` is present
    - `export type GridItem`, `export type ThiingsGridProps`, `export type ItemConfig`, `export type Position`, `export type PostSource` are all present
    - ThiingsGridPhysics is imported and instantiated
    - No `debounce` or `throttle` function definitions remain in ThiingsGrid.tsx
    - No `ensureSpiralPositions` or `getSpiralPosition` methods remain in ThiingsGrid.tsx
  </acceptance_criteria>
  <done>ThiingsGrid.tsx is under 300 lines, delegates to ThiingsGridPhysics and ThiingsGridObservers, all external imports are preserved</done>
</task>

</tasks>

<verification>
1. `wc -l packages/web/lib/components/ThiingsGrid.tsx` reports <= 300
2. `grep -c "export type" packages/web/lib/components/ThiingsGrid.tsx` shows all 5 type exports
3. `grep "import.*ThiingsGrid" packages/web/app/explore/ExploreClient.tsx` still works (import path unchanged)
4. `grep "import.*PostSource" packages/web/lib/components/PostBadge.tsx` still works
</verification>

<success_criteria>
- ThiingsGrid.tsx <= 300 lines (REF-01 satisfied)
- All exported types preserved (no breaking changes)
- Physics engine and observer setup in dedicated files
- No behavioral changes to grid rendering
</success_criteria>

<output>
After completion, create `.planning/phases/46-component-refactoring/46-01-SUMMARY.md`
</output>
