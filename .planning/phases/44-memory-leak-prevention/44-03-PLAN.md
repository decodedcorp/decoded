---
phase: 44-memory-leak-prevention
plan: 03
type: execute
wave: 2
depends_on: [44-01, 44-02]
files_modified: []
autonomous: false
requirements: [MEM-04]

must_haves:
  truths:
    - "TypeScript compilation succeeds with zero errors across the entire web package"
    - "No new ESLint warnings from react-hooks/exhaustive-deps introduced by changes"
    - "Dev server starts and renders main page without console errors related to GSAP or fetch"
  artifacts: []
  key_links:
    - from: "all modified files"
      to: "packages/web build"
      via: "tsc --noEmit + next build"
      pattern: "exit code 0"
---

<objective>
Verify all memory leak prevention changes compile, lint, and work correctly. Provide manual memory profiling instructions for MEM-04.

Purpose: Validate that Plan 01 (GSAP contextSafe + setTimeout) and Plan 02 (AbortController) changes are correct and don't introduce regressions.
Output: Build passes, lint passes, manual profiling checkpoint completed.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-memory-leak-prevention/44-RESEARCH.md
@.planning/phases/44-memory-leak-prevention/44-01-SUMMARY.md
@.planning/phases/44-memory-leak-prevention/44-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build and lint verification</name>
  <files></files>
  <read_first>
    .planning/phases/44-memory-leak-prevention/44-01-SUMMARY.md
    .planning/phases/44-memory-leak-prevention/44-02-SUMMARY.md
  </read_first>
  <action>
Run full verification suite:

1. TypeScript check: `cd packages/web && bunx tsc --noEmit`
2. ESLint check: `cd packages/web && bun run lint`
3. Build check: `cd packages/web && bun run build`

If any step fails:
- Read the error output carefully
- Fix the specific file causing the error
- Re-run the failing check
- Document any fixes made

Also run a targeted grep audit to confirm all changes landed:
```bash
# MEM-01: contextSafe usage
grep -r "contextSafe" packages/web/lib/components/ --include="*.tsx" -l

# MEM-02: signal support
grep -r "signal" packages/web/lib/api/client.ts
grep -r "({ signal })" packages/web/lib/hooks/admin/ --include="*.ts" -l

# MEM-03: setTimeout reduction
grep -rn "setTimeout" packages/web/lib/components/detail/ItemDetailCard.tsx
grep "requestAnimationFrame" packages/web/lib/components/main/TrendingListSection.tsx
```
  </action>
  <verify>
    <automated>cd packages/web && bunx tsc --noEmit && bun run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` exits with code 0
    - `bun run lint` exits with code 0 (or only pre-existing warnings, no new ones)
    - `bun run build` exits with code 0
    - grep confirms contextSafe in at least 3 component files
    - grep confirms signal in apiClient and at least 4 admin hook files
  </acceptance_criteria>
  <done>Full build, type check, and lint pass. All MEM-01/02/03 changes verified present via grep audit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Memory profiling verification (MEM-04)</name>
  <files></files>
  <action>
Present the memory profiling verification checkpoint to the user. The user will manually verify using Chrome DevTools that no memory leaks remain after all changes from Plan 01 and Plan 02.

What was built:
- GSAP contextSafe() applied to all event handler animations (TrendingListSection, MasonryGridItem, ImageDetailModal, DraggableDoodle, PolaroidCard, StickerPeel)
- AbortController signal threading through apiClient, adminFetch, useLogStream, VtonModal, uploadImage
- setTimeout workarounds replaced (ItemDetailCard direct addEventListener, TrendingListSection requestAnimationFrame, useImageUpload direct call)

Verification steps for user:
1. Start dev server: `cd packages/web && bun run dev`
2. Open Chrome at http://localhost:3000
3. Open DevTools (F12) > Memory tab
4. Click "Take heap snapshot" (snapshot 1 - baseline)
5. Navigate: main page -> feed -> item detail -> back to main (repeat 5 times)
6. Click "Take heap snapshot" (snapshot 2 - after navigation)
7. Click the garbage can icon to force GC
8. Click "Take heap snapshot" (snapshot 3 - after GC)
9. Select snapshot 3, change view to "Comparison" with snapshot 1
10. Check: #Delta column should show no significant positive growth in GSAP-related objects (Timeline, Tween), EventListener counts, or Detached DOM nodes
  </action>
  <verify>
    <automated>echo "Manual verification required - Chrome DevTools Memory profiling"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms no significant memory growth on repeated navigation (5x cycle)
    - No detached DOM nodes related to GSAP animations
    - EventListener count does not grow after repeated navigation + GC
  </acceptance_criteria>
  <done>User approves memory profiling results - no memory leaks detected on repeated navigation</done>
</task>

</tasks>

<verification>
- TypeScript, ESLint, and Next.js build all pass
- User confirms no memory growth on repeated navigation via Chrome DevTools
</verification>

<success_criteria>
All Phase 44 changes are verified: build passes, lint passes, and manual memory profiling confirms no leaks on repeated navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/44-memory-leak-prevention/44-03-SUMMARY.md`
</output>
