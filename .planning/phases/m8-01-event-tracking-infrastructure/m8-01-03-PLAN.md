---
phase: m8-01-event-tracking-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [m8-01-01, m8-01-02]
files_modified:
  - packages/web/app/layout.tsx
  - packages/web/lib/components/feed/FeedTabs.tsx
  - packages/web/lib/components/explore/ExploreGrid.tsx
  - packages/web/lib/components/search/SearchOverlay.tsx
  - packages/web/lib/components/detail/PostDetail.tsx
autonomous: true
requirements: [TRACK-01, TRACK-02, TRACK-03]

must_haves:
  truths:
    - "layout.tsx에서 behaviorStore의 flush 타이머가 초기화되어 30초마다 + 페이지 이탈 시 이벤트가 전송된다"
    - "피드 카드를 3초 이상 보면 dwell_time 이벤트가 dev 콘솔에 로깅된다"
    - "포스트를 클릭하면 post_click 이벤트가 dev 콘솔에 로깅된다"
    - "카테고리 필터를 변경하면 category_filter 이벤트가 dev 콘솔에 로깅된다"
    - "검색어를 제출하면 search_query 이벤트가 dev 콘솔에 로깅된다"
    - "페이지를 스크롤하면 25/50/75/100% 마일스톤에서 scroll_depth 이벤트가 dev 콘솔에 로깅된다"
  artifacts:
    - path: "packages/web/app/layout.tsx"
      provides: "flush 타이머 초기화 (initFlushTimer)"
    - path: "packages/web/lib/components/feed/FeedTabs.tsx"
      provides: "dwell_time 이벤트 (useTrackDwellTime 연결)"
    - path: "packages/web/lib/components/explore/ExploreGrid.tsx"
      provides: "category_filter + post_click 이벤트"
  key_links:
    - from: "packages/web/app/layout.tsx"
      to: "packages/web/lib/stores/behaviorStore.ts"
      via: "initFlushTimer() 호출 in useEffect"
      pattern: "initFlushTimer"
    - from: "packages/web/lib/components/feed/FeedTabs.tsx"
      to: "packages/web/lib/hooks/useTrackDwellTime.ts"
      via: "useTrackDwellTime(postId) ref 연결"
      pattern: "useTrackDwellTime"
---

<objective>
Plan 01(API) + Plan 02(store/hooks)를 실제 컴포넌트에 와이어링한다. 모든 6가지 이벤트 타입(post_click, post_view, spot_click, search_query, category_filter, dwell_time, scroll_depth)이 실제 UI에서 발화되도록 연결.

Purpose: 트래킹 인프라가 실제 유저 상호작용에서 작동하는 것을 증명한다.
Output: 기존 컴포넌트에 이벤트 훅 삽입, layout에 flush 타이머 초기화
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/m8-01-event-tracking-infrastructure/m8-01-CONTEXT.md
@.planning/phases/m8-01-event-tracking-infrastructure/m8-01-RESEARCH.md
@.planning/phases/m8-01-event-tracking-infrastructure/m8-01-01-SUMMARY.md
@.planning/phases/m8-01-event-tracking-infrastructure/m8-01-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs -->
From packages/web/lib/stores/behaviorStore.ts:
```typescript
export type EventType = 'post_click' | 'post_view' | 'spot_click' | 'search_query' | 'category_filter' | 'dwell_time' | 'scroll_depth';
export interface TrackEventPayload { event_type: EventType; entity_id?: string; metadata?: Record<string, unknown>; }
export const useBehaviorStore: UseBoundStore<StoreApi<BehaviorState>>;
export function initFlushTimer(): () => void;
```

From packages/web/lib/hooks/useTrackEvent.ts:
```typescript
export function useTrackEvent(): (payload: TrackEventPayload) => void;
```

From packages/web/lib/hooks/useTrackDwellTime.ts:
```typescript
export function useTrackDwellTime(entityId: string): React.RefObject<HTMLElement>;
```

From packages/web/lib/hooks/useTrackScrollDepth.ts:
```typescript
export function useTrackScrollDepth(): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: layout.tsx flush 타이머 + 주요 컴포넌트 이벤트 와이어링</name>
  <files>packages/web/app/layout.tsx, packages/web/lib/components/feed/FeedTabs.tsx, packages/web/lib/components/explore/ExploreGrid.tsx</files>
  <action>
**layout.tsx — flush 타이머 초기화:**
- 새 클라이언트 컴포넌트 `EventFlushProvider` 생성 (layout.tsx 내부 또는 별도 파일)
- useEffect에서 initFlushTimer() 호출, cleanup 반환
- useTrackScrollDepth() 호출 (모든 페이지에 스크롤 깊이 추적 적용)
- layout.tsx의 children 래핑에 EventFlushProvider 추가
- 별도 파일이 더 깔끔하면 `packages/web/app/EventFlushProvider.tsx` 생성 ('use client')

**FeedTabs.tsx — dwell_time 이벤트:**
- 먼저 파일을 읽어서 피드 카드가 렌더링되는 위치를 파악
- 각 FeedCard 또는 피드 아이템에 useTrackDwellTime(postId) ref를 연결
- 피드 아이템을 감싸는 div/article에 ref={dwellRef} 적용
- 피드 아이템 클릭 시 post_click 이벤트도 함께 삽입: track({ event_type: 'post_click', entity_id: postId, metadata: { source: 'feed' } })

**ExploreGrid.tsx — category_filter + post_click:**
- 먼저 파일을 읽어서 카테고리 필터와 그리드 아이템 구조 파악
- 카테고리 필터 변경 핸들러에 track({ event_type: 'category_filter', metadata: { category_id, category_name } }) 삽입
- 그리드 아이템 클릭에 track({ event_type: 'post_click', entity_id: postId, metadata: { source: 'explore' } }) 삽입

주의:
- 기존 컴포넌트 로직을 최소한으로 수정 — 이벤트 코드만 추가
- 컴포넌트를 읽어보고 실제 구조에 맞게 적응적으로 삽입
- 'use client' 지시어가 이미 있는 컴포넌트에서만 훅 사용
- files_modified 목록에 없는 파일도 필요하면 수정 가능 (실제 구조에 따라)
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>layout.tsx에 flush 타이머 + scroll_depth 활성화. FeedTabs에 dwell_time + post_click. ExploreGrid에 category_filter + post_click. TypeScript 컴파일 에러 없음.</done>
</task>

<task type="auto">
  <name>Task 2: search_query, spot_click, post_view 이벤트 와이어링</name>
  <files>packages/web/lib/components/search/SearchOverlay.tsx, packages/web/lib/components/detail/PostDetail.tsx</files>
  <action>
**SearchOverlay.tsx — search_query 이벤트:**
- 먼저 파일을 읽어서 검색 제출 핸들러 위치 파악
- 검색어 제출(form submit 또는 엔터키) 시 track({ event_type: 'search_query', metadata: { query: searchTerm } }) 삽입
- CONTEXT.md 결정: search_query metadata에 실제 검색어 저장 (m8-03 DYNUI-03 검색 갭 위젯에 필요)

**PostDetail.tsx (또는 posts/[id] 페이지 컴포넌트) — post_view + spot_click:**
- 먼저 파일을 읽어서 포스트 상세 페이지 구조 파악
- 페이지 마운트 시 post_view 이벤트: track({ event_type: 'post_view', entity_id: postId })
- useEffect에서 1회 발화 (dependency: postId)
- spot 클릭 핸들러에 spot_click 이벤트: track({ event_type: 'spot_click', entity_id: spotId, metadata: { post_id: postId } })

주의:
- 파일 경로가 다를 수 있음 — 먼저 읽어서 확인. 포스트 상세는 app/posts/[id]/ 하위일 수 있음
- 실제 컴포넌트 구조에 맞게 적응적으로 삽입
- post_view는 페이지 로드 시 1회만 — useEffect dependency에 postId 포함
- source 필드: Claude's Discretion으로 post_click에 source 포함 (metadata에 source: 'feed' | 'explore' | 'search' | 'profile')
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>SearchOverlay에 search_query. PostDetail에 post_view + spot_click. 총 6가지 이벤트 타입이 모두 실제 컴포넌트에 연결됨. TypeScript 컴파일 에러 없음.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` 에러 없음
2. dev 서버에서 각 이벤트 타입별 console.log 발화 확인 가능
3. grep으로 6가지 이벤트 타입이 각각 track() 호출에 포함됨 확인
</verification>

<success_criteria>
- 6가지 이벤트 타입(post_click, post_view, spot_click, search_query, category_filter, dwell_time) + scroll_depth가 실제 UI 인터랙션에서 발화
- flush 타이머가 layout 레벨에서 초기화
- 기존 컴포넌트의 비-트래킹 기능에 영향 없음
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/m8-01-event-tracking-infrastructure/m8-01-03-SUMMARY.md`
</output>
