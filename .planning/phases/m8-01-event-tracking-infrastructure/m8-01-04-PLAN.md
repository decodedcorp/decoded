---
phase: m8-01-event-tracking-infrastructure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/components/profile/DataSourcesCard.tsx
  - packages/web/lib/components/profile/index.ts
  - packages/web/app/profile/ProfileClient.tsx
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "DataSourcesCard.tsx 파일이 삭제되어 존재하지 않는다"
    - "ProfileClient.tsx에서 DataSourcesCard import와 렌더링이 제거되었다"
    - "ProfileClient.tsx에서 useSocialAccounts 훅 호출이 제거되었다"
    - "profile/index.ts barrel export에서 DataSourcesCard가 제거되었다"
    - "프로필 페이지가 에러 없이 렌더링된다"
  artifacts:
    - path: "packages/web/lib/components/profile/DataSourcesCard.tsx"
      provides: "DELETED — 파일이 존재하지 않아야 함"
    - path: "packages/web/app/profile/ProfileClient.tsx"
      provides: "DataSourcesCard 관련 코드 제거된 프로필 클라이언트"
    - path: "packages/web/lib/components/profile/index.ts"
      provides: "DataSourcesCard export 제거된 barrel"
  key_links:
    - from: "packages/web/app/profile/ProfileClient.tsx"
      to: "packages/web/lib/components/profile/index.ts"
      via: "DataSourcesCard import 제거됨"
      pattern: "DataSourcesCard"
---

<objective>
DataSourcesCard.tsx(미구현 인스타그램/핀터레스트 연동 UI)를 완전 삭제하고 관련 참조를 정리한다.

Purpose: 미구현 기능의 UI 노출 제거 — 유저 혼란 방지. CLEAN-01 요구사항 충족.
Output: 1개 파일 삭제, 2개 파일 수정
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/m8-01-event-tracking-infrastructure/m8-01-CONTEXT.md

<interfaces>
<!-- Current ProfileClient.tsx imports (line 7-27) -->
From packages/web/app/profile/ProfileClient.tsx:
```typescript
import {
  ProfileHeader, StatsCards, BadgeGrid, RankingList, BadgeModal,
  ProfileEditModal, ProfileDesktopLayout, ActivityTabs, ActivityContent,
  EmptyState, ActivityItemCard, type ActivityTab, ProfileBio, FollowStats,
  SavedGrid, StyleDNACard, ArchiveStats, InkEconomyCard,
  DataSourcesCard,  // <-- REMOVE THIS
} from "@/lib/components/profile";

import {
  useMe, useUserStats, useMyBadges, useMyRanking,
  useUserActivities, useProfileExtras,
  useSocialAccounts,  // <-- REMOVE THIS
  useTryOnCount,
} from "@/lib/hooks/useProfile";
```

<!-- Current profile/index.ts (line 22) -->
From packages/web/lib/components/profile/index.ts:
```typescript
export { DataSourcesCard } from "./DataSourcesCard";  // <-- REMOVE THIS LINE
```

<!-- Touch points in ProfileClient.tsx -->
Line 26: DataSourcesCard import
Line 36: useSocialAccounts import
Line ~367: <DataSourcesCard accounts={socialAccounts} /> (mobile layout)
Line ~390: <DataSourcesCard accounts={socialAccounts} /> (desktop layout)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DataSourcesCard 파일 삭제 + 참조 정리</name>
  <files>packages/web/lib/components/profile/DataSourcesCard.tsx, packages/web/lib/components/profile/index.ts, packages/web/app/profile/ProfileClient.tsx</files>
  <action>
3단계 수술적 제거:

1. **DataSourcesCard.tsx 삭제:**
   - `rm packages/web/lib/components/profile/DataSourcesCard.tsx`

2. **profile/index.ts에서 export 제거:**
   - line 22의 `export { DataSourcesCard } from "./DataSourcesCard";` 삭제

3. **ProfileClient.tsx에서 관련 코드 제거:**
   - import 목록에서 `DataSourcesCard` 제거 (line 26)
   - import 목록에서 `useSocialAccounts` 제거 (line 36) — Pitfall 7 방지
   - 컴포넌트 본문에서 `const { data: socialAccounts } = useSocialAccounts()` 또는 유사한 훅 호출 제거
   - JSX에서 `<DataSourcesCard accounts={socialAccounts} />` 2곳 제거 (모바일 ~line 367, 데스크톱 ~line 390)
   - socialAccounts 변수의 다른 사용처가 없는지 확인 — 없으면 안전하게 제거

주의사항 (CONTEXT.md 결정):
- DB 쿼리(queries/profile.ts)의 SocialAccount 페치 로직은 유지 — DB 테이블이 존재하므로
- 제거 후 레이아웃 재정리 없음 — 나머지 카드들이 자연스럽게 리플로우
- useSocialAccounts 훅 정의 자체(lib/hooks/useProfile.ts 내부)는 삭제하지 않음 — 다른 곳에서 사용될 수 있으므로. 단, ProfileClient.tsx에서의 호출만 제거
  </action>
  <verify>
    <automated>cd packages/web && ! test -f lib/components/profile/DataSourcesCard.tsx && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>DataSourcesCard.tsx 파일 존재하지 않음. ProfileClient.tsx와 index.ts에서 관련 import/export/render 모두 제거. TypeScript 컴파일 에러 없음.</done>
</task>

</tasks>

<verification>
1. `! test -f packages/web/lib/components/profile/DataSourcesCard.tsx` (파일 없음)
2. `grep -r "DataSourcesCard" packages/web/` 결과 0건
3. `grep -r "useSocialAccounts" packages/web/app/profile/` 결과 0건
4. `npx tsc --noEmit` 에러 없음
</verification>

<success_criteria>
- DataSourcesCard.tsx 완전 삭제
- ProfileClient.tsx에서 DataSourcesCard + useSocialAccounts 참조 완전 제거
- profile/index.ts barrel export 정리
- TypeScript 컴파일 에러 없음
- 프로필 페이지 렌더링에 영향 없음
</success_criteria>

<output>
After completion, create `.planning/phases/m8-01-event-tracking-infrastructure/m8-01-04-SUMMARY.md`
</output>
