---
phase: 41-read-hook-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/lib/hooks/useProfile.ts
  - packages/web/lib/hooks/useCategories.ts
  - packages/web/lib/utils/badge-mapper.ts
  - packages/web/lib/utils/ranking-mapper.ts
  - packages/web/lib/api/badges.ts
  - packages/web/lib/api/rankings.ts
  - packages/web/lib/api/categories.ts
  - packages/web/lib/api/index.ts
autonomous: true
requirements: [MIG-01, MIG-08]

must_haves:
  truths:
    - "useMyBadges() returns data from generated hook (not empty stub)"
    - "useMyRanking() returns data from generated hook (not empty stub)"
    - "useCategories() returns data from generated hook via customInstance (not raw fetch)"
    - "badge-mapper.ts imports types from generated/models (not lib/api/types)"
    - "ranking-mapper.ts imports types from generated/models (not lib/api/types)"
    - "lib/api/badges.ts, rankings.ts, categories.ts are deleted"
  artifacts:
    - path: "packages/web/lib/hooks/useProfile.ts"
      provides: "Migrated useMyBadges and useMyRanking using generated hooks"
      contains: "from \"@/lib/api/generated/badges/badges\""
    - path: "packages/web/lib/hooks/useCategories.ts"
      provides: "Migrated useCategories using generated hook"
      contains: "from \"@/lib/api/generated/categories/categories\""
    - path: "packages/web/lib/utils/badge-mapper.ts"
      provides: "Updated type imports to generated models"
      contains: "from \"@/lib/api/generated/models\""
    - path: "packages/web/lib/utils/ranking-mapper.ts"
      provides: "Updated type imports to generated models"
      contains: "from \"@/lib/api/generated/models\""
  key_links:
    - from: "packages/web/lib/hooks/useProfile.ts"
      to: "packages/web/lib/api/generated/badges/badges.ts"
      via: "import useMyBadges as useMyBadgesGenerated"
      pattern: "useMyBadgesGenerated"
    - from: "packages/web/lib/hooks/useProfile.ts"
      to: "packages/web/lib/api/generated/rankings/rankings.ts"
      via: "import useMyRankingDetail"
      pattern: "useMyRankingDetail"
    - from: "packages/web/lib/hooks/useCategories.ts"
      to: "packages/web/lib/api/generated/categories/categories.ts"
      via: "import useGetCategories"
      pattern: "useGetCategories"
---

<objective>
Migrate badges, rankings, and categories read hooks from manual stubs to Orval-generated hooks, and update mapper files to import types from generated models instead of lib/api/types.ts.

Purpose: These three domains are the simplest migration targets (badges and rankings are empty stubs, categories is a direct fetch). Migrating them first establishes the import-aliasing pattern for MIG-08 and validates the generated hook integration path.

Output: useMyBadges, useMyRanking, useCategories all use generated hooks; badge-mapper.ts and ranking-mapper.ts import from generated models; manual API files deleted.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-read-hook-migration/41-RESEARCH.md

<interfaces>
<!-- Generated hook signatures the executor needs -->

From packages/web/lib/api/generated/badges/badges.ts:
- useMyBadges(options?: { query?: Partial<UseQueryOptions<...>>, request?: ... }): UseQueryResult
- useListBadges(options?): UseQueryResult
- getMyBadgesQueryKey(): readonly string[]

From packages/web/lib/api/generated/rankings/rankings.ts:
- useMyRankingDetail(options?: { query?: Partial<UseQueryOptions<...>>, request?: ... }): UseQueryResult
- useListRankings(params?: ListRankingsParams, options?): UseQueryResult
- getMyRankingDetailQueryKey(): readonly string[]

From packages/web/lib/api/generated/categories/categories.ts:
- useGetCategories(options?: { query?: Partial<UseQueryOptions<...>>, request?: ... }): UseQueryResult
- getGetCategoriesQueryKey(): readonly string[]

From packages/web/lib/api/generated/models/:
- EarnedBadgeItem (replaces ApiEarnedBadgeItem — fields: id, name, type, rarity, earned_at, description, icon_url, progress, criteria)
- AvailableBadgeItem (replaces ApiAvailableBadgeItem — fields: id, name, rarity, progress, description, icon_url)
- MyRankingDetailResponse (replaces ApiMyRankingDetail — fields: overall_rank, total_points, weekly_points, monthly_points, solution_stats, category_rankings)
- CategoryRank (replaces ApiCategoryRank — fields: category_code, category_name, rank, points)
- CategoryResponse (replaces Category — fields: id, code, name: CategoryName, color_hex, display_order, is_active, description, icon_url)
- MyBadgesResponse (replaces MyBadgesResponse — fields: data: EarnedBadgeItem[], available_badges: AvailableBadgeItem[])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate badges/rankings/categories hooks and update mapper type imports</name>
  <read_first>
    packages/web/lib/hooks/useProfile.ts
    packages/web/lib/hooks/useCategories.ts
    packages/web/lib/utils/badge-mapper.ts
    packages/web/lib/utils/ranking-mapper.ts
    packages/web/lib/api/badges.ts
    packages/web/lib/api/rankings.ts
    packages/web/lib/api/categories.ts
    packages/web/lib/api/generated/badges/badges.ts
    packages/web/lib/api/generated/rankings/rankings.ts
    packages/web/lib/api/generated/categories/categories.ts
    packages/web/lib/api/generated/models/earnedBadgeItem.ts
    packages/web/lib/api/generated/models/availableBadgeItem.ts
    packages/web/lib/api/generated/models/myBadgesResponse.ts
    packages/web/lib/api/generated/models/myRankingDetailResponse.ts
    packages/web/lib/api/generated/models/categoryRank.ts
    packages/web/lib/api/generated/models/categoryResponse.ts
  </read_first>
  <files>
    packages/web/lib/hooks/useProfile.ts
    packages/web/lib/hooks/useCategories.ts
    packages/web/lib/utils/badge-mapper.ts
    packages/web/lib/utils/ranking-mapper.ts
  </files>
  <action>
**useProfile.ts — badges hook migration:**
1. Remove `import { fetchMyBadges } from "@/lib/api/badges"`.
2. Remove `import { fetchMyRanking } from "@/lib/api/rankings"`.
3. Add `import { useMyBadges as useMyBadgesGenerated } from "@/lib/api/generated/badges/badges"`.
4. Add `import { useMyRankingDetail as useMyRankingDetailGenerated } from "@/lib/api/generated/rankings/rankings"`.
5. Update type imports: replace `MyBadgesResponse` inline import from `@/lib/api/types` with `import type { MyBadgesResponse } from "@/lib/api/generated/models"`. Replace `ApiMyRankingDetail` inline import with `import type { MyRankingDetailResponse } from "@/lib/api/generated/models"`.
6. Remove `useQuery` from `@tanstack/react-query` import if no longer needed by `useMyBadges` or `useMyRanking` (check: `useMe`, `useUserStats`, `useUser`, `useProfileExtras`, `useSocialAccounts`, `useTryOnCount` still use it — keep it).

Rewrite `useMyBadges`:
```typescript
export function useMyBadges(
  options?: Omit<UseQueryOptions<MyBadgesResponse, Error>, "queryKey" | "queryFn">
) {
  return useMyBadgesGenerated({
    query: {
      queryKey: profileKeys.badges(),
      staleTime: 1000 * 60 * 5,
      ...options,
    }
  });
}
```

Rewrite `useMyRanking`:
```typescript
export function useMyRanking(
  options?: Omit<UseQueryOptions<MyRankingDetailResponse, Error>, "queryKey" | "queryFn">
) {
  return useMyRankingDetailGenerated({
    query: {
      queryKey: profileKeys.rankings(),
      staleTime: 1000 * 60 * 2,
      ...options,
    }
  });
}
```

Keep `useMe`, `useUserStats`, `useUserActivities`, `useUser`, `useUpdateProfile`, `useProfileExtras`, `useSocialAccounts`, `useTryOnCount` UNCHANGED — they are migrated in Plan 41-03.

**useCategories.ts — categories hook migration:**
1. Remove `import { getCategories, type Category } from "@/lib/api"`.
2. Add `import { useGetCategories as useGetCategoriesGenerated } from "@/lib/api/generated/categories/categories"`.
3. Add `import type { CategoryResponse } from "@/lib/api/generated/models"`.
4. Remove `useQuery` import from `@tanstack/react-query` (no longer needed — generated hook wraps it).

Rewrite `useCategories`:
```typescript
export function useCategories() {
  return useGetCategoriesGenerated({
    query: {
      queryKey: CATEGORIES_QUERY_KEY,
      staleTime: 1000 * 60 * 60,
      gcTime: 1000 * 60 * 60 * 24,
    }
  });
}
```

Update `useFindCategoryId` and `useFindCategory`: change `Category` type references to `CategoryResponse`. Note that `CategoryResponse.name` is `CategoryName` (object with `ko`/`en`), not `LocalizedName` — the shape is the same but the wrapper type name differs. The `code` field exists on both. Verify `categories.find((cat) => cat.code === code)` still works (it does — `CategoryResponse` has `code: string`).

Update `useCategoryCodeMap` similarly: `categories: Category[]` becomes `categories: CategoryResponse[]`.

For the return type of `useFindCategory`, change from `Category | undefined` to `CategoryResponse | undefined`.

**badge-mapper.ts — type import migration:**
1. Replace `import type { ApiEarnedBadgeItem, ApiAvailableBadgeItem } from "@/lib/api/types"` with:
   `import type { EarnedBadgeItem as ApiEarnedBadgeItem, AvailableBadgeItem as ApiAvailableBadgeItem } from "@/lib/api/generated/models"`.
2. No other changes needed — the alias keeps the internal variable names stable.
3. Verify field access: `item.id`, `item.name`, `item.type`, `item.rarity`, `item.earned_at`, `item.description`, `item.progress` — all exist on `EarnedBadgeItem`. `AvailableBadgeItem` has `id`, `name`, `rarity`, `progress`, `description`, `icon_url` — matches.

**ranking-mapper.ts — type import migration:**
1. Replace `import type { ApiMyRankingDetail, ApiCategoryRank } from "@/lib/api/types"` with:
   `import type { MyRankingDetailResponse as ApiMyRankingDetail, CategoryRank as ApiCategoryRank } from "@/lib/api/generated/models"`.
2. No other changes needed — aliases keep internal usage stable.
3. Verify field access: `detail.overall_rank`, `detail.total_points`, `detail.weekly_points`, `detail.monthly_points`, `detail.solution_stats`, `detail.category_rankings` — all exist on `MyRankingDetailResponse`. `cr.category_name`, `cr.category_code`, `cr.rank` — all exist on `CategoryRank`.
  </action>
  <verify>
    <automated>cd packages/web && bunx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/lib/hooks/useProfile.ts contains `from "@/lib/api/generated/badges/badges"`
    - packages/web/lib/hooks/useProfile.ts contains `from "@/lib/api/generated/rankings/rankings"`
    - packages/web/lib/hooks/useProfile.ts does NOT contain `from "@/lib/api/badges"`
    - packages/web/lib/hooks/useProfile.ts does NOT contain `from "@/lib/api/rankings"`
    - packages/web/lib/hooks/useProfile.ts contains `profileKeys.badges()` inside useMyBadges (cache key preserved)
    - packages/web/lib/hooks/useProfile.ts contains `profileKeys.rankings()` inside useMyRanking (cache key preserved)
    - packages/web/lib/hooks/useCategories.ts contains `from "@/lib/api/generated/categories/categories"`
    - packages/web/lib/hooks/useCategories.ts does NOT contain `from "@/lib/api"`
    - packages/web/lib/hooks/useCategories.ts contains `CATEGORIES_QUERY_KEY` inside useCategories
    - packages/web/lib/utils/badge-mapper.ts contains `from "@/lib/api/generated/models"`
    - packages/web/lib/utils/badge-mapper.ts does NOT contain `from "@/lib/api/types"`
    - packages/web/lib/utils/ranking-mapper.ts contains `from "@/lib/api/generated/models"`
    - packages/web/lib/utils/ranking-mapper.ts does NOT contain `from "@/lib/api/types"`
    - `cd packages/web && bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>useMyBadges, useMyRanking, and useCategories call generated hooks with preserved query keys; badge-mapper and ranking-mapper import from generated models; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete manual API files and update index.ts re-exports</name>
  <read_first>
    packages/web/lib/api/index.ts
    packages/web/lib/api/badges.ts
    packages/web/lib/api/rankings.ts
    packages/web/lib/api/categories.ts
  </read_first>
  <files>
    packages/web/lib/api/badges.ts
    packages/web/lib/api/rankings.ts
    packages/web/lib/api/categories.ts
    packages/web/lib/api/index.ts
  </files>
  <action>
1. Delete `packages/web/lib/api/badges.ts` (was empty stub returning `{ data: [], available_badges: [] }`).
2. Delete `packages/web/lib/api/rankings.ts` (was empty stub returning zeroed data).
3. Delete `packages/web/lib/api/categories.ts` (was raw fetch wrapper).

4. Update `packages/web/lib/api/index.ts`:
   - Remove the categories re-export block entirely:
     ```
     // Category APIs
     export {
       getCategories,
       findCategoryIdByCode,
       findCategoryById,
     } from "./categories";
     ```
   - Note: `findCategoryIdByCode` and `findCategoryById` are utility functions that were re-exported. Check if they are imported anywhere else. If they are, move them into `useCategories.ts` as standalone helper functions (they take a `Category[]` array parameter and are pure functions). The hook file already has similar functions (`useFindCategoryId`, `useFindCategory`). These standalone versions are likely unused since the hook versions exist — verify with grep before deleting.
   - The badges.ts and rankings.ts files are NOT re-exported from index.ts (they were imported directly by useProfile.ts), so no index.ts changes needed for those.

5. Run `grep -r "from.*['\"]@/lib/api/badges['\"]" packages/web/lib/ packages/web/app/ --include="*.ts" --include="*.tsx"` to confirm no remaining imports of the deleted file.
6. Run `grep -r "from.*['\"]@/lib/api/rankings['\"]" packages/web/lib/ packages/web/app/ --include="*.ts" --include="*.tsx"` to confirm no remaining imports.
7. Run `grep -r "from.*['\"]@/lib/api/categories['\"]" packages/web/lib/ packages/web/app/ --include="*.ts" --include="*.tsx"` to confirm no remaining imports.
8. Run `grep -r "findCategoryIdByCode\|findCategoryById" packages/web/ --include="*.ts" --include="*.tsx"` to check if standalone utility functions are used anywhere. If used, move them to `useCategories.ts` as exported pure functions.
  </action>
  <verify>
    <automated>cd packages/web && bunx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/lib/api/badges.ts does NOT exist
    - packages/web/lib/api/rankings.ts does NOT exist
    - packages/web/lib/api/categories.ts does NOT exist
    - packages/web/lib/api/index.ts does NOT contain `from "./categories"`
    - packages/web/lib/api/index.ts does NOT contain `getCategories`
    - packages/web/lib/api/index.ts does NOT contain `findCategoryIdByCode`
    - `grep -r "from.*@/lib/api/badges" packages/web/lib/ packages/web/app/ --include="*.ts" --include="*.tsx"` returns no matches
    - `grep -r "from.*@/lib/api/rankings" packages/web/lib/ packages/web/app/ --include="*.ts" --include="*.tsx"` returns no matches
    - `grep -r "from.*@/lib/api/categories" packages/web/lib/ packages/web/app/ --include="*.ts" --include="*.tsx"` returns no matches
    - `cd packages/web && bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Manual badges.ts, rankings.ts, categories.ts deleted; index.ts updated to remove stale re-exports; no remaining imports reference deleted files; TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
cd packages/web && bunx tsc --noEmit && echo "PASS" || echo "FAIL"
</verification>

<success_criteria>
- badges, rankings, categories domains fully migrated to generated hooks
- Mapper files import types from generated/models with aliasing
- Manual API files deleted with no orphan imports
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/41-read-hook-migration/41-01-SUMMARY.md`
</output>
