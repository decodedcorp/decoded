---
phase: 41-read-hook-migration
plan: 04
type: execute
wave: 2
depends_on: ["41-01", "41-02", "41-03"]
files_modified:
  - packages/web/lib/hooks/admin/useDashboard.ts
  - packages/web/lib/api/types.ts
  - packages/web/lib/api/index.ts
autonomous: true
requirements: [MIG-04, MIG-08]

must_haves:
  truths:
    - "Admin Next.js-internal hooks (useDashboardStats, useChartData, useTodaySummary, useAiCostKPI, useAuditList, usePipelines, useServerLogs) remain UNCHANGED"
    - "lib/api/types.ts is pruned: types with generated equivalents are removed, types still needed by mutations/upload are kept"
    - "No TypeScript compilation errors after type pruning"
    - "All imports of removed types are updated to use generated models"
  artifacts:
    - path: "packages/web/lib/api/types.ts"
      provides: "Pruned types.ts with only mutation/upload/utility types remaining"
    - path: "packages/web/lib/api/index.ts"
      provides: "Updated re-exports reflecting pruned types"
  key_links:
    - from: "packages/web/lib/api/types.ts"
      to: "packages/web/lib/api/generated/models/"
      via: "Types moved to generated — consumers import from generated"
      pattern: "from.*generated/models"
---

<objective>
Handle admin domain migration decisions and prune lib/api/types.ts by removing types that now have generated equivalents. Admin Next.js-internal hooks stay unchanged. Types still needed by mutations, uploads, and server-side functions are kept.

Purpose: This is the final cleanup for Phase 41. After Plans 01-03 migrated all read hooks, the manual types.ts still contains type definitions that now have equivalents in generated/models/. Removing them prevents dual-type confusion and establishes generated models as the SSOT.

Output: types.ts contains only mutation/upload/utility types; admin hooks unchanged; all consumer imports updated.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-read-hook-migration/41-RESEARCH.md

Depends on Plans 41-01, 41-02, 41-03 completing first.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify admin hooks stay unchanged, prune types.ts</name>
  <read_first>
    packages/web/lib/hooks/admin/useDashboard.ts
    packages/web/lib/hooks/admin/useAiCost.ts
    packages/web/lib/hooks/admin/useAudit.ts
    packages/web/lib/hooks/admin/usePipeline.ts
    packages/web/lib/hooks/admin/useServerLogs.ts
    packages/web/lib/api/admin/dashboard.ts
    packages/web/lib/api/types.ts
    packages/web/lib/api/index.ts
    packages/web/lib/api/posts.ts
    packages/web/lib/api/comments.ts
    packages/web/lib/api/spots.ts
    packages/web/lib/api/solutions.ts
  </read_first>
  <files>
    packages/web/lib/api/types.ts
    packages/web/lib/api/index.ts
  </files>
  <action>
**Step 1: Confirm admin hooks are untouched (MIG-04)**

Admin hooks in `packages/web/lib/hooks/admin/` (useDashboard.ts, useAiCost.ts, useAudit.ts, usePipeline.ts, useServerLogs.ts) call Next.js-internal routes backed by mock data. They have NO generated equivalents (except `useGetDashboardStats` which calls a different backend endpoint). Per research decision: keep ALL admin hooks exactly as-is. Do NOT modify any file in `packages/web/lib/hooks/admin/` or `packages/web/lib/api/admin/`.

Verify by reading each file and confirming no changes are needed.

**Step 2: Audit types.ts for removable types (MIG-08)**

After Plans 41-01 through 41-03, the following types in types.ts should no longer have consumers importing them (they were replaced by generated model imports):

Types SAFE TO REMOVE (have generated equivalents AND all consumers migrated):
- `MyBadgesResponse` — consumers now import from generated/models
- `ApiEarnedBadgeItem` — consumers use `EarnedBadgeItem` from generated
- `ApiAvailableBadgeItem` — consumers use `AvailableBadgeItem` from generated
- `ApiBadgeResponse` — was parent of ApiEarnedBadgeItem
- `ApiBadgeCriteria` — was used by ApiBadgeResponse
- `RankingListResponse` — was used by rankings.ts (deleted)
- `ApiMyRankingDetail` — consumers use `MyRankingDetailResponse` from generated
- `ApiCategoryRank` — consumers use `CategoryRank` from generated
- `ApiRankingItem` — was used by rankings.ts (deleted)
- `ApiRankingUser` — was used by rankings.ts (deleted)
- `ApiMyRanking` — was used by rankings.ts (deleted)
- `LocalizedName` — was used by Category type
- `CategoriesResponse` — was used by categories.ts (deleted)

Types SAFE TO REMOVE if consumers migrated in Plan 41-03:
- `UserResponse` — check if still imported by useProfile.ts or profileStore.ts. After Plan 41-03, useProfile.ts imports from generated. But `profileStore.ts` may still import from types.ts. Run `grep -r "UserResponse.*from.*types" packages/web/ --include="*.ts" --include="*.tsx"` to check.
- `UserStatsResponse` — similar check
- `PaginatedActivitiesResponse` — similar check
- `ActivitiesListParams` — similar check
- `UserActivityType`, `UserActivityItem`, `UserActivityPostMeta`, `UserActivitySpotMeta` — similar check
- `PostsListResponse`, `PostsListPagination` — check if usePosts.ts still uses from types. After Plan 41-03, usePosts.ts uses generated types.
- `Post`, `PostUser`, `PostMediaSource` — check if still used. May still be used by usePosts.ts PostsPage type or by mutation hooks.

Types that MUST STAY (still used by active mutation/upload/server code):
- `ApiError` — used by posts.ts upload error handling
- `UploadResponse` — used by posts.ts uploadImage
- `AnalyzeRequest`, `AnalyzeResponse`, `AnalyzeMetadata`, `DetectedItem` — used by posts.ts analyzeImage
- `ExtractPostMetadataRequest`, `ExtractPostMetadataResponse` — used by posts.ts
- `MediaSourceType`, `MediaSource`, `SpotRequest`, `ContextType`, `MediaMetadataItem` — used by posts.ts create functions
- `CreatePostRequest`, `CreatePostResponse` — used by posts.ts
- `SpotSolution`, `SpotWithSolutionRequest`, `CreatePostWithSolutionRequest` — used by posts.ts
- `Category` — check: was used by categories.ts (deleted), spots.ts type, and useCategories.ts. After migration, useCategories uses `CategoryResponse` from generated. BUT the `Spot` type has `category?: Category`. If `Spot` stays, `Category` stays. Actually check `Spot` usage — spots.ts mutation hooks use `Spot` from types.ts. So `Category` must stay until Phase 42.
- `Spot`, `SpotListResponse`, `CreateSpotDto`, `UpdateSpotDto` — used by spots.ts mutations
- `Solution`, `SolutionListItem` (manual version), `CreateSolutionDto`, `UpdateSolutionDto` — used by solutions.ts mutations
- `ExtractMetadataRequest`, `ExtractMetadataResponse`, `ConvertAffiliateRequest`, `ConvertAffiliateResponse` — used by solutions.ts
- `UpdatePostDto`, `PostResponse` — used by posts.ts updatePost
- `PostDetailResponse`, `SpotWithTopSolution`, `TopSolutionSummary` — check if posts.ts fetchPostDetail is deleted. If yes, and no other consumer, can remove. BUT `PostDetailResponse` may be used by `postDetailToImageDetail` adapter.
- `PostMagazineResponse` and all PostMagazine* types — used by useImages.ts usePostMagazine (if fetchPostMagazine still imports from posts.ts)
- `PostsListParams` — check if still used by posts.ts fetchPostsServer. YES — keep.
- `PostsListResponse` — check if still used by posts.ts fetchPostsServer. YES — keep.
- `Post` type — check if still used by fetchPostsServer return type or usePosts.ts PostsPage. If PostsPage uses `Post[]`, keep it.
- Coordinate utilities (`apiToStoreCoord`, `storeToApiCoord`) — these are functions, not types. Keep.
- `UpdateUserDto` — used by useProfile.ts useUpdateProfile mutation.

**Step 3: Execute the pruning**

Run `grep -rn` for each candidate type to confirm no remaining imports before removing. For each type being removed, search:
```bash
grep -rn "TypeName" packages/web/lib/ packages/web/app/ --include="*.ts" --include="*.tsx" | grep -v "generated/" | grep -v "node_modules"
```

Only remove types where grep returns zero matches (excluding generated/ and the types.ts file itself).

For types being removed, also check `packages/web/lib/api/index.ts` — it does `export * from "./types"`, so removing a type from types.ts automatically removes it from the barrel export. No separate index.ts change needed for type removal.

**Step 4: Clean up index.ts if needed**

After Plan 41-03, index.ts should have had users block removed. Verify current state. The `export * from "./types"` line stays (types.ts still has types). Remove any stale individual re-exports that reference deleted functions/files.

Expected final index.ts exports:
- `apiClient, getAuthToken` from `./client`
- `export * from "./types"` (barrel for remaining types)
- Post creation/upload functions from `./posts`
- Spot mutation functions from `./spots`
- Solution mutation functions from `./solutions`
- `AdoptSolutionDto, AdoptResponse` types from `./solutions`
  </action>
  <verify>
    <automated>cd packages/web && bunx tsc --noEmit 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/lib/hooks/admin/useDashboard.ts is UNCHANGED from before Phase 41
    - packages/web/lib/hooks/admin/useAiCost.ts is UNCHANGED
    - packages/web/lib/hooks/admin/useAudit.ts is UNCHANGED
    - packages/web/lib/hooks/admin/usePipeline.ts is UNCHANGED
    - packages/web/lib/hooks/admin/useServerLogs.ts is UNCHANGED
    - packages/web/lib/api/types.ts does NOT contain `MyBadgesResponse` interface
    - packages/web/lib/api/types.ts does NOT contain `ApiEarnedBadgeItem` interface
    - packages/web/lib/api/types.ts does NOT contain `ApiMyRankingDetail` interface
    - packages/web/lib/api/types.ts does NOT contain `RankingListResponse` interface
    - packages/web/lib/api/types.ts still contains `UploadResponse` interface
    - packages/web/lib/api/types.ts still contains `CreatePostRequest` interface
    - packages/web/lib/api/types.ts still contains `Spot` interface
    - packages/web/lib/api/types.ts still contains `CreateSolutionDto` interface
    - packages/web/lib/api/types.ts still contains `apiToStoreCoord` function
    - `grep -rn "ApiEarnedBadgeItem\|ApiMyRankingDetail\|MyBadgesResponse" packages/web/lib/ packages/web/app/ --include="*.ts" --include="*.tsx" | grep -v generated/ | grep -v node_modules` returns no matches
    - `cd packages/web && bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Admin hooks confirmed unchanged; types.ts pruned of types with generated equivalents; remaining types are strictly for mutation/upload/server use; no orphan type references; TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
cd packages/web && bunx tsc --noEmit && bun run lint 2>&1 | tail -10 && echo "PHASE 41 COMPLETE"
</verification>

<success_criteria>
- Admin domain hooks confirmed untouched (MIG-04 satisfied by explicit non-migration)
- types.ts pruned of all types that have generated equivalents
- No orphan imports reference removed types
- TypeScript and lint pass clean
- All five requirement IDs (MIG-01, MIG-02, MIG-03, MIG-04, MIG-08) addressed across Plans 01-04
</success_criteria>

<output>
After completion, create `.planning/phases/41-read-hook-migration/41-04-SUMMARY.md`
</output>
