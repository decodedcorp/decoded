---
phase: 41-read-hook-migration
plan: 04
type: execute
wave: 3
depends_on: ["41-01", "41-02", "41-03"]
files_modified:
  - packages/web/lib/hooks/admin/useDashboard.ts
  - packages/web/lib/api/types.ts
  - packages/web/lib/api/mutation-types.ts
  - packages/web/lib/api/index.ts
  - packages/web/lib/api/posts.ts
  - packages/web/lib/api/comments.ts
  - packages/web/lib/api/spots.ts
  - packages/web/lib/api/solutions.ts
  - packages/web/lib/api/client.ts
  - packages/web/lib/api/adapters/postDetailToImageDetail.ts
  - packages/web/lib/hooks/usePosts.ts
  - packages/web/lib/hooks/useSpots.ts
  - packages/web/lib/hooks/useSolutions.ts
  - packages/web/lib/hooks/useImages.ts
  - packages/web/lib/hooks/useProfile.ts
  - packages/web/lib/stores/profileStore.ts
  - packages/web/lib/utils/main-page-mapper.ts
autonomous: true
requirements: [MIG-04, MIG-08]

must_haves:
  truths:
    - "Admin Next.js-internal hooks (useDashboardStats, useChartData, useTodaySummary, useAiCostKPI, useAuditList, usePipelines, useServerLogs) remain UNCHANGED"
    - "lib/api/types.ts is DELETED — file does not exist after this plan"
    - "lib/api/mutation-types.ts exists with only mutation/upload/utility types that have no generated equivalents"
    - "All imports that referenced lib/api/types are updated to either generated/models or mutation-types.ts"
    - "No TypeScript compilation errors after types.ts deletion"
  artifacts:
    - path: "packages/web/lib/api/mutation-types.ts"
      provides: "Relocated mutation/upload/utility types — the only manual types remaining"
    - path: "packages/web/lib/api/index.ts"
      provides: "Updated re-exports: mutation-types.ts instead of types.ts"
  key_links:
    - from: "packages/web/lib/api/mutation-types.ts"
      to: "packages/web/lib/api/generated/models/"
      via: "Types split — read types in generated, mutation types in mutation-types.ts"
      pattern: "from.*mutation-types"
    - from: "packages/web/lib/api/index.ts"
      to: "packages/web/lib/api/mutation-types.ts"
      via: "export * from ./mutation-types"
      pattern: "from.*mutation-types"
---

<objective>
Handle admin domain migration decisions, relocate mutation/upload types from types.ts to mutation-types.ts, then DELETE types.ts per ROADMAP SC-5. Admin Next.js-internal hooks stay unchanged. After this plan, lib/api/types.ts no longer exists and all type imports come from either generated/models or mutation-types.ts.

Purpose: ROADMAP SC-5 requires types.ts to be deleted with all types imported from generated/model/. Types still needed by mutations, uploads, and server-side functions (which have no generated equivalents) are relocated to a new mutation-types.ts file. This establishes generated models as the single source of truth for types.

Output: types.ts deleted; mutation-types.ts created with retained types; all consumer imports updated; admin hooks unchanged.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-read-hook-migration/41-RESEARCH.md

Depends on Plans 41-01, 41-02, 41-03 completing first.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify admin hooks unchanged, audit types.ts for final consumer state</name>
  <read_first>
    packages/web/lib/hooks/admin/useDashboard.ts
    packages/web/lib/hooks/admin/useAiCost.ts
    packages/web/lib/hooks/admin/useAudit.ts
    packages/web/lib/hooks/admin/usePipeline.ts
    packages/web/lib/hooks/admin/useServerLogs.ts
    packages/web/lib/api/admin/dashboard.ts
    packages/web/lib/api/types.ts
    packages/web/lib/api/index.ts
    packages/web/lib/api/posts.ts
    packages/web/lib/api/comments.ts
    packages/web/lib/api/spots.ts
    packages/web/lib/api/solutions.ts
    packages/web/lib/api/client.ts
    packages/web/lib/api/adapters/postDetailToImageDetail.ts
    packages/web/lib/hooks/usePosts.ts
    packages/web/lib/hooks/useSpots.ts
    packages/web/lib/hooks/useSolutions.ts
    packages/web/lib/hooks/useImages.ts
    packages/web/lib/hooks/useProfile.ts
    packages/web/lib/stores/profileStore.ts
    packages/web/lib/utils/main-page-mapper.ts
  </read_first>
  <files>
    packages/web/lib/api/mutation-types.ts
    packages/web/lib/api/types.ts
    packages/web/lib/api/index.ts
    packages/web/lib/api/posts.ts
    packages/web/lib/api/comments.ts
    packages/web/lib/api/spots.ts
    packages/web/lib/api/solutions.ts
    packages/web/lib/api/client.ts
    packages/web/lib/api/adapters/postDetailToImageDetail.ts
    packages/web/lib/hooks/usePosts.ts
    packages/web/lib/hooks/useSpots.ts
    packages/web/lib/hooks/useSolutions.ts
    packages/web/lib/hooks/useImages.ts
    packages/web/lib/hooks/useProfile.ts
    packages/web/lib/stores/profileStore.ts
    packages/web/lib/utils/main-page-mapper.ts
  </files>
  <action>
**Step 1: Confirm admin hooks are untouched (MIG-04)**

Admin hooks in `packages/web/lib/hooks/admin/` (useDashboard.ts, useAiCost.ts, useAudit.ts, usePipeline.ts, useServerLogs.ts) call Next.js-internal routes backed by mock data. They have NO generated equivalents (except `useGetDashboardStats` which calls a different backend endpoint). Per research decision: keep ALL admin hooks exactly as-is. Do NOT modify any file in `packages/web/lib/hooks/admin/` or `packages/web/lib/api/admin/`.

Verify by reading each file and confirming no changes are needed.

**Step 2: Full consumer audit of types.ts**

After Plans 41-01 through 41-03, run grep to find ALL remaining imports of types.ts:
```bash
grep -rn "from.*[\"']@/lib/api/types[\"']\|from.*[\"']\./types[\"']\|from.*[\"']\.\./api/types[\"']" packages/web/lib/ packages/web/app/ --include="*.ts" --include="*.tsx" | grep -v generated/ | grep -v node_modules
```

For each match, classify the imported type as:
- **HAS generated equivalent** → update import to `from "@/lib/api/generated/models"`
- **NO generated equivalent (mutation/upload/utility)** → will go into `mutation-types.ts`

Types that MUST move to mutation-types.ts (no generated equivalent — used by active mutation/upload/server code):
- `ApiError` — used by client.ts error handling
- `UploadResponse` — used by posts.ts uploadImage
- `AnalyzeRequest`, `AnalyzeResponse`, `AnalyzeMetadata`, `DetectedItem` — used by posts.ts analyzeImage
- `ExtractPostMetadataRequest`, `ExtractPostMetadataResponse` — used by posts.ts
- `MediaSourceType`, `MediaSource`, `SpotRequest`, `ContextType`, `MediaMetadataItem` — used by posts.ts create functions
- `CreatePostRequest`, `CreatePostResponse` — used by posts.ts
- `SpotSolution`, `SpotWithSolutionRequest`, `CreatePostWithSolutionRequest` — used by posts.ts
- `Spot`, `SpotListResponse`, `CreateSpotDto`, `UpdateSpotDto` — used by spots.ts mutations
- `Solution`, `SolutionListItem` (manual version), `CreateSolutionDto`, `UpdateSolutionDto` — used by solutions.ts mutations
- `ExtractMetadataRequest`, `ExtractMetadataResponse`, `ConvertAffiliateRequest`, `ConvertAffiliateResponse` — used by solutions.ts
- `UpdatePostDto`, `PostResponse` — used by posts.ts updatePost
- `PostDetailResponse`, `SpotWithTopSolution`, `TopSolutionSummary` — check if still used by adapters/postDetailToImageDetail.ts
- `PostMagazineResponse`, `PostMagazineLayout*` types — used by useImages.ts usePostMagazine
- `PostsListParams`, `PostsListResponse`, `PostsListPagination` — used by posts.ts fetchPostsServer
- `Post`, `PostUser`, `PostMediaSource` — check if still used by usePosts.ts PostsPage type, main-page-mapper.ts
- `Category` — used by Spot type (and thus spots.ts mutations)
- `UpdateUserDto` — used by useProfile.ts useUpdateProfile
- Coordinate utility functions (`apiToStoreCoord`, `storeToApiCoord`) — these are functions, not just types; must also move
- All `Comment*` types used by comments.ts mutations (`CommentUser`, `CommentResponse`, `CreateCommentDto`)

Types SAFE TO REMOVE (generated equivalents AND all consumers migrated by Plans 01-03):
- `MyBadgesResponse`, `ApiEarnedBadgeItem`, `ApiAvailableBadgeItem`, `ApiBadgeResponse`, `ApiBadgeCriteria`
- `RankingListResponse`, `ApiMyRankingDetail`, `ApiCategoryRank`, `ApiRankingItem`, `ApiRankingUser`, `ApiMyRanking`
- `LocalizedName`, `CategoriesResponse`
- `UserResponse`, `UserStatsResponse` — ONLY if grep confirms no remaining consumer
- `PaginatedActivitiesResponse`, `ActivitiesListParams`, `UserActivityType`, `UserActivityItem`, `UserActivityPostMeta`, `UserActivitySpotMeta` — ONLY if grep confirms

**Step 3: Create mutation-types.ts**

Create `packages/web/lib/api/mutation-types.ts` containing ONLY the types that have no generated equivalent. Copy them from types.ts. Include the coordinate utility functions as well.

Add a header comment:
```typescript
/**
 * Manual types for mutations, uploads, and server-side functions.
 * These have NO generated equivalents — they are used by code paths
 * excluded from Orval generation (binary uploads, server functions,
 * Supabase-direct mutations).
 *
 * Phase 42 will migrate mutations to generated hooks and further
 * reduce this file.
 */
```

**Step 4: Update ALL consumers to import from new locations**

For each file that imports from `@/lib/api/types` or `./types`:
1. Types with generated equivalents → change to `from "@/lib/api/generated/models"`
2. Types without generated equivalents → change to `from "@/lib/api/mutation-types"` or `from "./mutation-types"`

Files to update (exhaustive list from grep — verify actual state after Plans 01-03):
- `packages/web/lib/api/client.ts`: `ApiError` → `from "./mutation-types"`
- `packages/web/lib/api/posts.ts`: all remaining type imports → `from "./mutation-types"`
- `packages/web/lib/api/spots.ts`: `Spot, SpotListResponse, CreateSpotDto, UpdateSpotDto` → `from "./mutation-types"`
- `packages/web/lib/api/solutions.ts`: all remaining type imports → `from "./mutation-types"`
- `packages/web/lib/api/adapters/postDetailToImageDetail.ts`: `PostDetailResponse, SpotWithTopSolution` → check if generated models have these. If yes, import from generated. If no, import from mutation-types.
- `packages/web/lib/hooks/usePosts.ts`: `Post, PostsListResponse, PostsListParams, UpdatePostDto, PostResponse` → split: types with generated → generated, others → mutation-types
- `packages/web/lib/hooks/useSpots.ts`: `Spot, CreateSpotDto, UpdateSpotDto` → `from "@/lib/api/mutation-types"`
- `packages/web/lib/hooks/useSolutions.ts`: mutation types → `from "@/lib/api/mutation-types"`
- `packages/web/lib/hooks/useImages.ts`: `Post, PostsListParams, PostMagazineResponse` → split by availability
- `packages/web/lib/hooks/useProfile.ts`: `UpdateUserDto` → `from "@/lib/api/mutation-types"`
- `packages/web/lib/stores/profileStore.ts`: `UserResponse, UserStatsResponse` → `from "@/lib/api/generated/models"` (these have generated equivalents)
- `packages/web/lib/utils/main-page-mapper.ts`: `Post` → check if generated `PostListItem` can replace it; if not, `from "@/lib/api/mutation-types"`

**Step 5: Delete types.ts**

After all consumers are updated and TypeScript compiles clean:
```bash
rm packages/web/lib/api/types.ts
```

**Step 6: Update index.ts**

Replace `export * from "./types"` with `export * from "./mutation-types"`.

Remove any stale individual re-exports that reference deleted functions/files.

Expected final index.ts exports:
- `apiClient, getAuthToken` from `./client`
- `export * from "./mutation-types"` (barrel for remaining types)
- Post creation/upload functions from `./posts`
- Spot mutation functions from `./spots`
- Solution mutation functions from `./solutions`
- `AdoptSolutionDto, AdoptResponse` types from `./solutions`

**Step 7: Final verification**

```bash
cd packages/web && bunx tsc --noEmit
grep -rn "from.*[\"']@/lib/api/types[\"']\|from.*[\"']\./types[\"']\|from.*[\"']\.\./api/types[\"']" packages/web/lib/ packages/web/app/ --include="*.ts" --include="*.tsx" | grep -v generated/ | grep -v node_modules
```

The grep MUST return zero matches. If any remain, update them.
  </action>
  <verify>
    <automated>cd packages/web && bunx tsc --noEmit 2>&1 | tail -30 && echo "---" && test ! -f packages/web/lib/api/types.ts && echo "types.ts DELETED" || echo "FAIL: types.ts still exists"</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/lib/hooks/admin/useDashboard.ts is UNCHANGED from before Phase 41
    - packages/web/lib/hooks/admin/useAiCost.ts is UNCHANGED
    - packages/web/lib/hooks/admin/useAudit.ts is UNCHANGED
    - packages/web/lib/hooks/admin/usePipeline.ts is UNCHANGED
    - packages/web/lib/hooks/admin/useServerLogs.ts is UNCHANGED
    - packages/web/lib/api/types.ts does NOT exist (deleted)
    - packages/web/lib/api/mutation-types.ts exists
    - packages/web/lib/api/mutation-types.ts contains `UploadResponse` interface
    - packages/web/lib/api/mutation-types.ts contains `CreatePostRequest` interface
    - packages/web/lib/api/mutation-types.ts contains `Spot` interface
    - packages/web/lib/api/mutation-types.ts contains `CreateSolutionDto` interface
    - packages/web/lib/api/mutation-types.ts contains `apiToStoreCoord` function
    - packages/web/lib/api/mutation-types.ts does NOT contain `MyBadgesResponse` (removed — has generated equivalent)
    - packages/web/lib/api/mutation-types.ts does NOT contain `ApiEarnedBadgeItem` (removed — has generated equivalent)
    - packages/web/lib/api/mutation-types.ts does NOT contain `ApiMyRankingDetail` (removed — has generated equivalent)
    - packages/web/lib/api/mutation-types.ts does NOT contain `RankingListResponse` (removed — has generated equivalent)
    - packages/web/lib/api/index.ts contains `from "./mutation-types"` (not `from "./types"`)
    - `grep -rn "from.*@/lib/api/types\|from.*\./types\|from.*\.\./api/types" packages/web/lib/ packages/web/app/ --include="*.ts" --include="*.tsx" | grep -v generated/ | grep -v node_modules` returns ZERO matches
    - `cd packages/web && bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Admin hooks confirmed unchanged; types.ts DELETED per ROADMAP SC-5; mutation-types.ts created with only mutation/upload/utility types; all consumer imports updated to generated/models or mutation-types; zero remaining imports of types.ts; TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
cd packages/web && bunx tsc --noEmit && bun run lint 2>&1 | tail -10 && test ! -f packages/web/lib/api/types.ts && echo "PHASE 41 COMPLETE — types.ts deleted" || echo "FAIL"
</verification>

<success_criteria>
- Admin domain hooks confirmed untouched (MIG-04 satisfied by explicit non-migration)
- types.ts DELETED per ROADMAP SC-5
- mutation-types.ts created with only types that have no generated equivalents
- All consumer imports updated to reference either generated/models or mutation-types.ts
- No orphan imports reference removed types or deleted types.ts
- TypeScript and lint pass clean
- All five requirement IDs (MIG-01, MIG-02, MIG-03, MIG-04, MIG-08) addressed across Plans 01-04
</success_criteria>

<output>
After completion, create `.planning/phases/41-read-hook-migration/41-04-SUMMARY.md`
</output>
