---
phase: 43-ci-hardening-and-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - packages/web/scripts/pre-push.sh
autonomous: true
requirements: [CI-01, CI-02, CI-03]

must_haves:
  truths:
    - "Generated files in packages/web/lib/api/generated/ are not tracked by git (except .gitkeep)"
    - "pre-push.sh regenerates API code from spec before typecheck runs"
    - "If the OpenAPI spec changed, the regenerated files feed into the existing TypeScript check which will catch mismatches"
  artifacts:
    - path: ".gitignore"
      provides: "Exclusion of generated API files with .gitkeep negation"
      contains: "!packages/web/lib/api/generated/.gitkeep"
    - path: "packages/web/scripts/pre-push.sh"
      provides: "Step 0: API spec drift check before lint/format/typecheck"
      contains: "bun run generate:api"
  key_links:
    - from: "packages/web/scripts/pre-push.sh"
      to: "packages/web/orval.config.ts"
      via: "bun run generate:api invokes orval --config orval.config.ts"
      pattern: "bun run generate:api"
    - from: ".gitignore"
      to: "packages/web/lib/api/generated/.gitkeep"
      via: "negation pattern preserves sentinel file"
      pattern: "!packages/web/lib/api/generated/.gitkeep"
---

<objective>
Wire .gitignore to exclude generated API files while preserving the .gitkeep sentinel, and add a Step 0 to the pre-push hook that regenerates API code before the TypeScript check catches any drift.

Purpose: CI-01/CI-02/CI-03 -- generated files should never be committed; spec drift is caught by the pre-push hook's typecheck after regeneration.
Output: Updated .gitignore with .gitkeep negation, updated pre-push.sh with generate:api step.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-ci-hardening-and-tooling/43-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .gitkeep negation to .gitignore and verify generated files are excluded</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current state -- line 54-55 has the generated/ exclusion but NO .gitkeep negation)
  </read_first>
  <action>
The root `.gitignore` already has `packages/web/lib/api/generated/` at line 55. It is MISSING the negation pattern for `.gitkeep`. The `.gitkeep` file IS currently tracked (confirmed via `git ls-files`).

Add the negation pattern immediately after line 55. The final block must look exactly like this:

```gitignore
# Orval generated API client (regenerated from OpenAPI spec)
packages/web/lib/api/generated/
!packages/web/lib/api/generated/.gitkeep
```

The order matters: exclusion first, then negation. Do NOT add a second `packages/web/lib/api/generated/` line -- only append the negation line after the existing one.

After editing, verify `.gitkeep` is still tracked by running `git ls-files packages/web/lib/api/generated/.gitkeep`. It should output the path. If it does not, something went wrong with the negation pattern.
  </action>
  <verify>
    <automated>git ls-files packages/web/lib/api/generated/.gitkeep | grep -q .gitkeep && echo "PASS: .gitkeep still tracked" || echo "FAIL: .gitkeep not tracked"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains the line `!packages/web/lib/api/generated/.gitkeep` immediately after `packages/web/lib/api/generated/`
    - `git ls-files packages/web/lib/api/generated/.gitkeep` outputs `packages/web/lib/api/generated/.gitkeep`
    - `git ls-files packages/web/lib/api/generated/` outputs ONLY `.gitkeep` (no other generated files)
  </acceptance_criteria>
  <done>.gitignore has both the exclusion and .gitkeep negation; only .gitkeep is tracked in git</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 0 (API spec drift check) to pre-push.sh</name>
  <files>packages/web/scripts/pre-push.sh</files>
  <read_first>
    - packages/web/scripts/pre-push.sh (current state -- 4 steps: ESLint, Prettier, TypeScript, Build)
    - packages/web/orval.config.ts (confirms input.target is "../api-server/openapi.json")
  </read_first>
  <action>
Insert a new Step 0 between the `cd "$SCRIPT_DIR/.."` line (line 17) and the existing `echo "=== 1. ESLint ==="` line (line 19). The new step regenerates API code from the OpenAPI spec so the subsequent TypeScript check (Step 3) catches any drift.

Insert this exact block after line 17 (after `cd "$SCRIPT_DIR/.."`) and before line 19 (before `echo "=== 1. ESLint ==="`):

```bash

echo "=== 0. API Spec Drift Check ==="
if [ -f "../api-server/openapi.json" ]; then
  bun run generate:api
  echo "    Regeneration complete — TypeScript check will catch drift"
else
  echo "    WARNING: ../api-server/openapi.json not found — skipping generation"
  echo "    (api-server directory required for spec drift detection)"
fi

```

Critical details:
- The CWD at this point is `packages/web/` (from `cd "$SCRIPT_DIR/.."`)
- The spec path `../api-server/openapi.json` is relative to `packages/web/`, matching `orval.config.ts` input.target
- If the spec file is missing (developer doesn't have backend checked out), emit a warning but do NOT block the push -- the TypeScript check will still run against whatever generated files are on disk
- The `bun run generate:api` command resolves to `orval --config orval.config.ts` (from package.json scripts)
- Turborepo caches the generation, so this is fast if the spec hasn't changed
- Do NOT add a `git diff --exit-code` step. Research concluded that TypeScript check (Step 3) is the actual safety net -- if the spec changed and generated types differ, consumers will break at type level
  </action>
  <verify>
    <automated>grep -q 'bun run generate:api' packages/web/scripts/pre-push.sh && grep -q 'api-server/openapi.json' packages/web/scripts/pre-push.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/web/scripts/pre-push.sh` contains the string `bun run generate:api`
    - `packages/web/scripts/pre-push.sh` contains the string `../api-server/openapi.json`
    - The generate step appears BEFORE the `=== 1. ESLint ===` line (step 0 runs first)
    - The script still has `set -euo pipefail` at the top
    - The spec-missing case uses an `if` guard, not a hard `exit 1`
    - `bash -n packages/web/scripts/pre-push.sh` exits 0 (valid syntax)
  </acceptance_criteria>
  <done>pre-push.sh runs generate:api as Step 0 before lint/typecheck, with a graceful fallback when spec file is absent</done>
</task>

</tasks>

<verification>
1. `.gitignore` has both the exclusion and negation pattern for generated files
2. `git ls-files packages/web/lib/api/generated/` returns only `.gitkeep`
3. `grep -n 'generate:api' packages/web/scripts/pre-push.sh` shows the step exists
4. `bash -n packages/web/scripts/pre-push.sh` validates shell syntax
</verification>

<success_criteria>
- CI-01: generate:api runs as part of pre-push (prebuild equivalent for local CI)
- CI-02: Spec drift detected via regeneration + TypeScript typecheck catching type mismatches
- CI-03: Generated directory gitignored with .gitkeep preserved
</success_criteria>

<output>
After completion, create `.planning/phases/43-ci-hardening-and-tooling/43-01-SUMMARY.md`
</output>
