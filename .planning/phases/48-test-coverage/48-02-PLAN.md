---
phase: 48-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/playwright.config.ts
  - packages/web/tests/auth.setup.ts
  - packages/web/tests/login.spec.ts
  - packages/web/tests/navigation.spec.ts
  - packages/web/tests/ai-pipeline.spec.ts
  - packages/web/tests/fixtures/test-image.jpg
  - packages/web/.gitignore
autonomous: true
requirements: [E2E-01, E2E-02, E2E-03, TEST-02, TEST-03]
must_haves:
  truths:
    - "playwright.config.ts uses 'bun dev' instead of 'yarn dev' and has auth setup project"
    - "Supabase REST-based storageState auth fixture exists and is structurally valid"
    - "Login flow E2E test exists with unauthenticated project config"
    - "Main page navigation E2E test exists using storageState auth"
    - "AI pipeline E2E test exists with mocked /api/v1/posts/analyze endpoint"
  artifacts:
    - path: "packages/web/playwright.config.ts"
      provides: "Updated Playwright config with bun, auth setup project, storageState"
      contains: "bun dev"
    - path: "packages/web/tests/auth.setup.ts"
      provides: "Supabase signInWithPassword storageState fixture"
      contains: "signInWithPassword"
    - path: "packages/web/tests/login.spec.ts"
      provides: "Login page E2E test"
      contains: "page.goto"
    - path: "packages/web/tests/navigation.spec.ts"
      provides: "Authenticated navigation E2E test"
      contains: "data-testid"
    - path: "packages/web/tests/ai-pipeline.spec.ts"
      provides: "AI upload pipeline E2E test with mocked backend"
      contains: "page.route"
  key_links:
    - from: "packages/web/playwright.config.ts"
      to: "packages/web/tests/auth.setup.ts"
      via: "setup project testMatch"
      pattern: "auth\\.setup\\.ts"
    - from: "packages/web/tests/navigation.spec.ts"
      to: "storageState.json"
      via: "chromium project storageState config"
      pattern: "storageState"
    - from: "packages/web/tests/ai-pipeline.spec.ts"
      to: "/api/v1/posts/analyze"
      via: "page.route mock intercept"
      pattern: "page\\.route.*analyze"
---

<objective>
Fix Playwright config (yarn to bun), create Supabase auth fixture with storageState, and write three E2E test specs for login, navigation, and AI pipeline flows.

Purpose: Close E2E-01 (config fix + auth fixture), E2E-02 (login + navigation tests), and E2E-03 (AI pipeline test). Tests are written for post-merge execution since the worktree cannot start a dev server.
Output: Updated playwright.config.ts, auth.setup.ts fixture, 3 E2E spec files, 1 test fixture image.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-test-coverage/48-RESEARCH.md

<interfaces>
<!-- Current Playwright config that needs updating -->

From packages/web/playwright.config.ts:
```typescript
// Current: webServer.command = "yarn dev" — must change to "bun dev"
// Current: single "chromium" project — must add "setup" and "chromium-no-auth" projects
// Current: no storageState — must add for authenticated tests
```

From packages/web/lib/supabase/client.ts (check during execution):
```typescript
// Need to confirm: Does the app use createBrowserClient (cookie-based SSR)
// or createClient (localStorage-based)?
// This affects how auth.setup.ts injects the session.
```

<!-- data-testid selectors to use in E2E specs (created by Plan 01) -->
Key selectors:
- [data-testid="thiings-grid"] — main page grid
- [data-testid="vton-modal"] — VTON modal
- [data-testid="vton-try-on-button"] — try-on trigger
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix playwright.config.ts and create auth storageState fixture</name>
  <files>packages/web/playwright.config.ts, packages/web/tests/auth.setup.ts, packages/web/.gitignore</files>
  <read_first>
    - packages/web/playwright.config.ts (current full config — understand all existing settings to preserve)
    - packages/web/lib/supabase/client.ts (confirm session storage method: localStorage vs cookies)
    - packages/web/.gitignore (check if .playwright/ is already gitignored)
    - packages/web/.env.local.example (check existing env var names for Supabase URL and anon key)
  </read_first>
  <action>
    1. Update `packages/web/playwright.config.ts`:
       - Change webServer.command from "yarn dev" to "bun dev"
       - Keep ALL existing settings (testDir, fullyParallel, forbidOnly, retries, workers, reporter, timeout, expect, use.baseURL, use.trace, use.screenshot, use.video, outputDir)
       - Replace the single "chromium" project with three projects:
         a. "setup" project: testMatch: /auth\.setup\.ts/, no dependencies
         b. "chromium" project: uses devices['Desktop Chrome'], storageState: '.playwright/storageState.json', dependencies: ['setup']
         c. "chromium-no-auth" project: uses devices['Desktop Chrome'], testMatch: /login\.spec\.ts/, NO storageState, NO dependencies
       - Add testIgnore for auth.setup.ts in the chromium and chromium-no-auth projects to prevent double-running

    2. Create `packages/web/tests/auth.setup.ts`:
       - Import { test as setup } from '@playwright/test'
       - Import { createClient } from '@supabase/supabase-js'
       - Define STORAGE_STATE_PATH as path.join(__dirname, '../.playwright/storageState.json')
       - Create setup('authenticate') that:
         a. Creates Supabase client with process.env.NEXT_PUBLIC_SUPABASE_URL and process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
         b. Calls supabase.auth.signInWithPassword with process.env.TEST_USER_EMAIL and process.env.TEST_USER_PASSWORD
         c. Throws if error or no session
         d. Navigates page to baseURL '/'
         e. Uses page.evaluate to inject the session into localStorage using the Supabase key format: `sb-{projectRef}-auth-token` where projectRef is extracted from the Supabase URL (the subdomain part)
         f. The localStorage value should be JSON.stringify({ access_token, refresh_token, ... }) matching the session object from signInWithPassword
         g. Saves page.context().storageState({ path: STORAGE_STATE_PATH })
       - IMPORTANT: Check lib/supabase/client.ts first. If it uses @supabase/ssr createBrowserClient (cookie-based), the injection pattern is different — use page.context().addCookies() instead.

    3. Add `.playwright/` to packages/web/.gitignore if not already present (storageState.json contains auth tokens).

    4. Ensure packages/web/.playwright/ directory is mentioned in .gitignore. Also add TEST_USER_EMAIL and TEST_USER_PASSWORD to .env.local.example as comments so future developers know they exist.
  </action>
  <verify>
    <automated>grep -c "bun dev" packages/web/playwright.config.ts && grep -c "setup" packages/web/playwright.config.ts && test -f packages/web/tests/auth.setup.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - playwright.config.ts webServer.command is "bun dev" (not "yarn dev")
    - Three projects defined: "setup", "chromium" (with storageState), "chromium-no-auth"
    - auth.setup.ts exists with Supabase signInWithPassword + storageState save
    - .playwright/ is in .gitignore
    - No yarn references remain in playwright.config.ts
  </acceptance_criteria>
  <done>Playwright config uses bun, has auth setup project with storageState pattern, and storageState output is gitignored. E2E-01 is satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Write E2E test specs for login, navigation, and AI pipeline</name>
  <files>packages/web/tests/login.spec.ts, packages/web/tests/navigation.spec.ts, packages/web/tests/ai-pipeline.spec.ts, packages/web/tests/fixtures/test-image.jpg</files>
  <read_first>
    - packages/web/app/login/page.tsx OR packages/web/app/(auth)/login/page.tsx (find login page — identify OAuth buttons, form elements)
    - packages/web/app/page.tsx OR packages/web/app/(main)/page.tsx (find main page — identify key sections)
    - packages/web/app/request/upload/page.tsx OR similar (find the AI upload page route)
    - packages/web/tests/visual-qa.spec.ts (existing Playwright test pattern — follow same style)
    - packages/web/lib/api/generated/ (check analyze endpoint path to mock correctly)
  </read_first>
  <action>
    1. Create `packages/web/tests/login.spec.ts` (chromium-no-auth project):
       - Test: "login page renders" — goto /login, assert page has OAuth button (Google) or login form visible
       - Test: "unauthenticated user redirected to login" — goto a protected route like /profile, expect URL to contain /login
       - Use getByRole and getByText selectors for login page elements (data-testid not needed on login page since it wasn't refactored in Phase 46)

    2. Create `packages/web/tests/navigation.spec.ts` (chromium project, uses storageState):
       - Test: "main page loads with grid" — goto /, waitForLoadState('networkidle'), assert [data-testid="thiings-grid"] is visible
       - Test: "can navigate to images page" — goto /images, assert URL is /images
       - Test: "can navigate to profile" — click profile link or goto /profile, assert page loads without redirect to login
       - These tests use data-testid selectors from Plan 01 where available

    3. Create `packages/web/tests/ai-pipeline.spec.ts` (chromium project, uses storageState):
       - IMPORTANT: Mock the AI endpoint using page.route() — NEVER call real backend
       - Test: "upload image and see mocked analysis result" —
         a. Mock `**/api/v1/posts/analyze` with page.route to return { items: [{ id: 'mock-1', label: 'Dress', confidence: 0.95 }] }
         b. Navigate to the upload page (check actual route — likely /request/upload or /upload)
         c. Use page.locator('input[type="file"]').setInputFiles() with the test fixture
         d. Click analyze/detect button
         e. Assert result container appears (use data-testid if available, otherwise a reasonable selector)
       - Read the actual upload page component to determine the correct route, button text, and result container

    4. Create `packages/web/tests/fixtures/test-image.jpg`:
       - Generate a minimal valid JPEG file programmatically using Node.js Buffer
       - Use the smallest valid JPEG possible (< 1KB): the JFIF header + minimal scan data
       - Alternative: use a base64-encoded minimal JPEG and write it with Buffer.from(base64, 'base64')
       - Command to generate: `node -e "const fs=require('fs'); const buf=Buffer.from('', 'base64'); fs.writeFileSync('packages/web/tests/fixtures/test-image.jpg', buf);"`

    CRITICAL CONSTRAINT: These E2E tests CANNOT be run in this worktree (no dev server). They must be structurally valid TypeScript that compiles without errors. Do NOT attempt to run playwright test. Verify only that the files exist and have correct structure.

    After writing all specs, run a TypeScript syntax check if possible: `cd packages/web && npx tsc --noEmit tests/login.spec.ts tests/navigation.spec.ts tests/ai-pipeline.spec.ts 2>&1 | head -20` (may not work without full TS project config for test files — acceptable to skip if it fails).
  </action>
  <verify>
    <automated>ls -la packages/web/tests/login.spec.ts packages/web/tests/navigation.spec.ts packages/web/tests/ai-pipeline.spec.ts packages/web/tests/fixtures/test-image.jpg 2>&1 && grep -c "page.route" packages/web/tests/ai-pipeline.spec.ts && echo "ALL E2E SPECS EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - login.spec.ts exists with >= 2 test cases for login page rendering and redirect behavior
    - navigation.spec.ts exists with >= 2 test cases using storageState auth for authenticated navigation
    - ai-pipeline.spec.ts exists with page.route() mock for /api/v1/posts/analyze — NEVER calls real backend
    - test-image.jpg exists as a valid JPEG file in tests/fixtures/
    - All spec files import from '@playwright/test' and follow existing test conventions
    - navigation.spec.ts uses [data-testid="thiings-grid"] selector (from Plan 01)
  </acceptance_criteria>
  <done>Three E2E spec files cover login flow, authenticated navigation, and AI pipeline with mocked backend. Test fixture image committed. E2E-02 and E2E-03 are satisfied. Tests are ready for post-merge execution.</done>
</task>

</tasks>

<verification>
- All E2E spec files exist and are valid TypeScript: `ls packages/web/tests/*.spec.ts | wc -l` (should be >= 4 including existing visual-qa.spec.ts)
- auth.setup.ts exists: `test -f packages/web/tests/auth.setup.ts`
- playwright.config.ts uses bun: `grep "bun dev" packages/web/playwright.config.ts`
- No yarn references: `grep "yarn" packages/web/playwright.config.ts | wc -l` (should be 0)
- AI pipeline test mocks backend: `grep "page.route" packages/web/tests/ai-pipeline.spec.ts`
- Test fixture exists: `test -f packages/web/tests/fixtures/test-image.jpg`
- NOTE: Playwright tests themselves are NOT run in this worktree — validation is structural only
</verification>

<success_criteria>
- playwright.config.ts: bun dev, 3 projects (setup, chromium, chromium-no-auth), storageState path
- auth.setup.ts: Supabase signInWithPassword + storageState save
- login.spec.ts: 2+ tests for login page
- navigation.spec.ts: 2+ tests for authenticated navigation with data-testid selectors
- ai-pipeline.spec.ts: 1+ test with page.route() mock, file upload, result assertion
- test-image.jpg: valid JPEG fixture < 5KB
- .playwright/ in .gitignore
</success_criteria>

<output>
After completion, create `.planning/phases/48-test-coverage/48-02-SUMMARY.md`
</output>
