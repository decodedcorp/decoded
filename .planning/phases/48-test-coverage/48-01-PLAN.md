---
phase: 48-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/tests/physics.test.ts
  - packages/web/tests/hooks.test.ts
  - packages/web/lib/components/ThiingsGrid.tsx
  - packages/web/lib/components/vton/VtonModal.tsx
  - packages/web/lib/components/vton/VtonPhotoArea.tsx
  - packages/web/lib/components/vton/VtonItemPanel.tsx
  - packages/web/lib/components/detail/ItemDetailCard.tsx
  - packages/web/lib/components/detail/ImageDetailModal.tsx
  - packages/web/lib/components/detail/OtherSolutionsList.tsx
autonomous: true
requirements: [REF-05, E2E-04, TEST-01]
must_haves:
  truths:
    - "ThiingsGridPhysics pure functions (debounce, throttle, getDistance) have passing unit tests"
    - "dataUrlToBlob helper has a passing unit test"
    - "All four refactored component trees have data-testid attributes on key interactive elements"
  artifacts:
    - path: "packages/web/tests/physics.test.ts"
      provides: "Unit tests for ThiingsGridPhysics pure functions"
      min_lines: 50
    - path: "packages/web/tests/hooks.test.ts"
      provides: "Unit tests for dataUrlToBlob and other pure helpers"
      min_lines: 15
    - path: "packages/web/lib/components/ThiingsGrid.tsx"
      provides: "data-testid on grid container and grid items"
      contains: "data-testid"
    - path: "packages/web/lib/components/vton/VtonModal.tsx"
      provides: "data-testid on modal root"
      contains: "data-testid"
    - path: "packages/web/lib/components/detail/ItemDetailCard.tsx"
      provides: "data-testid on card root and adopt button"
      contains: "data-testid"
    - path: "packages/web/lib/components/detail/ImageDetailModal.tsx"
      provides: "data-testid on modal root and close button"
      contains: "data-testid"
  key_links:
    - from: "packages/web/tests/physics.test.ts"
      to: "packages/web/lib/components/ThiingsGridPhysics.ts"
      via: "import { debounce, throttle, getDistance }"
      pattern: "import.*ThiingsGridPhysics"
---

<objective>
Add Vitest unit tests for Phase 46 extracted pure functions and mark all four refactored component trees with data-testid attributes for stable E2E selectors.

Purpose: Close REF-05 (data-testid marking) and E2E-04 (interactive component testid) requirements while establishing unit test coverage for the physics engine utilities.
Output: 2 new test files (physics.test.ts, hooks.test.ts) passing via `bun run test:unit`, plus ~15 data-testid attributes across 4 component trees.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-test-coverage/48-RESEARCH.md

<interfaces>
<!-- Key exports the executor needs for unit tests -->

From packages/web/lib/components/ThiingsGridPhysics.ts:
```typescript
export type Position = { x: number; y: number };
export function debounce<T extends (...args: unknown[]) => unknown>(fn: T, delay: number): T & { cancel: () => void };
export function throttle<T extends (...args: unknown[]) => unknown>(fn: T, limit: number, options?: { leading?: boolean; trailing?: boolean }): T;
export function getDistance(p1: Position, p2: Position): number;
export class ThiingsGridPhysics { /* ... constructor, spiral math, etc. */ }
```

From packages/web/lib/hooks/useVtonTryOn.ts:
```typescript
// WARNING: This file imports { useState, useCallback } from "react" at module scope.
// Importing dataUrlToBlob will trigger React module evaluation.
// Use dynamic import or extract to separate file if node env crashes.
export function dataUrlToBlob(dataUrl: string): Promise<Blob>;
export function copyImageToClipboard(dataUrl: string): Promise<void>;
```

From packages/web/vitest.config.ts:
```typescript
// environment: 'node', globals: true, include: ['tests/**/*.test.ts']
// alias: '@' -> __dirname
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Vitest unit tests for ThiingsGridPhysics and dataUrlToBlob</name>
  <files>packages/web/tests/physics.test.ts, packages/web/tests/hooks.test.ts</files>
  <read_first>
    - packages/web/lib/components/ThiingsGridPhysics.ts (full file — understand all exported functions, their signatures, edge cases)
    - packages/web/lib/hooks/useVtonTryOn.ts (lines 1-25 — understand dataUrlToBlob implementation and module-scope imports)
    - packages/web/vitest.config.ts (confirm environment: 'node', globals: true, path alias)
    - packages/web/tests/zod-validation.test.ts (existing test pattern — follow same import style)
  </read_first>
  <behavior>
    physics.test.ts:
    - getDistance returns 0 for identical points
    - getDistance returns correct Euclidean distance (3-4-5 triangle)
    - getDistance handles negative coordinates
    - debounce delays execution until delay elapses (vi.useFakeTimers)
    - debounce.cancel prevents execution
    - debounce resets timer on repeated calls (only fires once)
    - throttle fires immediately on first call when leading:true
    - throttle suppresses calls within limit window
    - throttle fires trailing call when trailing:true (default)

    hooks.test.ts:
    - dataUrlToBlob converts data:image/png;base64,... to Blob with correct mime type
    - dataUrlToBlob returns Blob with size > 0
    - dataUrlToBlob handles image/jpeg mime type
  </behavior>
  <action>
    Create `packages/web/tests/physics.test.ts`:
    - Import { debounce, throttle, getDistance } from '@/lib/components/ThiingsGridPhysics'
    - ThiingsGridPhysics.ts has NO browser/React imports — safe to import in node env
    - Write describe blocks for getDistance, debounce, throttle with the behaviors listed above
    - Use vi.useFakeTimers() / vi.advanceTimersByTime() for timing tests
    - Use vi.useRealTimers() in afterEach or per-test cleanup

    Create `packages/web/tests/hooks.test.ts`:
    - IMPORTANT: useVtonTryOn.ts imports React at module scope. Importing dataUrlToBlob directly will trigger React module evaluation.
    - Strategy: Try importing with `import { dataUrlToBlob } from '@/lib/hooks/useVtonTryOn'`. Since React IS in node_modules (bun workspace), this should resolve. If it fails due to 'use client' directive or JSX, use an inline re-implementation approach: copy the pure function logic into the test and test it there, noting in comment that it mirrors the source.
    - Actually, Node 22 + vitest node env can import React (it just can't use DOM). The useState/useCallback imports are fine — they only fail when CALLED, not when imported. So direct import should work.
    - Test dataUrlToBlob with a minimal base64 PNG data URL
    - Note: `atob` and `Blob` are available in Node 22+

    Run RED first: `cd packages/web && bunx vitest run tests/physics.test.ts tests/hooks.test.ts` — should fail (files don't exist yet, then tests should fail before implementation — but these test existing implementations, so they should pass immediately after writing correct assertions).

    Since we're testing EXISTING code, the TDD cycle is: write test -> verify green immediately.
  </action>
  <verify>
    <automated>cd packages/web && bunx vitest run tests/physics.test.ts tests/hooks.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - physics.test.ts has >= 8 test cases covering getDistance, debounce, throttle
    - hooks.test.ts has >= 2 test cases covering dataUrlToBlob
    - All tests pass with `bun run test:unit`
    - No browser-only APIs used in tests (no document, window, navigator)
  </acceptance_criteria>
  <done>All unit tests green. ThiingsGridPhysics pure functions and dataUrlToBlob have automated test coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add data-testid attributes to four refactored component trees</name>
  <files>
    packages/web/lib/components/ThiingsGrid.tsx,
    packages/web/lib/components/vton/VtonModal.tsx,
    packages/web/lib/components/vton/VtonPhotoArea.tsx,
    packages/web/lib/components/vton/VtonItemPanel.tsx,
    packages/web/lib/components/detail/ItemDetailCard.tsx,
    packages/web/lib/components/detail/ImageDetailModal.tsx,
    packages/web/lib/components/detail/OtherSolutionsList.tsx
  </files>
  <read_first>
    - packages/web/lib/components/ThiingsGrid.tsx (find root container div and item wrapper elements)
    - packages/web/lib/components/vton/VtonModal.tsx (find modal root, try-on button, result image)
    - packages/web/lib/components/vton/VtonPhotoArea.tsx (find photo area container)
    - packages/web/lib/components/vton/VtonItemPanel.tsx (find item panel container)
    - packages/web/lib/components/detail/ItemDetailCard.tsx (find card root, adopt button)
    - packages/web/lib/components/detail/ImageDetailModal.tsx (find modal root, close button, main image)
    - packages/web/lib/components/detail/OtherSolutionsList.tsx (find solutions list container)
  </read_first>
  <action>
    Add data-testid attributes to key interactive/structural elements in each component tree. Follow the kebab-case naming convention: `{component}-{element}`.

    ThiingsGrid.tsx:
    - Root container: data-testid="thiings-grid"
    - Each grid item wrapper: data-testid="thiings-grid-item"

    VtonModal.tsx:
    - Modal root: data-testid="vton-modal"
    - Try-on button: data-testid="vton-try-on-button"
    - Result image: data-testid="vton-result-image"

    VtonPhotoArea.tsx:
    - Container: data-testid="vton-photo-area"

    VtonItemPanel.tsx:
    - Container: data-testid="vton-item-panel"

    ItemDetailCard.tsx:
    - Card root: data-testid="item-detail-card"
    - Adopt button: data-testid="item-adopt-button"

    ImageDetailModal.tsx:
    - Modal root: data-testid="image-detail-modal"
    - Close button: data-testid="image-detail-close"
    - Main image: data-testid="image-detail-image"

    OtherSolutionsList.tsx:
    - List container: data-testid="item-solutions-list"

    IMPORTANT: Only add the data-testid prop to the appropriate JSX element. Do NOT change any other code, styling, or behavior. This is a purely additive change.

    Total: ~13-15 data-testid attributes across 7 files.
  </action>
  <verify>
    <automated>grep -r "data-testid" packages/web/lib/components/ThiingsGrid.tsx packages/web/lib/components/vton/ packages/web/lib/components/detail/ItemDetailCard.tsx packages/web/lib/components/detail/ImageDetailModal.tsx packages/web/lib/components/detail/OtherSolutionsList.tsx --include="*.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep finds >= 13 data-testid attributes across the target files
    - Each of the 4 component trees (ThiingsGrid, Vton*, ItemDetailCard, ImageDetailModal) has at least 1 data-testid
    - No functional code changes — only data-testid attributes added
    - Naming follows kebab-case convention: {component}-{element}
  </acceptance_criteria>
  <done>All four refactored component trees have data-testid attributes on key interactive elements. REF-05 and E2E-04 are satisfied.</done>
</task>

</tasks>

<verification>
- `cd packages/web && bunx vitest run --reporter=verbose` — all unit tests pass (existing + new)
- `grep -r "data-testid" packages/web/lib/components/ --include="*.tsx" | wc -l` — >= 13 attributes
- No TypeScript errors in modified files: `cd packages/web && bunx tsc --noEmit --skipLibCheck 2>&1 | grep -E "(ThiingsGrid|Vton|ItemDetail|ImageDetail|OtherSolutions)" | head -5` (should be empty)
</verification>

<success_criteria>
- 10+ unit test cases passing for ThiingsGridPhysics pure functions
- 2+ unit test cases passing for dataUrlToBlob
- 13+ data-testid attributes across ThiingsGrid, VtonModal tree, ItemDetailCard, ImageDetailModal
- `bun run test:unit` green with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/48-test-coverage/48-01-SUMMARY.md`
</output>
