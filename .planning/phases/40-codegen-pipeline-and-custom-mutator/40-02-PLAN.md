---
phase: 40-codegen-pipeline-and-custom-mutator
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - packages/web/package.json
  - package.json
  - turbo.json
  - packages/web/lib/api/generated/models/index.ts
  - packages/web/lib/api/generated/zod/index.zod.ts
autonomous: true
requirements: [INFRA-03, SPEC-04]

must_haves:
  truths:
    - "bun run generate:api in packages/web runs orval and produces generated hook files"
    - "bun run generate:api at repo root runs turbo generate and produces the same output"
    - "turbo.json has a generate task with outputs declared for caching"
    - "build and typecheck tasks depend on generate in turbo.json"
    - "Generated files have no double-prefix /api/v1/api/v1/ in any URL"
    - "bun run typecheck passes with generated files included"
    - "Generated directory contains per-tag .ts files, models/ directory, and zod/ directory"
  artifacts:
    - path: "packages/web/package.json"
      provides: "generate:api script (orval --config orval.config.ts)"
      contains: "generate:api"
    - path: "package.json"
      provides: "Root-level generate:api script (turbo run generate)"
      contains: "generate:api"
    - path: "turbo.json"
      provides: "Turborepo generate task with correct dependency ordering"
      contains: "generate"
    - path: "packages/web/lib/api/generated/"
      provides: "Per-tag hook files, models directory, zod directory"
  key_links:
    - from: "package.json (root)"
      to: "turbo.json"
      via: "generate:api script calls turbo run generate"
      pattern: '"generate:api".*"turbo run generate"'
    - from: "turbo.json"
      to: "packages/web/package.json"
      via: "turbo runs generate:api script from packages/web"
      pattern: '"generate".*"outputs"'
    - from: "turbo.json build task"
      to: "turbo.json generate task"
      via: "dependsOn includes generate"
      pattern: '"build".*"dependsOn".*"generate"'
    - from: "packages/web/lib/api/generated/*.ts"
      to: "packages/web/lib/api/mutator/custom-instance.ts"
      via: "import { customInstance } from mutator path"
      pattern: "custom-instance"
---

<objective>
Wire the `generate:api` script into packages/web, root package.json, and Turborepo so code generation runs before build/typecheck and is accessible from the repo root, then execute the full smoke test: generate all hooks from the OpenAPI spec, verify no double-prefix URLs, and confirm typecheck passes.

Purpose: This is the final plan of Phase 40 — it proves the entire codegen pipeline works end-to-end. Without this, Phase 41 (hook migration) cannot start because there are no generated hooks to migrate to. The root-level script enables `bun run generate:api` from anywhere per ROADMAP success criteria.

Output: Working `generate:api` script (both package and root level), updated `turbo.json`, populated `lib/api/generated/` directory, passing typecheck
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-codegen-pipeline-and-custom-mutator/40-RESEARCH.md
@.planning/phases/40-codegen-pipeline-and-custom-mutator/40-01-SUMMARY.md

<interfaces>
<!-- Key artifacts from Plan 01 that this plan depends on -->

From packages/web/lib/api/mutator/custom-instance.ts (created by Plan 01):
```typescript
export const customInstance = async <T>(config: AxiosRequestConfig, options?: AxiosRequestConfig): Promise<T>;
export type ErrorType<Error> = import("axios").AxiosError<Error>;
export type BodyType<BodyData> = BodyData;
```

From packages/web/orval.config.ts (finalized by Plan 01):
```typescript
// decodedApi block: tags-split, react-query hooks, custom mutator
// decodedApiZod block: standalone zod schemas
// hooks.afterAllFilesWrite: "./node_modules/.bin/prettier --write"
```

From turbo.json (current state — needs generate task):
```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": [".next/**", "dist/**"] },
    "dev": { "cache": false, "persistent": true },
    "lint": { "dependsOn": ["^build"] },
    "test": {}
  }
}
```

From packages/web/package.json scripts (current state — needs generate:api):
```json
{
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "lint": "eslint app lib",
  "format": "prettier --write .",
  "format:check": "prettier --check .",
  "typecheck": "tsc --noEmit",
  "test:visual": "playwright test tests/visual-qa.spec.ts"
}
```

From root package.json scripts (current state — needs generate:api):
```json
{
  "dev": "turbo run dev",
  "build": "turbo run build",
  "lint": "turbo run lint",
  "test": "turbo run test",
  ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generate:api script to package.json (web + root) and generate task to turbo.json</name>
  <files>packages/web/package.json, package.json, turbo.json</files>
  <read_first>
    - packages/web/package.json (current scripts block — add generate:api without changing existing scripts)
    - package.json (root — add generate:api script for monorepo-level access)
    - turbo.json (current tasks — add generate task, update build and typecheck dependsOn)
    - packages/web/orval.config.ts (confirm config file name for the orval CLI command)
  </read_first>
  <action>
**1. Add `generate:api` script to `packages/web/package.json`:**

In the `"scripts"` block, add this entry (after the existing `"typecheck"` line):

```json
"generate:api": "orval --config orval.config.ts"
```

The `orval` binary is in devDependencies and bun resolves it from `node_modules/.bin/`. The `--config` flag points to `orval.config.ts` which is at the packages/web root (same directory as package.json).

**2. Add `generate:api` script to root `package.json`:**

In the root `package.json` `"scripts"` block, add this entry (after the existing `"test"` line):

```json
"generate:api": "turbo run generate"
```

This enables `bun run generate:api` from the repo root per ROADMAP success criteria ("bun run generate:api를 실행하면" without directory qualification). RESEARCH.md explicitly documents this root script. Turbo dispatches to packages/web's `generate:api` script via the `generate` task definition.

**3. Update `turbo.json` to add `generate` task and update `build`/`typecheck` dependencies:**

Replace the entire turbo.json with:

```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "generate": {
      "outputs": ["lib/api/generated/**"]
    },
    "build": {
      "dependsOn": ["^build", "generate"],
      "outputs": [".next/**", "dist/**"]
    },
    "typecheck": {
      "dependsOn": ["generate"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "test": {}
  }
}
```

Changes from current turbo.json:
- ADD `"generate"` task with `"outputs": ["lib/api/generated/**"]` (enables Turborepo caching — if openapi.json unchanged, skip regeneration)
- UPDATE `"build"` dependsOn: `["^build"]` becomes `["^build", "generate"]` (generate runs before build)
- ADD `"typecheck"` task with `"dependsOn": ["generate"]` (generate runs before typecheck; previously typecheck had no entry in turbo.json)
- PRESERVE existing `"dev"`, `"lint"`, `"test"` tasks unchanged

The `generate` task has no `^` prefix in dependsOn (it does not depend on other packages building first). It produces output that `build` and `typecheck` consume.

NOTE: Turborepo maps the `generate` task name to `generate:api` in packages/web via script naming convention — the turbo task is `generate` and the package script must be named `generate:api` (turbo matches `generate` prefix). Verify this works in Task 2.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo && grep -q '"generate:api"' packages/web/package.json && grep -q '"generate:api".*"turbo run generate"' package.json && grep -q '"generate"' turbo.json && node -e "const t=require('./turbo.json'); const b=t.tasks.build.dependsOn; const tc=t.tasks.typecheck.dependsOn; console.log(b.includes('generate') && tc.includes('generate') ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/web/package.json` contains `"generate:api": "orval --config orval.config.ts"` in scripts
    - Root `package.json` contains `"generate:api": "turbo run generate"` in scripts
    - `turbo.json` contains a `"generate"` task with `"outputs": ["lib/api/generated/**"]`
    - `turbo.json` `build.dependsOn` includes both `"^build"` and `"generate"`
    - `turbo.json` `typecheck.dependsOn` includes `"generate"`
    - `turbo.json` `dev`, `lint`, `test` tasks are unchanged from current state
  </acceptance_criteria>
  <done>generate:api script exists in packages/web and root package.json, turbo.json has generate task with correct dependency ordering for build and typecheck</done>
</task>

<task type="auto">
  <name>Task 2: Run orval generate and execute full smoke test</name>
  <files>packages/web/lib/api/generated/models/index.ts, packages/web/lib/api/generated/zod/index.zod.ts</files>
  <read_first>
    - packages/web/orval.config.ts (confirm full config is correct before running)
    - packages/web/lib/api/mutator/custom-instance.ts (confirm mutator exists before running)
    - packages/api-server/openapi.json (confirm spec file exists at the input.target path — first 5 lines only to verify)
  </read_first>
  <action>
Run the full code generation pipeline and verify all smoke test criteria. Execute these steps in order:

**Step 1: Run orval generate**
```bash
cd packages/web && bun run generate:api
```
Expected: Orval reads `../api-server/openapi.json`, generates per-tag hook files in `lib/api/generated/`, generates models in `lib/api/generated/models/`, generates zod schemas in `lib/api/generated/zod/`, and runs `./node_modules/.bin/prettier --write` on all generated files.

If `prettier --write` fails (binary not found even with full path), check that prettier is in devDependencies and run `bun install` to ensure node_modules is up to date.

**Step 2: Verify output structure**
```bash
ls lib/api/generated/
ls lib/api/generated/models/
ls lib/api/generated/zod/
```
Expected: Multiple `.ts` files in generated/ (one per OpenAPI tag), a `models/` subdirectory with TypeScript type files, and a `zod/` subdirectory with `.zod.ts` files.

**Step 3: Verify no double-prefix**
```bash
grep -r "api/v1/api/v1" lib/api/generated/
```
Expected: ZERO matches. If any match found, this is a critical bug — the mutator baseURL is wrong.

**Step 4: Spot-check generated hook exports**
```bash
grep -l "useGetMyProfile\|useHomeFeed\|useListPosts\|useGetPostDetail" lib/api/generated/*.ts
```
Expected: At least some of these hooks exist in the generated files (exact names depend on operationIds in the spec).

**Step 5: Run typecheck**
```bash
bun run typecheck
```
Expected: Exit code 0, no type errors. If type errors appear in generated files, check:
- Missing ErrorType/BodyType exports in custom-instance.ts
- tsconfig.json `skipLibCheck` setting (should be true)
- If generated files use types not available, investigate the specific error

**Step 6: Verify generated files are gitignored**
```bash
cd /Users/kiyeol/development/decoded/decoded-monorepo && git status packages/web/lib/api/generated/
```
Expected: No generated files appear in git status (they are in .gitignore).

**Step 7: Verify root-level generate:api works**
```bash
cd /Users/kiyeol/development/decoded/decoded-monorepo && bun run generate:api
```
Expected: Turbo dispatches to packages/web, orval runs, exits cleanly. This confirms the ROADMAP success criterion that `bun run generate:api` works without directory qualification.

If any step fails, diagnose and fix before proceeding. The smoke test must be fully green before this task is complete.
  </action>
  <verify>
    <automated>cd /Users/kiyeol/development/decoded/decoded-monorepo/packages/web && test -d lib/api/generated/models && test -d lib/api/generated/zod && test "$(grep -rc 'api/v1/api/v1' lib/api/generated/ 2>/dev/null | awk -F: '{s+=$2}END{print s}')" = "0" && bun run typecheck && echo "SMOKE-PASS" || echo "SMOKE-FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/web/lib/api/generated/` contains multiple `.ts` files (at least 5 per-tag files)
    - `packages/web/lib/api/generated/models/` directory exists with TypeScript type definition files
    - `packages/web/lib/api/generated/zod/` directory exists with `.zod.ts` files
    - `grep -r "api/v1/api/v1" packages/web/lib/api/generated/` returns zero matches (no double-prefix)
    - `bun run typecheck` exits with code 0 (no type errors including generated files)
    - `git status packages/web/lib/api/generated/` shows no tracked changes (gitignored)
    - At least one generated file contains `import { customInstance }` referencing the mutator
    - `bun run generate:api` from repo root completes successfully via turbo
  </acceptance_criteria>
  <done>Full codegen pipeline produces typed hooks from OpenAPI spec, no double-prefix in URLs, typecheck passes, generated files are gitignored, root-level generate:api works via turbo</done>
</task>

</tasks>

<verification>
- `bun run generate:api` in packages/web exits cleanly (orval generates all files)
- `bun run generate:api` at repo root exits cleanly (turbo dispatches to packages/web)
- `ls packages/web/lib/api/generated/*.ts | wc -l` returns 5+ files
- `grep -r "api/v1/api/v1" packages/web/lib/api/generated/` returns 0 matches
- `bun run typecheck` in packages/web exits 0
- `git status packages/web/lib/api/generated/` shows nothing (gitignored)
- `node -e "const t=require('./turbo.json'); console.log(t.tasks.build.dependsOn.includes('generate'))"` returns true
- `grep "generate:api" package.json` confirms root-level script exists
</verification>

<success_criteria>
- generate:api script runs orval and populates lib/api/generated/ with per-tag hooks, models, and zod schemas
- Root-level `bun run generate:api` works via turbo without directory qualification
- No double-prefix URLs in any generated file
- TypeScript compilation passes including all generated files
- Turborepo pipeline correctly orders generate before build and typecheck
- Generated files are excluded from git tracking
</success_criteria>

<output>
After completion, create `.planning/phases/40-codegen-pipeline-and-custom-mutator/40-02-SUMMARY.md`
</output>
